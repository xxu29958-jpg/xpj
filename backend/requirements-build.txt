# Build-time deps for freezing the backend into a distributable EXE.
# Installed into a SEPARATE .venv-build so the runtime .venv stays lean
# (no OCR). PyInstaller's exact version comes from the checked-in Windows
# toolchain contract. See packaging/README.md + scripts/build_backend_exe.ps1.
-r requirements.txt
