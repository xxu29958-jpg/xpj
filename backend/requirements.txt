fastapi==0.136.1
uvicorn[standard]==0.46.0
sqlalchemy==2.0.49
pydantic==2.13.3
python-dotenv==1.2.2
python-multipart==0.0.27
Pillow==12.2.0
pillow-heif==1.3.0
tzdata==2026.2
jinja2==3.1.6
PyJWT[crypto]==2.13.0
# v1.1 Batch 3: Alembic adopted for forward-only schema changes; new
# tables/columns ship as Alembic revisions. (The legacy idempotent SQLite
# startup migrator was retired in the PG-only slimming — debt #4.)
alembic==1.16.4
# ADR-0041: PostgreSQL engine migration. psycopg 3 (binary wheels - no system
# libpq needed) is the SQLAlchemy driver for postgresql+psycopg:// URLs.
# PostgreSQL is the only database (debt #4); SQLite was retired in the PG-only slimming.
psycopg[binary]==3.3.4
