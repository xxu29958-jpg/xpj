# Consumer art and convenience wave

> Execute with superpowers:executing-plans. The main agent owns production changes; bounded read-only review may run through the requesting-code-review skill. This plan implements the existing full Goal, not a separate product or a new approval gate.

**Goal:** The five work domains feel like one mature household tool, with recognizable art, readable hierarchy and lower effort in first entry, everyday work and recovery.

**Base:** Qualified main `b3408fff332efb5dcbcaf0e80dc81d4edea0a430`; merge the separately qualified external-debt context slice before final integration. Work in the already isolated `xiaopiaojia-clean` checkout, branch `codex/product-art-convenience-20260905`; preserve its untracked `tmp/`.

**Architecture:** Keep the existing modular backend owners, native Web forms, Android state/Room/Outbox, shared semantic appearance and production shells. New art is presentation, never a state owner. No second application, synthetic production data or duplicate current styling stack.

**Tech stack:** Jinja and existing CSS/JavaScript; Kotlin/Compose and existing resources; project-bound raster art only where it improves the actual task.

## Allowed changes and exclusions

- Replace inconsistent decorative state art, hierarchy and copy in real Web and Android consumers. Preserve the receipt/coral-side-clip recognition and reject the green top-clip direction.
- Improve reachable first-entry and recovery actions and reduce redundant controls, preserving native/no-JS and permission behavior where applicable.
- Keep theme, texture, accent and custom-background preferences coherent across the affected screens. Inspect text, focus and long content with the actual background pipeline.
- Capability gaps discovered while doing this remain in the same product atlas. A change to durable publication or identity must be its own bounded vertical task, not smuggled into a visual patch.
- No local PostgreSQL, Android build/emulator, installer or full-suite testing. Cloud executes heavy lanes; local work is source checks and bounded browser visual interaction. No Windows lifecycle operation is opened.

## Execution sequence

- [ ] Inspect current production consumers and establish design context from Goal, contracts and real code. Historical screenshots are clues only. Do not present them as current runtime qualification.
- [ ] Build a compact shared art direction for the actual states: first receipt, settled empty, unavailable/retry and planning. Select only assets with a real consumer. Inspect generated bytes, composition and background treatment before publication.
- [ ] Improve the first-entry/empty/recovery composition through existing shared Web and Android consumers. Write behavior regressions before behavior changes; visual-only decisions are judged by the real rendered surface, not arbitrary CSS assertions.
- [ ] Complete hierarchy and convenience across the five primary domains and the real detail/form paths they lead to. Preserve all real permission, filtering, batch, keyboard, offline, conflict, loading and error actions. No page is complete merely because its image changed.
- [ ] Inspect 360/768/1440 Web surfaces, both appearances, selected custom-background and keyboard/no-JS paths. Use synthetic preview data only in an explicitly isolated visual harness; command/runtime acceptance still requires real cloud/authorized environment execution. Inspect real Android consumers using cloud artifacts and qualified connected execution, not a Web imitation.
- [ ] Retire superseded production assets/styles/components only after all current consumers move. Keep historical evidence outside the runtime separate from shipped assets.
- [ ] One bounded review, targeted regression for admitted defects, exact-head cloud qualification, normal integration and exact merge-main qualification. Update the product atlas with completed work and remaining limitations. This wave is not the whole Internal Beta RC.

## Load-bearing evidence

| Claim | Falsifier | Evidence |
|---|---|---|
| Art and hierarchy help a real task | First action pushed offscreen, unreadable content, decorative false state | Current production-template browser frames and actions; real Compose consumers |
| Convenience does not change authority | Wrong ledger/role, duplicated command, lost draft or recovery | Existing native form and Android state/transport regressions, only affected additions |
| One production visual vocabulary remains | Old asset or component still has a current consumer, missing light/dark counterpart | Consumer search plus real rendered asset loading |
| Appearance is usable, not merely configured | Background masks text/actions, stale theme/contrast or keyboard trap | Actual appearance controls and text/focus inspection |

## Starting observations

Read current shell, tokens, Inbox state consumers and the product atlas. Existing Web empty Inbox uses the old dozing mascot; Android uses its own MascotEmptyIllustration. The shared receipt/coral-side-clip mark is already present. There is no running prior visual-preview server on the previously used ports, so old browser tabs do not prove current runtime behavior. Design context has been recorded; no new art or production visual changes are qualified yet.

## First task: useful empty receipt states

The selected artwork is a quiet paper tray with receipts and a coral side clip, genuinely transparent and static. It is decorative, never proof that a queue was cleared, a sync succeeded or a balance is correct. The production template now treats a zero total separately from a filtered-empty queue: no zero-count filter navigation, table selection or bulk form when these controls have no rows. One shared native capture form is placed in the empty task or populated-page header, preserving its ledger query, CSRF body, required file input, visible filename and no-JS submission. Import, authorized Owner setup and confirmed-ledger navigation remain reachable. Viewer presentation has no upload form.

Test-first evidence: the real Jinja template, executed through an explicitly read-only Starlette harness with no app/database imports, failed the no-empty-filter assertion in about 0.5 seconds before changes. The same probe passes after implementation. Existing cloud route tests now cover both a zero queue with a stale filter and a nonempty queue with zero filter matches, plus native form ownership and existing nonempty OCC selection. The shared asset is served by the real static route in the existing art consumer test. This is presentation evidence only, not a claim of authenticated or financial runtime qualification.

The existing Android connected upload interaction test first requires neutral empty copy rather than the unsupported historical-clear claim; a Midnight viewer case requires no upload and an operable refresh. These tests are intentionally ahead of the Android resource/illustration implementation and need an observed cloud RED before that implementation. No local Android or PostgreSQL runs. Actual 360 px Web preview already places upload, import and ledger navigation above the bottom domain bar; 768/1440, dark/custom appearance and final-head cloud qualification remain open. This first task does not qualify the whole art/convenience wave or the full product.

Observed Android RED: `345aadb8`, Connected `33967458856`, execution `101309988110`, ran 101 tests and failed only the two new neutral-empty-title assertions. Production Android now uses the same transparent asset for Inbox and Ledger empty states through `ReceiptEmptyIllustration`; both old wrapper consumers moved and `MascotEmptyIllustration.kt` is removed. Other still-used mascot event assets/controllers remain untouched. Actual empty/loading/filter routing, upload/read-only/refresh callbacks and Ledger single-CTA selection are unchanged. Ledger's empty explanation now includes its existing direct-entry task.

That candidate's CI `33967458857` also failed five stale presentation checks across ordinary 1/2 (`101309992553`) and 2/2 (`101309992635`). FIX: test actual development/Desktop ledger links and the scoped native capture query instead of hidden fields from an irrelevant empty bulk form; seed a real pending row before asserting bulk CSRF/OCC or filtered-empty behavior; require neutral empty copy after batch rejection while retaining the complete undo/OCC assertions. Do not restore unusable controls or reduce permission/isolation assertions.

Actual Web template inspection at 360, 768 and 1440 px covers the light and dark artwork, including a 1440 px layout with document scroll width equal to viewport width. Existing Android interaction tests will publish three real synthetic-state captures (Paper Inbox writer, Midnight Inbox viewer and full Ledger empty writer) through Gradle's `additionalTestOutputDir`; the Connected workflow uploads only those named PNGs. This reuses the existing emulator run, adds no local emulator or second renderer, and follows the [AndroidX output-copy convention](https://android.googlesource.com/platform/frameworks/support/+/f2e05c341382db64d127118a13451dcaa554b702/benchmark/perfetto/src/main/java/androidx/benchmark/perfetto/PerfettoRule.kt). Capture retrieval and visual review are still required; a green interaction run alone does not establish visual quality. Custom-background inspection and the wider wave remain open.

Candidate `d743da15` passed CI `33968415752`, CodeQL `33968415738` and Connected `33968415787`. The three `consumer-art-previews` PNGs were retrieved and visually inspected: Paper Inbox writer, Midnight Inbox viewer and full Ledger empty writer. The real consumers show the shared art without an opaque image box and retain their upload/refresh/direct-entry actions. This is synthetic-state Compose rendering, not a claim that the full application journey or all devices are qualified. The bounded `1b0a7631..d743da15` review found no current code defects.

Custom-background inspection then admitted one current visual FIX: the new Web empty task had overridden its semantic surface with transparency, allowing the background image to interfere with text and links. A read-only browser assertion on the actual computed background failed before the fix. Removing that override restores the existing `surface-sunken` owner, and the same assertion passes in Paper (`rgb(239, 233, 220)`) and Midnight (`rgb(34, 30, 21)`). Mobile padding keeps text inside the surface. At 360 px with the actual custom-background controls, native capture ends at approximately 546 px, useful links remain above the bottom navigation and document width stays 360 px. Viewer and filtered-empty synthetic states were inspected separately. Keyboard Tab reaches the native file picker, then the upload button with a visible focus ring. The temporary background and viewport were restored through the real controls. These CSS changes require a new exact cloud candidate; the prior green is not reused for it.

Correction candidate `f2427982829a617dfe5e28032111f75f4b0ec273` passed CI `33969516195`, CodeQL `33969516165` and Connected `33969516216`; all three fresh preview PNGs were retrieved and are byte-identical to the already inspected frames. Targeted independent review of `d743da15..f2427982` found no additional defect. The art candidate now integrates main `690b28b4` (#363), whose change only restores cloud real-db capacity through the existing three-way partition. The resulting new art head must complete its own cloud gates before integration; previous candidate results are historical evidence, not a substitute.

Candidate `6eafa60a` passed Connected `33970451628`; its three fresh Compose captures were retrieved and inspected. CI `33970451630` failed before a MerchantAlias Tone test body with an IO continuation accessing Main after fixture teardown. This is not attributed to the artwork: its Android source is unchanged from `f2427982`. A separate, controlled fixture repair is in #365 and must qualify before integration. Historical `6eafa60a` is not a qualified candidate.

The same current captures exposed inconsistent primary controls within this task: Inbox uses `AppPrimaryButton`, while the genuine-empty Ledger used Material's unrelated default pill. The Ledger action now consumes the existing primary component with a decorative Add icon; label, role gating, single-CTA selection and callback are unchanged. The existing connected single-entry test now also clicks the action and requires the real manual-entry sheet to be displayed; it and the fresh artifact must verify the result. This is a visual-only correction, not a new button framework. The Owner's photo remains a reference, not a selected palette or layout; modern consumer quality is assessed in the actual application, not inferred from paper texture or a decorative asset.

Current integration includes independently qualified main `90a9bb19` (#364 budget first-step convenience) and #365's test-only asynchronous-completion repairs. The latter's controlled alias cleanup passed cloud JVM qualification at `c20bcb01`; its Connected run then exposed an independent missing wait before the initial Pending cache/API/Room completion. That existing test now waits for the real current ViewModel's Loaded postcondition, verifies preserved ownership on return and retains exact one/two-sync, route and consumed-request assertions. Its final source `45799237` is included here, with fresh independent qualification required for both candidates. No production dispatcher or navigation logic is changed by those test corrections.

Exact candidate `49cb0bedd7b9cea880848a0d1e77e359c552e086` passed CI `33975562003`, CodeQL `33975562007` and Connected `33975562011` (actual execution `101331538337`). The three fresh PNGs from artifact `9972338886` were retrieved and inspected: the Ledger direct-entry button now matches the Inbox primary component, the receipt has no opaque backdrop, and Midnight viewer remains read-only with refresh. The connected test also opens the real manual-entry sheet. These remain synthetic-state consumer frames, not whole-application or final visual acceptance.

#365 is merged as `872c759a9b326746a38f9d87b14dc7a24080b255`, with its own successful CI `33975834073`, CodeQL `33975833963` and Connected `33975833975`. The repository's strict required-status policy rejects an outdated PR base; main was therefore merged normally into this branch as `4bd66b89`, with no content difference from `49cb0bed`. This bookkeeping update and the resulting exact candidate need fresh cloud gates before normal merge, followed by the merged main's own qualification. No gate bypass, local heavy run or Windows lifecycle reopening is authorized.

First-task closure: final candidate `ada29913979df787f3fbcc9ea3e1f025e4cc66b0` passed CI `33976767803`, CodeQL `33976767794` and Connected `33976767791`. Its three retrieved consumer PNGs are byte-identical to the inspected `49cb0bed` frames. The final-head review found no new blocker. #362 was normally squash-merged as `fba121c9552d9dca4a03fe319da587d31f2bf2a9`, which independently passed CI `33977721474`, CodeQL `33977721459` and Connected `33977721470`. The wider wave remains open.

## Second task: readable consumer workspace frame

The physical scene is a household member checking daily spending in daylight or returning to the ledger in a dim room after work. The chosen direction is clean, low-chroma daylight surfaces and quiet graphite at night, with the existing evergreen reserved for useful actions and selections. The photo is not a palette mandate. Web and Android keep the existing appearance owners, permissions, data and background choices.

Web uses a clean default texture without persisting an implicit choice; stored fiber and custom images remain intact. The existing pure Node appearance contract observed RED because initialization stored fiber and illegal preferences fell back to fiber; the changed bootstrap/default projection passes in 0.24 seconds. Older implicitly saved fiber cannot be distinguished from a deliberate preference, so it is preserved. Neutrals were designed in OKLCH and exported as sRGB because the actual shipped ECharts 6.0.0 parser returned null for OKLCH and accepted the equivalent hex. No color adapter or new library is added.

Desktop and mobile consume one primary-navigation partial, with the same five destinations, ledger query, active-domain authority and pending badge. Functional icons retain visible labels and are hidden from assistive technology. The mobile navigation height is also the content/batch offset owner. Primary-page duplicate eyebrows and the top bar's duplicated ledger label are retired. Text is more readable, input corners are consistent, and surfaces no longer rely on heavy warm shadows. Android's actual default background brush now consumes ThemeVisuals instead of the independent BackgroundVisuals palette; that unused glow/texture data type and its implementation-prescribing test are retired. Custom-image and readability overlays are unchanged.

The read-only Jinja preview uses the current production templates/static files with synthetic populated Inbox, Ledger, Debts, Budget and Insights read models; no app/database imports or command routes exist. All five were visually inspected at 360 px, with desktop Ledger and dark Insights at 1440 px. A selected Ledger batch form exposed real overflow: child width 328 px exceeded its 305 px inner width and document scroll width was 348 px against a 345 px client area. The existing narrow layout now uses a nonwrapping column and two-column action groups with their explanations below. The same live probe now reports document/client width 345/345, child width 305, and the batch bottom at 728 px above navigation top 735 px. No financial submission was performed in this preview.

The actual light-to-dark preview also exposed stale canvas colors. Both current ECharts consumers only read tokens on initialization. The same pure Node contract executes both real chart scripts and the real theme writer: RED retained Paper colors and one render; GREEN reprojects theme/accent changes into the same two instances, retaining amount/label data. Each consumer observes only the existing data-theme/data-accent attributes. Browser inspection and exact candidate cloud qualification must still close this task. This shared-frame slice is not whole-product art acceptance, complete Android journeys or durable draft continuity.

Follow-up browser inspection on `29a6ec29` confirms the live dark/accent switch refreshes the category canvas without navigation. The 768 px frame has client/scroll width 753/753; keyboard Tab reaches the next action with a visible 2 px accent focus ring. At 360 px with scripts disabled, the native correction form retains all three enabled input fields, no false cancellation/count controls and width 345/345; scripting was restored. A populated viewer Inbox has no financial write or selection controls; the remaining POST forms are the legitimate ledger selector and logout. Temporary viewport overrides were reset. These remain synthetic read-only template observations, not authenticated command execution.

The bounded `fba121c9..29a6ec29` review found no additional current blocker beyond one admitted retirement-consumer omission. Cloud CI `33979427385`, Backend contracts job `101341953063`, failed because `_audit_token_parity.py` still opened deleted `BackgroundVisuals.kt`. The audit now checks the already-listed ThemeVisuals/Theme owners and no longer requires the dead data type. Two same-valued semantic CSS tokens are explicit sRGB literals, preserving their independent roles and the existing parity parser. The focused source-only lane passes all 130 checks; Ruff, the pure Node contract and diff checks pass. The revised exact head still requires new cloud qualification; `29a6ec29` is not called qualified.

Final frame candidate `78febc176890cb25e5352bce72a0f7064f694aa1` passed CI `33980002606`, CodeQL `33980002598` and Connected `33980002594`, including actual execution `101343446399`. Artifact `9973627398` contains three fresh PNGs byte-identical to the inspected `29a6ec29` frames. Targeted final review closed the known FIX with no new blocker. #366 was normally squash-merged as `d74ef57ea9794919783efe19ea2c052dba3d6e20`; both commits share tree `891df829eb32770521a78b1315df6ce9543cc475`.

The merge SHA independently passed CodeQL `33980781789` and Connected `33980781853`. CI `33980781739` failed only Desktop manager job `101345509216`: `test_real_edge_theme_change_stays_in_the_browser` received `None` from the test CDP client and failed JSON decoding (439 other tests passed, 8 skipped). The original exception was not recorded, so its precise navigation instant is unknown. Source tracing and the real probe executed in Node prove that absent document/control nodes throw `TypeError`; the CDP helper incorrectly accepted a script-exception RemoteObject as a null result. This is an admitted test-observation defect, not evidence of a production theme regression or a successful main qualification.

A separate test-only correction waits for the document/control preconditions and makes CDP script exceptions fail explicitly with safe line/column metadata. Existing theme/storage/cookie/pressed-state assertions, transport-only retry and deadlines remain. The new focused regression observed four expected RED cases and one ready-state pass in 0.63 seconds, then five GREEN cases in 0.55 seconds. No Edge process, database or heavy suite ran locally. The first local pytest run attempted its standard cleanup in an old global temporary root and emitted a pre-existing permission warning; subsequent checks use a new task-specific temporary root and no old directory or permission is repaired. The correction still needs exact cloud execution and bounded review. Manual-entry convenience and durable draft recovery remain the next product task, not part of this fixture repair.
