# Consumer art and convenience wave

> Execute with superpowers:executing-plans. The main agent owns production changes; bounded read-only review may run through the requesting-code-review skill. This plan implements the existing full Goal, not a separate product or a new approval gate.

**Goal:** The five work domains feel like one mature household tool, with recognizable art, readable hierarchy and lower effort in first entry, everyday work and recovery.

**Base:** Qualified main `b3408fff332efb5dcbcaf0e80dc81d4edea0a430`; merge the separately qualified external-debt context slice before final integration. Work in the already isolated `xiaopiaojia-clean` checkout, branch `codex/product-art-convenience-20260905`; preserve its untracked `tmp/`.

**Architecture:** Keep the existing modular backend owners, native Web forms, Android state/Room/Outbox, shared semantic appearance and production shells. New art is presentation, never a state owner. No second application, synthetic production data or duplicate current styling stack.

**Tech stack:** Jinja and existing CSS/JavaScript; Kotlin/Compose and existing resources; project-bound raster art only where it improves the actual task.

## Allowed changes and exclusions

- Replace inconsistent decorative state art, hierarchy and copy in real Web and Android consumers. Preserve the receipt/coral-side-clip recognition and reject the green top-clip direction.
- Improve reachable first-entry and recovery actions and reduce redundant controls, preserving native/no-JS and permission behavior where applicable.
- Keep theme, texture, accent and custom-background preferences coherent across the affected screens. Inspect text, focus and long content with the actual background pipeline.
- Capability gaps discovered while doing this remain in the same product atlas. A change to durable publication or identity must be its own bounded vertical task, not smuggled into a visual patch.
- No local PostgreSQL, Android build/emulator, installer or full-suite testing. Cloud executes heavy lanes; local work is source checks and bounded browser visual interaction. No Windows lifecycle operation is opened.

## Execution sequence

- [ ] Inspect current production consumers and establish design context from Goal, contracts and real code. Historical screenshots are clues only. Do not present them as current runtime qualification.
- [ ] Build a compact shared art direction for the actual states: first receipt, settled empty, unavailable/retry and planning. Select only assets with a real consumer. Inspect generated bytes, composition and background treatment before publication.
- [ ] Improve the first-entry/empty/recovery composition through existing shared Web and Android consumers. Write behavior regressions before behavior changes; visual-only decisions are judged by the real rendered surface, not arbitrary CSS assertions.
- [ ] Complete hierarchy and convenience across the five primary domains and the real detail/form paths they lead to. Preserve all real permission, filtering, batch, keyboard, offline, conflict, loading and error actions. No page is complete merely because its image changed.
- [ ] Inspect 360/768/1440 Web surfaces, both appearances, selected custom-background and keyboard/no-JS paths. Use synthetic preview data only in an explicitly isolated visual harness; command/runtime acceptance still requires real cloud/authorized environment execution. Inspect real Android consumers using cloud artifacts and qualified connected execution, not a Web imitation.
- [ ] Retire superseded production assets/styles/components only after all current consumers move. Keep historical evidence outside the runtime separate from shipped assets.
- [ ] One bounded review, targeted regression for admitted defects, exact-head cloud qualification, normal integration and exact merge-main qualification. Update the product atlas with completed work and remaining limitations. This wave is not the whole Internal Beta RC.

## Load-bearing evidence

| Claim | Falsifier | Evidence |
|---|---|---|
| Art and hierarchy help a real task | First action pushed offscreen, unreadable content, decorative false state | Current production-template browser frames and actions; real Compose consumers |
| Convenience does not change authority | Wrong ledger/role, duplicated command, lost draft or recovery | Existing native form and Android state/transport regressions, only affected additions |
| One production visual vocabulary remains | Old asset or component still has a current consumer, missing light/dark counterpart | Consumer search plus real rendered asset loading |
| Appearance is usable, not merely configured | Background masks text/actions, stale theme/contrast or keyboard trap | Actual appearance controls and text/focus inspection |

## Starting observations

Read current shell, tokens, Inbox state consumers and the product atlas. Existing Web empty Inbox uses the old dozing mascot; Android uses its own MascotEmptyIllustration. The shared receipt/coral-side-clip mark is already present. There is no running prior visual-preview server on the previously used ports, so old browser tabs do not prove current runtime behavior. Design context has been recorded; no new art or production visual changes are qualified yet.

## First task: useful empty receipt states

The selected artwork is a quiet paper tray with receipts and a coral side clip, genuinely transparent and static. It is decorative, never proof that a queue was cleared, a sync succeeded or a balance is correct. The production template now treats a zero total separately from a filtered-empty queue: no zero-count filter navigation, table selection or bulk form when these controls have no rows. One shared native capture form is placed in the empty task or populated-page header, preserving its ledger query, CSRF body, required file input, visible filename and no-JS submission. Import, authorized Owner setup and confirmed-ledger navigation remain reachable. Viewer presentation has no upload form.

Test-first evidence: the real Jinja template, executed through an explicitly read-only Starlette harness with no app/database imports, failed the no-empty-filter assertion in about 0.5 seconds before changes. The same probe passes after implementation. Existing cloud route tests now cover both a zero queue with a stale filter and a nonempty queue with zero filter matches, plus native form ownership and existing nonempty OCC selection. The shared asset is served by the real static route in the existing art consumer test. This is presentation evidence only, not a claim of authenticated or financial runtime qualification.

The existing Android connected upload interaction test first requires neutral empty copy rather than the unsupported historical-clear claim; a Midnight viewer case requires no upload and an operable refresh. These tests are intentionally ahead of the Android resource/illustration implementation and need an observed cloud RED before that implementation. No local Android or PostgreSQL runs. Actual 360 px Web preview already places upload, import and ledger navigation above the bottom domain bar; 768/1440, dark/custom appearance and final-head cloud qualification remain open. This first task does not qualify the whole art/convenience wave or the full product.

Observed Android RED: `345aadb8`, Connected `33967458856`, execution `101309988110`, ran 101 tests and failed only the two new neutral-empty-title assertions. Production Android now uses the same transparent asset for Inbox and Ledger empty states through `ReceiptEmptyIllustration`; both old wrapper consumers moved and `MascotEmptyIllustration.kt` is removed. Other still-used mascot event assets/controllers remain untouched. Actual empty/loading/filter routing, upload/read-only/refresh callbacks and Ledger single-CTA selection are unchanged. Ledger's empty explanation now includes its existing direct-entry task.

That candidate's CI `33967458857` also failed five stale presentation checks across ordinary 1/2 (`101309992553`) and 2/2 (`101309992635`). FIX: test actual development/Desktop ledger links and the scoped native capture query instead of hidden fields from an irrelevant empty bulk form; seed a real pending row before asserting bulk CSRF/OCC or filtered-empty behavior; require neutral empty copy after batch rejection while retaining the complete undo/OCC assertions. Do not restore unusable controls or reduce permission/isolation assertions.

Actual Web template inspection at 360, 768 and 1440 px covers the light and dark artwork, including a 1440 px layout with document scroll width equal to viewport width. Existing Android interaction tests will publish three real synthetic-state captures (Paper Inbox writer, Midnight Inbox viewer and full Ledger empty writer) through Gradle's `additionalTestOutputDir`; the Connected workflow uploads only those named PNGs. This reuses the existing emulator run, adds no local emulator or second renderer, and follows the [AndroidX output-copy convention](https://android.googlesource.com/platform/frameworks/support/+/f2e05c341382db64d127118a13451dcaa554b702/benchmark/perfetto/src/main/java/androidx/benchmark/perfetto/PerfettoRule.kt). Capture retrieval and visual review are still required; a green interaction run alone does not establish visual quality. Custom-background inspection and the wider wave remain open.

Candidate `d743da15` passed CI `33968415752`, CodeQL `33968415738` and Connected `33968415787`. The three `consumer-art-previews` PNGs were retrieved and visually inspected: Paper Inbox writer, Midnight Inbox viewer and full Ledger empty writer. The real consumers show the shared art without an opaque image box and retain their upload/refresh/direct-entry actions. This is synthetic-state Compose rendering, not a claim that the full application journey or all devices are qualified. The bounded `1b0a7631..d743da15` review found no current code defects.

Custom-background inspection then admitted one current visual FIX: the new Web empty task had overridden its semantic surface with transparency, allowing the background image to interfere with text and links. A read-only browser assertion on the actual computed background failed before the fix. Removing that override restores the existing `surface-sunken` owner, and the same assertion passes in Paper (`rgb(239, 233, 220)`) and Midnight (`rgb(34, 30, 21)`). Mobile padding keeps text inside the surface. At 360 px with the actual custom-background controls, native capture ends at approximately 546 px, useful links remain above the bottom navigation and document width stays 360 px. Viewer and filtered-empty synthetic states were inspected separately. Keyboard Tab reaches the native file picker, then the upload button with a visible focus ring. The temporary background and viewport were restored through the real controls. These CSS changes require a new exact cloud candidate; the prior green is not reused for it.

Correction candidate `f2427982829a617dfe5e28032111f75f4b0ec273` passed CI `33969516195`, CodeQL `33969516165` and Connected `33969516216`; all three fresh preview PNGs were retrieved and are byte-identical to the already inspected frames. Targeted independent review of `d743da15..f2427982` found no additional defect. The art candidate now integrates main `690b28b4` (#363), whose change only restores cloud real-db capacity through the existing three-way partition. The resulting new art head must complete its own cloud gates before integration; previous candidate results are historical evidence, not a substitute.

Candidate `6eafa60a` passed Connected `33970451628`; its three fresh Compose captures were retrieved and inspected. CI `33970451630` failed before a MerchantAlias Tone test body with an IO continuation accessing Main after fixture teardown. This is not attributed to the artwork: its Android source is unchanged from `f2427982`. A separate, controlled fixture repair is in #365 and must qualify before integration. Historical `6eafa60a` is not a qualified candidate.

The same current captures exposed inconsistent primary controls within this task: Inbox uses `AppPrimaryButton`, while the genuine-empty Ledger used Material's unrelated default pill. The Ledger action now consumes the existing primary component with a decorative Add icon; label, role gating, single-CTA selection and callback are unchanged. The existing connected single-entry test now also clicks the action and requires the real manual-entry sheet to be displayed; it and the fresh artifact must verify the result. This is a visual-only correction, not a new button framework. The Owner's photo remains a reference, not a selected palette or layout; modern consumer quality is assessed in the actual application, not inferred from paper texture or a decorative asset.

Current integration includes independently qualified main `90a9bb19` (#364 budget first-step convenience) and #365's test-only asynchronous-completion repairs. The latter's controlled alias cleanup passed cloud JVM qualification at `c20bcb01`; its Connected run then exposed an independent missing wait before the initial Pending cache/API/Room completion. That existing test now waits for the real current ViewModel's Loaded postcondition, verifies preserved ownership on return and retains exact one/two-sync, route and consumed-request assertions. Its final source `45799237` is included here, with fresh independent qualification required for both candidates. No production dispatcher or navigation logic is changed by those test corrections.

Exact candidate `49cb0bedd7b9cea880848a0d1e77e359c552e086` passed CI `33975562003`, CodeQL `33975562007` and Connected `33975562011` (actual execution `101331538337`). The three fresh PNGs from artifact `9972338886` were retrieved and inspected: the Ledger direct-entry button now matches the Inbox primary component, the receipt has no opaque backdrop, and Midnight viewer remains read-only with refresh. The connected test also opens the real manual-entry sheet. These remain synthetic-state consumer frames, not whole-application or final visual acceptance.

#365 is merged as `872c759a9b326746a38f9d87b14dc7a24080b255`, with its own successful CI `33975834073`, CodeQL `33975833963` and Connected `33975833975`. The repository's strict required-status policy rejects an outdated PR base; main was therefore merged normally into this branch as `4bd66b89`, with no content difference from `49cb0bed`. This bookkeeping update and the resulting exact candidate need fresh cloud gates before normal merge, followed by the merged main's own qualification. No gate bypass, local heavy run or Windows lifecycle reopening is authorized.

First-task closure: final candidate `ada29913979df787f3fbcc9ea3e1f025e4cc66b0` passed CI `33976767803`, CodeQL `33976767794` and Connected `33976767791`. Its three retrieved consumer PNGs are byte-identical to the inspected `49cb0bed` frames. The final-head review found no new blocker. #362 was normally squash-merged as `fba121c9552d9dca4a03fe319da587d31f2bf2a9`, which independently passed CI `33977721474`, CodeQL `33977721459` and Connected `33977721470`. The wider wave remains open.

## Second task: readable consumer workspace frame

The physical scene is a household member checking daily spending in daylight or returning to the ledger in a dim room after work. The chosen direction is clean, low-chroma daylight surfaces and quiet graphite at night, with the existing evergreen reserved for useful actions and selections. The photo is not a palette mandate. Web and Android keep the existing appearance owners, permissions, data and background choices.

Web uses a clean default texture without persisting an implicit choice; stored fiber and custom images remain intact. The existing pure Node appearance contract observed RED because initialization stored fiber and illegal preferences fell back to fiber; the changed bootstrap/default projection passes in 0.24 seconds. Older implicitly saved fiber cannot be distinguished from a deliberate preference, so it is preserved. Neutrals were designed in OKLCH and exported as sRGB because the actual shipped ECharts 6.0.0 parser returned null for OKLCH and accepted the equivalent hex. No color adapter or new library is added.

Desktop and mobile consume one primary-navigation partial, with the same five destinations, ledger query, active-domain authority and pending badge. Functional icons retain visible labels and are hidden from assistive technology. The mobile navigation height is also the content/batch offset owner. Primary-page duplicate eyebrows and the top bar's duplicated ledger label are retired. Text is more readable, input corners are consistent, and surfaces no longer rely on heavy warm shadows. Android's actual default background brush now consumes ThemeVisuals instead of the independent BackgroundVisuals palette; that unused glow/texture data type and its implementation-prescribing test are retired. Custom-image and readability overlays are unchanged.

The read-only Jinja preview uses the current production templates/static files with synthetic populated Inbox, Ledger, Debts, Budget and Insights read models; no app/database imports or command routes exist. All five were visually inspected at 360 px, with desktop Ledger and dark Insights at 1440 px. A selected Ledger batch form exposed real overflow: child width 328 px exceeded its 305 px inner width and document scroll width was 348 px against a 345 px client area. The existing narrow layout now uses a nonwrapping column and two-column action groups with their explanations below. The same live probe now reports document/client width 345/345, child width 305, and the batch bottom at 728 px above navigation top 735 px. No financial submission was performed in this preview.

The actual light-to-dark preview also exposed stale canvas colors. Both current ECharts consumers only read tokens on initialization. The same pure Node contract executes both real chart scripts and the real theme writer: RED retained Paper colors and one render; GREEN reprojects theme/accent changes into the same two instances, retaining amount/label data. Each consumer observes only the existing data-theme/data-accent attributes. Browser inspection and exact candidate cloud qualification must still close this task. This shared-frame slice is not whole-product art acceptance, complete Android journeys or durable draft continuity.

Follow-up browser inspection on `29a6ec29` confirms the live dark/accent switch refreshes the category canvas without navigation. The 768 px frame has client/scroll width 753/753; keyboard Tab reaches the next action with a visible 2 px accent focus ring. At 360 px with scripts disabled, the native correction form retains all three enabled input fields, no false cancellation/count controls and width 345/345; scripting was restored. A populated viewer Inbox has no financial write or selection controls; the remaining POST forms are the legitimate ledger selector and logout. Temporary viewport overrides were reset. These remain synthetic read-only template observations, not authenticated command execution.

The bounded `fba121c9..29a6ec29` review found no additional current blocker beyond one admitted retirement-consumer omission. Cloud CI `33979427385`, Backend contracts job `101341953063`, failed because `_audit_token_parity.py` still opened deleted `BackgroundVisuals.kt`. The audit now checks the already-listed ThemeVisuals/Theme owners and no longer requires the dead data type. Two same-valued semantic CSS tokens are explicit sRGB literals, preserving their independent roles and the existing parity parser. The focused source-only lane passes all 130 checks; Ruff, the pure Node contract and diff checks pass. The revised exact head still requires new cloud qualification; `29a6ec29` is not called qualified.

Final frame candidate `78febc176890cb25e5352bce72a0f7064f694aa1` passed CI `33980002606`, CodeQL `33980002598` and Connected `33980002594`, including actual execution `101343446399`. Artifact `9973627398` contains three fresh PNGs byte-identical to the inspected `29a6ec29` frames. Targeted final review closed the known FIX with no new blocker. #366 was normally squash-merged as `d74ef57ea9794919783efe19ea2c052dba3d6e20`; both commits share tree `891df829eb32770521a78b1315df6ce9543cc475`.

The merge SHA independently passed CodeQL `33980781789` and Connected `33980781853`. CI `33980781739` failed only Desktop manager job `101345509216`: `test_real_edge_theme_change_stays_in_the_browser` received `None` from the test CDP client and failed JSON decoding (439 other tests passed, 8 skipped). The original exception was not recorded, so its precise navigation instant is unknown. Source tracing and the real probe executed in Node prove that absent document/control nodes throw `TypeError`; the CDP helper incorrectly accepted a script-exception RemoteObject as a null result. This is an admitted test-observation defect, not evidence of a production theme regression or a successful main qualification.

A separate test-only correction waits for the document/control preconditions and makes CDP script exceptions fail explicitly with safe line/column metadata. Existing theme/storage/cookie/pressed-state assertions, transport-only retry and deadlines remain. The new focused regression observed four expected RED cases and one ready-state pass in 0.63 seconds, then five GREEN cases in 0.55 seconds. No Edge process, database or heavy suite ran locally. The first local pytest run attempted its standard cleanup in an old global temporary root and emitted a pre-existing permission warning; subsequent checks use a new task-specific temporary root and no old directory or permission is repaired. The correction still needs exact cloud execution and bounded review. Manual-entry convenience and durable draft recovery remain the next product task, not part of this fixture repair.

Test-observation closure: #367 candidate `3b24f8a3ee5f78516903621e374b7ec336d99c83` passed CI `33982107220`, CodeQL `33982107214` and the Connected scope gate `33982107222`; actual Connected was scope-skipped on that test/docs-only candidate. Bounded review closed with no new FIX. Normal merge `9cf0754c6c95982fe5e8184aed70d1073d491677` has the same tree and independently passed CI `33982506046`, CodeQL `33982506028` and actual Connected `33982506039` (execution `101350140089`). Main's real PostgreSQL, Android, Desktop and Windows build lanes all passed in cloud. The original d74 failure remains historical failure evidence, not a green run.

## Third task: interrupted manual entry remains one intent

**Goal:** A household member starts an expense on a phone between other tasks, or in a desktop browser after work. Amount and Save are immediately clear; optional detail stays available. Refreshing, leaving the form or reopening the browser does not silently replace the entered values or their create identity. The user can distinguish a retained draft, an uncertain submission and an acknowledged saved expense.

**Allowed changes:** The existing native manual-entry adapter/template, its scoped browser draft consumer, and the canonical pending/fact landing contexts needed to acknowledge that same creation. Reuse the current Dataset Authority, Account/Device, ledger admission and `create_manual_expense`; no new financial writer or domain model. Keep the current light/dark semantic surfaces and typography. Retire the old always-expanded arrangement as its real controls move, not their capabilities.

**Forbidden surface:** No change to device-scoped idempotency, payload fingerprinting, automatic category behavior, currency/time interpretation, authorization, token renewal, Android Room/Outbox, data-root ownership or held Windows lifecycles. No server-side partial Expense pretending to be an unsubmitted draft. No background publication or claim that an offline browser can load the whole application.

**Recovery design:**

1. A browser draft stores only the six editable field strings, `client_ref`, submission phase and update time. Its scope contains the existing `dataset_id`, `client_generation`, Account public ID, ledger and original Device public ID. Never persist CSRF, cookies, bearer credentials or internal numeric actor IDs in this store.
2. Keep each intent in a separate versioned localStorage entry. An opaque fragment identifies the current draft on refresh; the same manual-entry page lists retained drafts for explicit continuation. Ordinary cross-browser-session persistence is useful here; private-session closure and user-cleared storage cannot be advertised as recoverable. See [localStorage semantics](https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage).
3. Acquire the browser's exclusive per-intent [Web Lock](https://developer.mozilla.org/en-US/docs/Web/API/Web_Locks_API) before editing or retiring shared storage. A second tab cannot overwrite that draft and can start a separate fresh form. Never steal the first tab's lock. If storage or locking is unavailable, preserve the native form and show that local retention is unavailable; do not pretend autosave succeeded.
4. Preserve the exact submitted snapshot and original key before native submission. A response-loss retry is the same payload and same Device key, not a new expense. An uncertain submitted snapshot is read-only until the server explicitly rejects it or its canonical Expense is observed. No automatic retry.
5. Only the canonical saved Expense, scoped to the current authenticated Device, can supply the acknowledgement consumed on the pending or confirmed landing page. Query strings and a submit event cannot clear a draft. Ordinary validation rejection retains values and key while permitting correction; changed identity/ledger/role remains blocked by the existing server guard.
6. A changed Account, ledger or dataset generation never receives an automatic restoration. A retained old Device draft in the same Account/ledger remains readable for manual reconciliation, not automatically rebound or retried as a new Device. This task does not claim transparent replay after re-enrollment; the existing fact list and explicit new-entry path remain the decision boundary.
7. Keep amount/currency prominent and optional merchant/category/date/note in native details. Server errors and nonempty restored optional values expose the section. Without JavaScript, all original fields and the native command remain usable; native invalid controls must reveal their section before focus.

**Done checks and evidence:** Extend the existing real PostgreSQL native-form tests first, covering full draft binding, canonical confirmed/pending acknowledgement, forged query markers, replacement Device, rejection and role/ledger refusal. Execute the real browser script in narrow pure Node regressions for snapshot/key retention and storage failure; use exact-head cloud Edge for real storage, lock, refresh/navigation and acknowledgement behavior. Inspect the real production template at 360/768/1440 in both appearances, with no financial writes in the preview. Run one bounded review, targeted fixes, exact candidate gates, normal merge and independent exact merge-main qualification. No local heavy tests.

This task is now in test-first construction. Its initial tests are intentionally ahead of production. Durable Web draft behavior, cross-session continuation and the consumer form have not yet been implemented or qualified; the full five-domain/Backstage Goal remains active.

The `71c27953` cloud CI `33983456156` completed with the seven intended native-consumer failures (scope, confirmed/pending acknowledgement, validation rejection and three binding/permission refusals). The other 278 real-db cases and both ordinary shards passed. No setup failure is being reclassified as the intended RED.

Semantic decision, 2026-09-06: original Device binding remains immutable. This is a contract inference from Product 6.8, 9.1–9.2, 10.3 and invariant 18.4, under the unchanged final-contract ZIP SHA-256 `0763580e2be2d522eb26a3eaf12626f6fce194999a11c64a5a17ec23603a4eb3`. Current `create_manual_expense` proves a Device-scoped key, not authority to retarget it. Copying an uncertain original submission into a new Device/key can duplicate a late accepted POST; extending the financial owner to replay under another Device is rejected for this slice. The chosen readable old-Device quarantine preserves the original values and supports manual reconciliation through the existing fact list. Same-binding refresh/restart recovery proceeds independently. No contract amendment, credential change or new financial writer is made. Recheck only if a later explicitly scoped re-enrollment journey requires a contract-compatible original-binding reconciliation capability.

Implementation now projects the existing Dataset/Account/Device/ledger scope into the native form and canonical pending/fact acknowledgement. Browser storage accepts only the six field strings and their immutable binding; its five pure Node checks observed the missing-consumer RED, then passed in 0.79 seconds. Two earlier invocations stopped during conftest import, before any test or PostgreSQL start; the correct isolated command uses `--noconftest` and a fresh task-specific `--basetemp`. No environment, old temporary directory or database was repaired.

The real 360 × 800 production-template preview now places Save at 437–485 px, against the earlier 892–940 px. Document/client widths are 360/360. Entering synthetic `28.50` and reloading restores the visible amount with the retained-draft state; no financial POST was issued. Optional details use native disclosure, with error/invalid revelation. Cloud Edge coverage uses the actual shipped scripts on a synthetic HTTP consumer to exercise real locks, reload, native POST/unknown response/retry, acknowledgement retirement, quarantine and storage failure. Its metadata is not a financial fact; the separate exact-head PostgreSQL lane proves the actual native producer and canonical owner. Neither cloud gate nor bounded review has yet qualified this implementation.

Candidate `945dddb7` failed CI `33985616460` and is not qualified. Six of the seven new native expectations passed; invalid-amount handling exposed my incorrect `AppError.code` assumption (the real property is `error`). Both ordinary shards passed. The source gate also caught a direct model import in the new route presenter and two functions crossing the existing size budget. The read-only projection now lives in a Request-free service presenter, native error classification has one helper, and the existing return-context owner assembles the edit navigation. The focused codebase source audit returns to the unchanged 4-long-function/0-route-model-import baseline and exits 0. No limit or assertion was weakened.

The same candidate's cloud Desktop run passed 445 tests, skipped 8 and failed the new fixture before product script execution: its parent document appended an iframe before a body existed. The fixture now has an explicit document body before its scripts. Its first failing run does not prove any browser draft behavior.

The bounded `9cf0754c..945dddb7` review admitted one P2 FIX: a lock-denied page returned through BFCache could select its unrelated SSR fresh key instead of the fragment's requested draft. A pure Node test of the real page lifecycle reproduced A→B lock selection, then the consumer was changed to retain the original requested intent and require its record. Other reviewed paths had no new blocker. This regression tests the real event consumer, not a claim that a particular browser admitted a physical BFCache entry. Revised exact-head cloud execution and targeted final review remain required. The preview additionally exposed a dark native date icon; the manual fields now project the existing theme into the browser's native `color-scheme` without a new theme owner.

Third-task closure: final candidate `43d77dd01411f3e3aa6dc0e5955352fe4ecc5ae9` passed CI `33986217561`, CodeQL `33986217517` and Connected scope gate `33986217581` (actual Connected was legitimately scope-skipped). All 285 real-db tests passed; cloud Desktop/Edge passed 446 tests with 8 skips. The targeted final review closed the BFCache FIX with no new blocker. Normal #368 merge-main `f1b4ff4a3eda6a6a76eb938543c8d7fb411e7d01`, tree `c9f4248694cd285904286d7b97fd757a15c1ca57`, independently passed CI `33986845297`, CodeQL `33986845319` and actual Connected `33986845322`. Its full PostgreSQL, Android, Desktop and Windows build lanes executed in cloud. Main Desktop/Edge passed 446 tests, 8 skipped and one existing warning in 83.84 seconds. Historical failures above remain failures. No local heavy test or Windows lifecycle action ran.

Actual 360/768/1440 light/dark production-template inspection is complete for this form, including restored optional content and the corrected native date icon. The original preference and viewport were restored. Physical BFCache admission and full browser-process restart were not separately exercised; the real persisted-event consumer has observed RED/GREEN and cloud Edge exercises real storage/navigation/native POST/retry/acknowledgement. Old-Device recovery remains readable reconciliation, not new-Device replay. These limits and the still-open full visual/product Goal are not erased by this slice's cloud qualification.

## Fourth task: Android external-debt submission continuity

**Starting subject:** `f1b4ff4a3eda6a6a76eb938543c8d7fb411e7d01`, independently qualified above. Continue in the already isolated `xiaopiaojia-invitation-binding` worktree on `codex/android-debt-create-continuity-20260906`; preserve its untracked `tmp/`. The main agent remains the sole production writer.

**User outcome:** After a household member presses Save on a manually entered external debt, interruption or an unknown response must retain the same payload and create identity. Pending local intent must be visible as pending, never counted or announced as a confirmed Debt. A rapid second Save must not publish another command, and the displayed submitted values must not change while that command is being accepted locally.

**Current counterexamples:** `DebtListViewModel.submitDraft` does not guard an already-submitting state, and `updateDraftField` can still replace the visible snapshot during the command. `DebtRepository.createDebt` mints a new UUID on every invocation; `DebtDraftUi` lives only in memory. These exact-source findings define the next task, not defects in the closed Web slice. Initial cloud tests exercise the actual ViewModel with only its external command boundary suspended. They require one emitted create and unchanged displayed amount, counterparty and note. Production has not yet changed; an observed cloud RED is still required.

**Semantic decision:** Immutable origin and same-intent response-loss retry are contract inferences from Product 6.8, 7, 9.1–9.2, 10.3 and invariants 18.4–18.6, not new financial policy. The selected contract ZIP retains SHA-256 `0763580e2be2d522eb26a3eaf12626f6fce194999a11c64a5a17ec23603a4eb3`. Intent-first Room publication is the implementation choice: persist the full original command and binding before any network create, then use the existing Outbox lease, scheduler, retry and status owners. A direct request followed by persistence only after IOException leaves a process-interruption window and is not sufficient for this task. Do not mint a replacement key to recover an unknown result or add a second remote writer.

**Allowed changes:** The Android external/manual Debt create repository boundary, ViewModel/sheet/list pending presentation, required production Outbox adapter/dispatcher/label/settlement registration and directly affected tests. Reuse the existing backend Debt command and public API, the original LogicalSessionBinding and Outbox owner identity, current minor-unit parsing and authoritative home currency. Required queue dependencies are explicit, never an optional direct-only fallback. Any public wire change must be separately justified before changing that boundary.

**Exclusions:** Do not expand repayment/proposal/adjustment/forgiveness or recurring commands, change Debt fold/consent/accounting meaning, guess currency after a failed first load, manufacture a local confirmed Debt, transfer old identity/generation intents, or open Windows HOLD work. This task protects the submitted create intent; unsubmitted editing-text restoration is not silently claimed by an Outbox implementation and remains a separate convenience gap.

**Execution and evidence:**

- [x] Run `DebtCreateContinuityViewModelTest` through the exact pushed cloud Android lane. At `5ab65fe4`, CI `33988018049` / Android fast `101365079270` ran 2,076 tests and failed only these two cases. Retrieved XML confirms `1` versus `2` creates and `123.45` versus `999.00` visible amount. CodeQL `33988018052` and actual Connected `33988018036` passed, but the candidate remains unqualified. The two ViewModel entry guards are now implemented locally and await fresh cloud execution.
- [ ] Test the real new create producer with the existing Outbox store boundary before implementing it: before-network persistence, same stored key/payload after a failed dispatch, viewer and changed-binding refusal. Add a real Room close/reopen and retry consumer in the existing cloud Connected environment; no local Gradle or emulator. Bind claims to the actually exercised restart level.
- [ ] Route this one creation through the existing durable queue. Migrate its actual ViewModel, sheet, list pending state and production repository/dispatcher wiring together. Preserve canonical list refresh separately from command acknowledgement. Unknown payloads and refused bindings remain visible in the existing recovery surface without payload mutation.
- [ ] Physically retire the replaced direct-create path and its obsolete per-attempt-key expectation; distinct user intents must still receive distinct keys. Keep all unrelated Debt operations unchanged. Inspect actual connected consumer frames and actions in light/dark, including pending/failure/read-only states and keyboard-visible Save.
- [ ] Complete one bounded main review, targeted regression for admitted fixes, exact candidate cloud gates, normal merge and independent merge-main qualification. Update this plan and the single atlas; do not label this task or the full wave complete from the initial two tests.

The next test uses the actual `RepositoryGraph` with its existing Outbox dependency and an on-disk Room database, replacing only session/network IO. Save must leave one original intent before any request; closing and reopening Room must retain the same row, key, payload and owner for the existing dequeue consumer. Viewer rejection must write and send nothing. The current Debt repository is still direct-only, so the durable-intent case is deliberately ahead of production. This is a Room close/reopen claim, not physical Android process-death qualification.

Observed second RED at `e9e7d54b8c77038a84cf7505b30c172e6132d76c`: Connected `33989079744`, execution `101367954932`, completed 104 tests and failed only the new Room assertion: Save published zero intents instead of one. It failed before close/reopen, so no restart claim is made for that head. CodeQL `33989079764` passed. CI `33989079750` failed Android fast on `submitDraft` at 61 lines versus the existing 60-line budget; the retrieved unit XML independently confirms both original ViewModel regressions now pass (2 tests, 0 failures/errors). No local heavy run or cloud rerun was used.

The implementation now has one mandatory `DebtCreationRepository` for bound, intent-first publication and one `CreateDebtDispatcher` for the unchanged network command. The old `DebtActions`/`DebtRepository` direct create and unused fake overrides are physically removed; their payload/validation/separate-intent assertions migrate to the new producer tests. The VM distinguishes local acceptance from canonical Debt, freezes the submitting draft, rejects late old-binding results, and observes durable DONE IDs so a fast Pending-to-Done transition still refreshes canonical consumers. Both personal tabs use the shared pending presentation and the existing SyncStatus recovery screen; the all-ledger page consumes the same pending component. No local intent enters totals or confirmed Debt groups.

New cloud tests cover original key/payload retry through the real on-disk Room store after close/reopen, an intentionally lost response at the synthetic network boundary, completion observation, local acceptance without a fabricated Debt, and real shared-chrome Paper/Midnight pending/read-only recovery actions. The existing Connected artifact publisher will expose the actual consumer PNGs. These changes still await exact-head compilation, cloud behavior and image inspection, and bounded review. Physical process death and unsubmitted editing-text restoration remain unclaimed; the full Goal and Windows HOLD boundaries are unchanged.

Candidate `a8417724c9389ec5a2ac5f3615cb4d4fad90800e` passed actual Connected `33991048149` / execution `101373254291` and CodeQL `33991048143`. Its two new pending Paper/Midnight consumer PNGs were retrieved and inspected: readable original label/amount/note, explicit not-yet-in-ledger copy, no viewer create entry and a reachable recovery action. These are shared-chrome synthetic-state renders, not full application/keyboard acceptance. CI `33991048136` failed only two production complexity gates: dispatcher complexity 16 versus 14 and create-form length 62 versus 60. Retrieved XML establishes all 2,080 unit tests passed, including six new producer tests and five continuity tests. HTTP outcome classification and currency-readiness UI now have their own focused functions without changing thresholds or assertions.

The bounded `f1b4ff4a..a8417724` review admitted one P2 FIX and found no second current blocker: independent access/queue collectors can drop a new binding's only pending/DONE snapshot if Room's callback runs first. A controlled Kotlin ordering test is now ahead of the coordination fix and awaits cloud RED; do not repair this with timing assumptions or a sleep. An additional real list-to-sheet interaction test covers reachable Save, disabled submitted controls, original values and local-acceptance handoff, while suspending only the local publication boundary. The full feature remains draft until this FIX, the exact cloud gates and final-head review close.

Observed ordering RED at `92452670d854d4b0039d8a59c9234091451a9819`: CI `33991836275`, Android fast `101375358960`, ran 2,081 tests and failed only `newLedgerPendingRowsSurviveQueueEmissionBeforeTheAccessObserverRuns`. Retrieved XML shows the original new-ledger intent expected but an empty pending list observed. The VM now combines access and queue notifications before applying the current binding, retaining the latest queue snapshot whichever source arrives first; the two independent collectors are retired. No delay, second durable owner, binding relaxation or unrelated refactor is added. Fresh exact-head GREEN is required.

The same historical `92452670` passed CodeQL `33991836273` and actual Connected `33991836293` / execution `101375355677` (107 tests). The new real list-to-sheet test passes and its editing/submitting device captures were retrieved and inspected: original fields and reachable Save remain visible, and submitted fields/actions become disabled. Neither image contains a visible soft keyboard, so these images do not qualify keyboard-visible operation. That remaining interaction check is explicit; full visual acceptance and physical Android process death remain unclaimed.

Ordering GREEN at `24ae61f032b2ec58080b36b172be5e4a20ca7dd6`: retrieved unit XML from CI `33992758249` establishes 2,081 tests with zero failures/errors, including all six continuity cases. CodeQL `33992758248` and actual Connected `33992758251` / execution `101377812755` passed. The bounded `a8417724..24ae61f0` follow-up review closed the ordering FIX and found no new blocker; no further production change was requested. CI still failed the test-only `FakeDebtActions` function budget (13 versus 11). Query and creation fakes now expose separate required ViewModel boundaries, with all scenario assertions and guards preserved. Production behavior and quality thresholds are unchanged; the new exact candidate must qualify.
