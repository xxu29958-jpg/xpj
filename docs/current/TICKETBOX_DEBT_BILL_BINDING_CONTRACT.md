# Debt-bill preparation and original household binding

## Working contract

Goal and the three final product contracts require original user work to stay with its logical household identity. This slice begins at source `d22511909553edf3f6c09055c32f60b9fc26fb48` in the isolated `codex/debt-bill-binding-20260907` worktree. The parent upload slice is still under exact cloud qualification; this preparation does not qualify it or authorize an early protected merge. Root is the sole writer; the two existing independent agents are unavailable because their calls reached the account limit.

The outcome is bounded: a debt-bill image accepted for preparation under household A must not be sent or applied under a subsequently selected household B. Recognition remains a suggestion. The existing Debt creation command, durable Outbox, financial interpretation and explicit Save remain the only publication chain. No new draft persistence policy, automatic command, API wire field, Windows lifecycle action or visual redesign is included.

## Before-impact closure

- Entrances: the obligations-domain composer and the debt-list route both use `rememberDebtBillImageLauncher`. After the picker callback, it calls `markBillParsePreparing`, performs image preparation on IO, and later calls `parseDebtBillImage` or `billParsePreparationFailed` without a captured identity.
- Owners and consumers: DebtListViewModel owns parsing progress, prefill, draft currency and sheet-open feedback; DebtRepository owns the bound multipart call; the two real sheet hosts consume the same ViewModel state. Both route instances, all direct JVM callers and every DebtActions implementation are affected by any interface correction.
- Existing guard and uncovered interval: DebtRepository guards a request against a switch during HTTP, but binds the current session only after image preparation. A switch during preparation can therefore admit A's bytes into a new request for B. The preparation continuation has no way to identify its original attempt. Existing same-binding parse/pre-fill and viewer tests do not exercise this interval.
- Old success exits: an obsolete preparation/response cannot open a new household's sheet, replace its draft, clear another attempt's busy state, or show an unrelated failure. Valid same-identity recognition must still prefill the original currency-aware suggestion and await explicit Save.
- Persistence/protocol/recovery: recognition itself does not accept a financial command or enqueue an offline intent. Existing submitted Debt payload/key/identity and upload originals stay with their existing owners. A stale recognition continuation must refuse; it must not create a replacement command. There is no database or public protocol migration.
- Direct producers: add a gated current-ViewModel counterexample covering the real preparation-to-request boundary, retain existing positive/currency/viewer cases, and cover the actual bound repository call and both UI adapters after correction. Kotlin compilation, JVM, Lint and Connected qualification stay in cloud; local work is limited to short source checks.

## Qualification state

The first cloud counterexample now has an observed behavioral RED, recorded below. The original identity and new-household draft postconditions remain the acceptance criteria. The candidate implementation has passed a complete light scan of 51 changed production and 50 changed JVM files against their actual baselines; Kotlin compilation, executed regression, Connected and protected-main qualification are still pending.


## Observed RED and after-impact closure

Source `4813e4c6ae01e213d073840ee1188603bcf74aff`, CI `34098563436`, Android-fast job `101667594455`: 2,244 JVM cases executed with exactly one failure. Artifact `10009830155` contains the new case's XML: one failure, no error or skip, with the expected message that original bytes reached a request bound to the new household. Preparation admission and the new household's currency-ready preconditions passed; later draft assertions were not reached at that failed head.

The same ViewModel now issues an attempt with its accepted logical binding, resolved currency and existing editor generation before IO. Both real entrances retain the same shared launcher, which passes that attempt with either the prepared image or the original preparation failure. One continuation handles both outcomes; the separate unbound failure callback and implicit start-after-preparation behavior are retired. Route re-entry, explicit reset and binding/permission changes invalidate the relevant work. Old callbacks cannot clear a newer attempt's progress or replace its draft. Valid recognition uses the captured currency and still requires explicit Save.

DebtActions now requires the expected binding. DebtRepository uses bindExact inside the actual IO boundary and its existing before/after HTTP guard. All five concrete JVM fake declarations and their direct callers migrate; the existing real repository success and viewer cases keep their assertions and now obtain the expected identity from their actual session fixture. The positive case additionally changes the live ledger before replaying the original expected identity and requires no second API call. The initial ViewModel oracle retains every postcondition through its required-input migration; two focused continuations cover an old preparation failure versus a newer attempt and a late response versus a new draft after real route re-entry semantics. Test scopes are cancelled explicitly.

No public wire format, database, Room/Outbox, financial command, provider policy or Windows boundary changes. The only new typed value is the ephemeral original-attempt contract; no alternate sender or persistence owner is introduced. A first light scan found that adding the shared continuation check would exceed the existing class function gate. Consolidating both preparation outcomes at their real continuation owner removes the duplicated failure entrance; no method is moved to another file solely to hide size, and no threshold or baseline changes. Full changed-candidate source checks and current cloud execution must qualify this implementation; the RED evidence above is not a GREEN claim.

## 796dc10e direct-producer closure

Android-fast job 101672987844 succeeds on source 796dc10e: compilation, executed JVM cases, Lint and type-aware Detekt pass. Backend-contract job 101672987635 independently rejects repository weight because DebtRepositoryTest grows from 788 to 804 physical lines. This does not invalidate the executed behavioral result, but the candidate is not qualified for merge.

Before-impact: the file's sole repository constructor wraps DebtApiHandler in a one-use DebtApiFactory, whose sole method delegates to handler.service(). There are no other consumers of either facade. The same handler can directly implement the existing ApiServiceFactory contract and construct the identical proxy. Retire the forwarding class and its local variable; migrate that sole provider call. All 35 tests, their assertion lines, session identities, exact-binding refusal, API observations and error cases must remain unchanged. This is direct-producer simplification; no production entry, success exit, persistence, protocol or recovery path changes.

After-impact: DebtApiFactory and handler.service are physically absent. The existing API handler directly implements the same factory method and constructs the identical proxy; the sole provider call receives that handler. All 35 test names and all 108 assertion lines are unchanged. The file is 799 physical lines; its actual unit-test Detekt baseline reports zero findings, and diff checks pass. No production source changes in this correction. Final qualification follows integration of the completed Capture dependency, with fresh cloud checks for that resulting source.

## Capture dependency integration

Before/after impact: starting from local f6c477f2, normal merge of Capture ae2fc09e produces exactly the merge-tree preview d4d169dfccef95ca9cd247760162a6613a9499a5, with no conflicts. All 12 incoming files belong to the original upload task persistence/replay, shared conditional failure API, its application migration, direct PostgreSQL producers and upload contract. No Android source differs from the reviewed Debt-bill correction in this integration; its original-attempt owner, real launcher and all five fake consumers remain unchanged. The incoming migration/protocol/recovery/worker impact is specified by the complete upload contract carried with it. The combined candidate receives fresh cloud gates. Capture must be protected-merged and its resulting main independently qualified before this PR can merge.


The combined 77c1918e source passed CI 34102840809, CodeQL 34102840765 and Connected 34102840555. Capture pre-merge review subsequently admitted missing-original disclosure and permanent-refusal Retry counterexamples. Normal integration of 17824a83 produces exactly preview tree a2706df5953d8d109a39d71122598621ec8a4490 without conflicts. Its ten incoming files are limited to the upload projection, retired saved-message exit, shared retry predicate, atomic retry guard, resource, direct JVM/Room producers and upload contract. Debt bill recognition, its original attempt and HTTP binding are unchanged. The carried upload contract covers all incoming consumers and recovery paths. Fresh combined-source gates are required; the previous green source cannot qualify this integration.
