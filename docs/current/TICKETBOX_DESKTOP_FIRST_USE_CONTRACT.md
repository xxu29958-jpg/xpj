# Desktop first use and pairing continuation

This bounded slice implements the active whole-system Goal and the September 6 capability-first ruling. It does not replace the full Internal Beta RC goal. Product identity and the final Product/Windows/post-G2 contracts remain the authority; existing UI and tests are evidence.

## User outcome and scope

A healthy local installation lets its Owner reach the existing code-generation task directly from the unbound Desktop entry, even without a phone connection URL. The entry explains that the Windows computer stores the household data and originals. It distinguishes an Owner's own device binding, a household member's personal device authorization, and invitation-based household entry.

A rejected fresh code leaves entered text available for correction and allows a new code. An unknown pair result keeps its original durable proof and tells the user to continue with the original code. That explanation survives a Manager restart through the Controller's non-secret projection. If the original code is lost, the pending attempt must finish expiring before a new code can start; passive refresh may observe that transition, but never spends a provisional proof. A new mismatched code must not erase or replace a possibly committed attempt.

Allowed changes are the actual Manager HTML entry and feedback, the Controller's non-secret projection and actionable error, directly related tests, and reuse of existing cloud test infrastructure to execute the real backend consumer. Backend identity/code/activation ownership, WinCred encoding, proof lifetime, teardown, account membership and permissions remain with their existing owners. No secret or entered code is copied into browser storage or status. No new auth protocol, public connectivity mutation, installer operation or held Windows lifecycle is introduced.

## Producer, consumer and retirement

- Backend identity and Owner/personal device authorization remain the only code/identity owners.
- `AppController.open_pairing` opens the existing loopback Owner task. Its availability follows local product readiness; a phone URL is not required for local binding.
- Existing WinCred recovery supplies only an `original_code_required` state to the Manager. The proof and any credential remain private to their current owners.
- The actual Manager preserves input on rejection or unknown result, gives truthful continuation, and keeps unavailable/session-read failures closed. Current roles continue to come from live membership.
- Installer-only instructions, the phone-dependent gate on the local code action, and the generic regenerate-code advice for a reused pending attempt retire.

## Minimal evidence and exit

The current Controller recovery test must fail when a restarted Manager cannot explain an unspent pending attempt; its existing same-proof replay and terminal/fresh-invalid cases remain meaningful. A real Edge execution of the production HTML must fail when a healthy local-only installation cannot reach the code command, unknown-result feedback loses the original context, or a failed state read enables binding. Synthetic providers isolate these UI observations; they do not prove a live backend ceremony.

Run short Controller and Edge tests locally, then the final candidate's relevant CI/CodeQL/scope gates and the real backend Desktop consumer in the cloud. Source review found that the existing Desktop job lacked the backend interpreter and silently skipped the live cases. The Windows build job now reuses its pinned native PostgreSQL inputs and the existing owned test-cluster start/stop scripts; its backend-capable interpreter runs real pairing, response-loss recovery, BFF and served-Web layout cases. Zero collected cases or any skip fails this gate. Relevant test-only changes also trigger that job. Complete bounded source review, normal protected integration and independent main qualification. Actual clean-Windows Setup/APK and full cross-client RC rehearsal remain the original Goal's later gates. No ten-minute-scale local test and no #372 detail work.

Current evidence: both Controller continuation cases and the real Edge first-use case reproduced RED on the previous production source. A second Edge RED showed the shared device-code action still incorrectly depended on the phone URL; that gate is retired too. The corrected consumer passes 82 short Controller, static and real Edge regression cases in approximately four seconds, including invalid/unknown/expired/read-failed states, existing binding lifetimes and role/visibility behavior. An actual 820-pixel frame was inspected; the first-use probe also runs at 390 pixels. Synthetic providers bound these observations; cloud and live-backend qualification remain pending.

Bounded review of `5bc5f65e` admitted one P2: an old selected ledger can disappear while another ledger remains accessible, and a new pairing attempt can have an unknown result. The retained old session previously suppressed the pending-attempt projection. Controller and actual UI now show original-code continuation in this rebind state as well. Targeted regression first produced two failures and then passed all four cases. The widened existing Controller/UI/browser selection passes 94 cases in 12.29 seconds; two real-backend layout cases skipped locally because no backend environment was started, and are included in the new non-skipping cloud step. The trigger regression produced RED and then the full 20-case scope suite passed; the GitHub workflow contract audit also passed. The new real-backend response-loss test is authored but not locally executed. An older test name claiming Manager death now states its actual persisted-activation-receipt scope; no real process-death evidence is claimed.

The first cloud candidate `20b2fd30` reached the new gate: native PostgreSQL started, but nine real consumers errored before seeding because application authentication failed; two independent bridge cases passed and the owned cluster stopped successfully. That runner's `win25-vs2026/20260824.214` image sets an unrelated machine-level `PGPASSWORD`. The fixture had inherited it, overriding the authorized passfile, while the existing PostgreSQL startup and lifecycle consumers already isolated their environments. The fixture now removes ambient `PG*` from its child environment except the explicit test passfile; it preserves the sealed route and cluster identity without changing the parent environment or credential owner. A short regression capturing the actual fixture's process launch reproduced RED and then passed. Final cloud authentication and product outcomes still require the corrected exact candidate.

Candidate `ed04f6c9` passed CI `34025818696`, CodeQL `34025818747` and Connected `34025818786`. Its native Desktop backend step executed all 11 cases without skips in 39.83 seconds, then stopped the owned cluster. Formal review admitted a P2 in future trigger coverage: backend pairing routes and response schemas could change without selecting this native consumer gate. Their existing producer files and the identity-service package now select the Windows lane while retaining backend qualification. The real scope regression reproduced the omitted gate and the complete 21-case scope suite passes. This trigger correction requires its own exact cloud qualification before integration; the original process-death and full RC limits remain.

The current `8c0a792c` also passed its real native-backend step, but a new formal P2 found the omitted pairing-code issuance producer. A bounded trace through the existing live ceremony additionally identified its ledger-create/switch preparation and predecessor-session revocation routes and their two direct owners. Each omitted path independently classified Windows as false; the actual producer regression failed again. These five explicit files now join the same existing producer scope. The earlier four-file review did not identify these direct consumer dependencies and is not complete trigger-coverage evidence. Current source retains the same product implementation and requires its own cloud qualification.

A further formal P2 exposed a mixed test-database authority in the existing helper: local URL/identity derivation still retained an ambient passfile. The helper now chooses the complete explicit URL/identity/passfile or the complete owned-local set; partial explicit input stops before connection, and the selected passfile is installed for both the lease and subsequent backend connections. The first short probe stopped at the absent local ownership marker, which was a fixture issue rather than the claimed failure. With only that physical marker substituted and the database boundary stopped, the actual helper reproduced four failures: ambient passfile retention and three partial-authority paths. The corrected six-case suite passes in 0.11 seconds without a local server or database. Server/SQL adapters are substituted only in this short Desktop-unit probe; the existing native Windows gate remains the real backend qualification. Both earlier trigger threads are resolved with readback; this helper finding and the new candidate qualification remain open.

## Impact closure after the September 6 user ruling

This supplementary trace compares `e8cfd762` with `4c25d8642bea6afec244560b211453c26661de84` and the subsequent uncommitted probe correction. It follows all directly changed entries and consumers rather than assuming an unchanged file is unaffected. It does not reopen held Windows lifecycle work.

| Scope and owner | Before → current behavior and directly traced consumers | Evidence and remaining limit |
|---|---|---|
| Both local code entries | At `e8cfd762`, `androidAction` could bypass the pending/read-failure gate applied to `desktopPairingCodeAction`. At `4c25d864`, `ui.html:733` applies one availability decision to both; both invoke `open_pairing` → `AppController.open_pairing:932` → the existing loopback Owner task. No phone URL is required. | `fixtures/desktop_first_use_probe.js` actually clicks both entries before a state read, while recovery is pending and after read failure; `test_desktop_first_use_edge.py:13` consumes all results. This is Manager HTML, not the BFF-served `/web` document. |
| Pending rebind and old success exits | A still-live old ledger previously selected the manage branch and hid the original-code input. `ui.html:747–790` now makes pending recovery select the original-code branch and closes the old home/manage/import-export projection; input remains available for the original attempt. | The same actual-HTML probe checks restored old membership, retained input and both closed new-code entries. The current correction does not change this product behavior or substitute an old phone-dependent assertion. |
| Durable intent, identity and permissions | `AppController.product_principal:310` projects the existing recovery state; `_pair_product_principal:444` reuses the persisted attempt before code consumption. WinCred recovery, activation, expiry and predecessor revocation remain their existing owners; none changed from `e8cfd762` to `4c25d864` or in this correction. | Traced through `productAction`/`pairProduct` → `/api/product/pair`, `product_principal`, `_load_rebind_recovery` and `_reconcile_rebind_recovery`. The actual response-loss/continuation case passed in the current native step; process-death and Windows lifecycle qualification remain outside this evidence. |
| Native backend producer and teardown | `_real_backend.py:101–192` replaces blocking seed reads with bounded queue observation and keeps draining helper output; the helper still selects the complete authorized DB route. Its unchanged consumers are both BFF render probes and `test_desktop_first_use_backend.py`. | Job `101483660856` reached all 11 cases: 10 passed, one failed at the 1180×760 render overflow assertion. This establishes a seeded backend and reached consumer, not a green native gate. |
| Both served-Web geometry producers | `_edge_cdp.py:271–294` polls immediately after navigation. `_REAL_RENDER_PROBE` and `_SERVED_WEB_PROBE` previously sampled as soon as `main-content` existed, including before stylesheet/deferred-script completion. Both now require `document.readyState === "complete"` and emit only numeric viewport/scroll widths as additional failure diagnostics. | `test_edge_probe_contract.py:69` evaluates both actual probe strings: loading/interactive produced four RED; complete still reports real overflow. After the correction the full short suite passes 11 cases. Both callers retain the overflow assertion and all later identity/accessibility/secret checks; no wait-until-fit or semantic retry was added. |
| Direct assets and native trigger coverage | Actual `/web` HTML/CSS comes from `backend/app/templates/web/base.html` and its stylesheet/script links through `ControlServer._serve_web_bridge:486`; Manager `ui.html` is not injected into that page. The existing Windows step (`ci.yml:343–355`) invokes both probe callers and rejects skipped/empty results. | At `4c25d864`, actual classifier calls omitted Windows for `_edge_cdp.py`, `base.html` and `product/shell.css`. Root admitted FIX under the new impact discipline. The existing BFF shared-Web prefix boundary now also selects Windows, and the existing native-helper set includes `_edge_cdp.py`; ordinary backend, Owner static resources and the BFF-excluded `web_auth.py` keep their prior scopes. Three actual-classifier RED became the full 21-case scope suite GREEN. |

The finite served-page trace is `/web` → `web_app.py` → `/web/pending` → `web_pending.py`, `web_common.py`, `_web_session_common.py` and `_web_pending_enrichment_watch.py` → `pending.html`/`base.html`. Its shell/capture includes are `_sidebar_nav`, `_brand_mark`, `_primary_nav_links`, `_ledger_switcher`, `_appearance_control`, `_mobile_nav`, `_bottom_nav` and `_inbox_capture`. The loaded stylesheet family is `tokens.css`, `product/shell.css`, `product/components.css`, `product/domains/inbox.css` and shared `confirm-modal.css`, with shell-referenced Noto/Inter fonts and brand/texture resources. Base loads appearance bootstrap, shared CSRF, PWA registration, `desktop.js` and its core/theme/background/ledger-switcher/keyboard/receipt/drawer/bulk/drag/split modules, plus shared confirmation handling; PWA registration directly references `sw.js`. These are already inside the BFF's `/static/web` and `/static/shared` allowlist and shared Web template/route/helper prefixes. Reusing that existing consumer boundary avoids a second dependency registry or a rule covering all backend code. Native fixture/helper and pairing owners retain their existing explicit scopes; the complete existing scope suite checks them alongside the new producer samples.

The current cloud failure is precisely `test_real_backend_bootstrap_pair_bridge_render_probe[1180-760]` at its overflow assertion (original line 478), with 10 other cases passing in 42.43 seconds. Pairing projection and bootstrap consumption passed before that assertion; its later ledger/foreign-403/URL checks were not reached for that parameter. The run exposed no uploaded native JUnit artifact, so this finding uses the complete failure log, not an XML readback. Early sampling is now a separately reproduced verification defect; it has **not** been proved to be the sole physical cause of this cloud overflow. The corrected exact candidate still needs the native step, including a completed-document overflow result. Local work ran the dependency-free Node/CDP contract suite (11 passed in 1.46 seconds), the actual scope classifier suite (21 passed in 0.72 seconds), Ruff and diff checks; no PostgreSQL, product backend, Gradle or long test ran locally.
