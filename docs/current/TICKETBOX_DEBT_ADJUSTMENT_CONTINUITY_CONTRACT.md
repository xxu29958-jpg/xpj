# Original debt-adjustment intent

This slice continues the whole-system Internal Beta RC Goal and the capability-first ruling. The final Product contract sections 6.4 and 9 own relationship facts and original-command recovery. The current source shows a missing Android continuity path: every direct `recordAdjustment` call generates a new key, while the backend can recover a committed response only under the original key and body. Closing the form also clears its submitted context. No duplicate financial posting has been demonstrated; the known consequence is loss of the original recovery path after an uncertain result.

## User outcome and owners

An authorized user adjusts one external debt with a signed amount and a reason. Local acceptance retains the original debt identity, label, currency, amount, reason, logical binding, payload revision, OCC and idempotency key before networking. Unknown results, protocol refusals and application restart retain that exact intent visibly. Retrying recovers the existing backend command under its original key; it neither guesses a fresh OCC nor creates a replacement adjustment. A confirmed conflict asks for reconciliation or explicit local-intent discard. The authoritative debt fold changes only through the existing backend adjustment command.

Reuse Room/Outbox, its session and expiry boundaries, the existing debt command, and current sync recovery entrances. The detail page and global recovery explain the original adjustment and distinguish local acceptance from committed financial fact. Retire the Android direct adjustment writer and migrate its real consumers. Other repayment, proposal, forgiveness, void and classification commands are outside this bounded change; their separate continuity gaps remain in the full Goal.

## Minimum proof and integration

First reproduce the present recovery failure through the actual client boundary. Then verify durable publication before networking, original-key response-loss recovery, unsupported/protocol/conflict retention, and unchanged binding/OCC/expiry semantics. A real Room/UI consumer must show original context after reopen and complete the actual save/recovery path. Existing backend adjustment idempotency and fold tests remain the server proof; expand them only if an executable gap requires it. Use short local source/contract checks and exact-candidate cloud Android/backend qualification, followed by bounded review and protected integration.

The branch uses Income candidate `dcf79dae` to reuse its corrected refusal model and read protocol. That parent is still awaiting its own gates; it is not a qualified main baseline. This dependent draft may run its test-first cloud counterexample in parallel, but integration and final qualification must use the actual integrated parent. After the parent merges, bring main forward normally and qualify this slice on its resulting exact head. No local long Gradle/PostgreSQL test, no new financial subsystem, and no held Windows lifecycle work are authorized by this slice.

Current implementation publishes a versioned original adjustment with its signed amount, reason, record currency, debt identity and label, principal binding, original OCC and key. The registered dispatcher owns the only Android network adjustment call, uses the original request unchanged, and does not cascade a fresh OCC. Detail reads and recovery are independent; local acceptance does not replace the canonical Debt. Detail and both global recovery consumers explain the original command, reject unsupported retries and retain protocol refusals. Original-key expiry and binding ownership remain with the existing Outbox.

Observed test-first evidence: source `1f3bd2c5`, CI `34029178586`, Android job `101475482566`, qualification checkout `60bcef60bc51cdcc621d0d669d14dfb57060cf4d`; 2,097 JVM tests with one failed original adjustment recovery case. That direct-writer test is migrated to the new actual repository/drain boundary when the writer is retired, alongside pre-network persistence, original-key replay, protocol/unknown/404 refusal, conflict, unsupported payload, permission and binding cases. New real Room/graph/detail Save and Retry coverage closes and reopens the on-disk database while the canonical GET fails. This is prepared coverage, not yet executed passing evidence.

The test-first candidate also failed repository-weight on its long/complex test function and one unchanged Connected hinge case reported no Compose hierarchy. Those are observed failures, not waived green results or established production regressions. The current implementation brings forward the already-qualified Income `ef54e1a5` predicate-owner placement; parent integration remains pending its new formal month-status finding. No result on either parent substitutes for this candidate's own gates.

## Impact closure rebuilt after the explicit owner-change discipline

This inventory was rebuilt on 2026-09-06 after the user required every business-semantic or owner change to enumerate its entrances, consumers, former success exits, persistence/protocol/recovery paths, and direct verification producers before and after implementation. It reconstructs the earlier implementation; it does not retroactively claim that discipline was followed before the work. The compared subjects are test-first `1f3bd2c5ac1d4b36434dff532e188a17a510af5e` and implementation `379a8ce175c8695f87c520375fde7acfdc00d047`. Line references below describe the latter unless marked otherwise.

| Path | Before | Current owner and consumer evidence | Direct verification / limit |
| --- | --- | --- | --- |
| All Android detail entrances | Debt, receivables, and goal-linked detail shared the direct `DebtActions.recordAdjustment` writer. | `StatsRoutes.kt:177,256,378` all use `DebtDetailHost:315`; its three VM factories (`StatsRoutes:230,362`, `DebtGoalRouteModels:40`) require the same adjustment repository. `DebtDetailScreen:398,417` feeds the existing signed amount/reason form into `DebtDetailViewModel.submit:259`. | `DebtAdjustmentViewModelTest` covers the submitted original fields, local acceptance, and replacement bindings; `DebtAdjustmentRoomContinuityTest:40` drives the real detail Save and Retry. Navigation factory wiring is code evidence, not three separate instrumented journeys. |
| Direct-writer and old-success retirement | `DebtRepository.recordAdjustment` minted a key for each call and returned a folded Debt; the detail VM cleared the form and announced recorded success. | That interface member, implementation, five old tests, and dedicated direct-call probe are removed. `RecordDebtAdjustmentDispatcher.kt:20` is the only Android production `recordDebtAdjustment` invocation. `DebtDetailViewModel:260,357` returns a queued intent ID, retains the canonical Debt, and says the original adjustment is saved locally. The old adjustment success resource has no reachable adjustment submission path. | Migrated `DebtAdjustmentRepositoryTest` has 12 repository/drain cases; the original test-first response-loss failure is recorded above. Other direct repayment/void/kind operations still take the committed-result branch; no replacement protocol was introduced for them. |
| Publication and persisted representation | No Android adjustment command was stored before networking. | `DebtAdjustmentRepository.kt:52` validates binding/role/debt/currency/amount/reason and same-target unresolved work, then stores revisioned subject, origin, original request/OCC/key. `OutboxRepository:380-408` inserts Room state under the binding lease before the scheduler callback. `PendingMutationType:51` adds a wire kind; Room columns/schema and backend request field names are unchanged. `RepositoryGraph:123`, `OutboxAdapterGraph:145`, and `AppContainer:290` connect one repository/adapter/dispatcher. | Repository tests inspect persistence before scheduler and zero direct sends. The Connected fixture reopens an on-disk `AppDatabase` through the production repository graph and compares original persisted fields. This is Room reopen evidence, not a claim of OS process-death qualification. |
| Dispatch, protocol, and backend fact owner | API and Web already shared the backend adjustment command and its idempotency/OCC ordering. | `OutboxDrainWorker:75` reaches the registered engine; `AppContainer:124` uses the persisted binding. The existing runtime interceptor still gates the POST; its only before/current diff moves an unchanged predicate into its class. Dispatcher unsupported/protocol/unknown-409/404 results preserve Failed state; `state_conflict` preserves Conflict; IO retains the original command for bounded retry. Success supplies no fresh token to cascade (`RecordDebtAdjustmentDispatcher:21`). API `routes/debts.py:196` and Web `routes/web_debt_actions.py:225` retain `debt_command_service.py:216`: original-key HIT precedes `record_adjustment`/OCC, and fact plus receipt commit together. | Dispatcher refusal/no-cascade tests and unchanged `backend/tests/test_debt_adjustment.py:169,186,291` cover replay, response loss/read models, and stale OCC. No backend source/test diff exists in this interval. A stale new-key 409 is not evidence of duplicate posting. |
| Detail recovery and new completion exit | Dismissal or reopen could lose the submitted amount/reason/key; canonical GET was the only detail source. | `DebtDetailViewModel:148` observes original commands independently of canonical GET. `DebtDetailChrome:89` renders them even when that read fails. `DebtPendingAdjustments:38` offers original Retry or confirmed local Drop; `DebtAdjustmentRepository:76` rechecks current binding/role/support and never changes OCC. Newly Done records refresh canonical Debt once; historical Done and Done-before-local-ack cases are guarded. Existing `DebtDetailScreen:147` reloads repayment history on canonical row-version change, and the three hosts retain their return-to-list/goal refresh callbacks. | Six VM cases include failed detail read, historical/new Done, early Done, and same-ledger identity changes. Real Room/UI test asserts old balance before dispatch, original-key replay, one fact, then the refreshed balance. No failed/unknown result uses the canonical-success exit. |
| Both global recovery entrances | Shared Sync screen had no adjustment context map or adjustment retry routing. | Obligations Sync (`MainProductNavRoutes:237`) and Settings Sync (`SettingsDestinationHost:378`, supplied by `SettingsRoute:81`) both require `OutboxRecoveryRepositories.debtAdjustments`. `OutboxStatusViewModel:55-76` exposes Pending/InFlight context and Failed/Conflict maps; `:109` routes adjustment Retry through its repository. `SyncStatusScreen:145,156,172` displays original context; unsupported Retry is absent. Conflict Keep Mine remains expense-only (`:229`), so debt cannot obtain a fresh OCC there. Both Drop paths use existing owner/ledger/status-checked Outbox deletion and an explicit explanation that local Drop does not undo a possible server fact. | `OutboxStatusViewModelTest` covers unsupported Retry/Drop and reopened Pending-to-InFlight context. Existing shared recovery UI tests remain producers; dedicated adjustment Connected coverage exercises detail, not two independent Sync navigation journeys. |
| Shared ownership, expiry, and adjacent consumers | Outbox owned binding transitions, same-target ordering, retry age and stale status checks. | Those implementations are unchanged and the new type enters them: `OutboxRepository:694-738,813-832,954-966`; `OutboxDrainEngine:283-338`. Retry preserves original key/body/OCC; unsupported cannot send; viewer cannot publish/requeue; wrong owner/ledger cannot borrow context; expired intent cannot be requeued. Existing creation, income, and recurring summaries remain in the same shared Sync maps and positions. | New repository cases directly exercise role/binding/unsupported/conflict/original bytes; existing Outbox expiry/resolve tests remain applicable through the unchanged shared functions. No independent lifecycle, Windows, or other debt-mutation owner was added. |
| Direct cloud producers and current follow-up edits | The direct-writer counterexample ran and failed at the test-first head; later implementation added real repository/Room consumers. | `ci_gap_trigger_scope.py:11-21,304` includes Android source/tests. `.github/workflows/ci.yml:990` runs JVM tests/count/lint/Detekt; `android-connected-test.yml:145` runs the full Connected task with exact qualification source checks. Repository-weight is also a direct producer: artifact `9988962501` reports the Sync context test at line 152 through class end 232, length 81. The authorized follow-up extracts only its original-context assertions into a helper before the test. Root separately extracts the identical recurring/income/adjustment summaries from both Conflict and Failed render sites into the same-package internal `SyncStatusOriginalIntentSummary` in existing `SyncStatusOverview.kt`; maps, ordering, guards, and Retry/Drop remain unchanged in that inspected diff. | Test helper inlining reconstructs the original test body exactly modulo whitespace; all 44 assertion calls remain, including Pending retry count 0, first InFlight count 1, complete row, key, original request/binding fields, single-row, failed/conflict and busy checks. Static physical span is now 74 under the previous reported boundaries; Lizard/Gradle were not rerun locally. The production summary extraction and this test edit require a new exact-head gate. |

Exact `379a8ce` cloud snapshot: Connected run `34031791504`, actual execution job `101482565554`, and CodeQL run `34031791503` are successful by independently read GitHub status. Root inspected actual Connected output and reports 115 executed, zero skipped and zero failed; this inventory did not independently obtain per-case JUnit for that run. CI `34031791511` is not qualified: backend-contract job `101482566294` failed repository-weight, and root inspected Android-fast job `101482566447` failing Detekt on `SyncStatusPageBody` complexity 17 against 14 after JVM execution. Neither result is waived or replaced by another head. The two follow-up extractions above address those concrete producers without reducing assertions or changing business guards; their new SHA and final CI/CodeQL/Connected results remain pending.

No additional actionable production omission was found in this bounded before/current path review. The recorded limits, parent integration, new-head qualification, protected merge, and independent merge-main qualification remain open; this table is not a full-Goal completion claim. This reviewer changed only this contract and the named JVM test, ran no Gradle/PG/emulator, and made no commit or push.

## Input-validation test consolidation after Android-fast Detekt

Before this test-only edit, source `a027ef9bb0f7d2d9babcc5eadf1c6b1cd7cd9132` was clean. Root reports Android-fast job `101486147691` failing `TooManyFunctions`: `DebtAdjustmentRepositoryTest` contains 12 functions against the configured class limit of 11 (`android/detekt.yml:39`). Source inspection confirms both counts. The impact inventory below was established before this edit and checked against the resulting diff; no production semantic or owner change is included.

| Impact surface | Before | After and evidence boundary |
| --- | --- | --- |
| Verification inputs and consumer | Separate `zeroAmountCannotPublishOrSend` and `blankReasonCannotPublishOrSend` tests call the actual `DebtAdjustmentRepository.save` through `DebtAdjustmentFixture`. | One `zeroAmountOrBlankReasonCannotPublishOrSend` test explicitly supplies `(0L, "  补记借款  ")` and `(3_000L, "   ")`, matching the former explicit/default inputs. Each loop iteration creates a new fixture. Both still assert failure, no persisted rows, no scheduler queue entries, and no API calls; input-specific assertion messages identify the failing case. |
| Entrances, former success exits, persistence, protocol and recovery | Detail and both Sync entrances, local acceptance, Room/Outbox publication, dispatcher, original-key/OCC recovery and backend fact ownership are inventoried above. | Their production files, fixture/adapter/probe, and the other ten repository tests are unchanged. The two invalid inputs retain the same rejection postcondition before publication or sending; no success exit, stored representation, role/binding rule, Retry or Drop path is added or removed. |
| Direct verification producers and risk | Android-fast `.github/workflows/ci.yml:985-990` executes JVM tests, result-count ratchet, lint and Detekt. Two independent JUnit methods previously covered these validation inputs. | The test class now has 11 functions and 11 `@Test` methods; two validation input cases remain. Total JUnit method count decreases by one. The existing result-count minimum, Detekt configuration and suppressions remain unchanged. Static input/assertion and diff checks are the local evidence; no Gradle/PG/emulator ran. New exact-head JVM/Detekt and result-count qualification remain required; the earlier failed job is not a passing result for this edit. |


## Formal P2 owner closure: test-first impact at a033c08b

This inventory is established before the new counterexamples, following the user requirement to enumerate every affected entrance, consumer and former success exit. Root admitted formal T5H/T5I (duplicate Facts ViT/Vib and Gov Ukh/Ukl); the previous bounded-review conclusion does not close these newly demonstrated gaps. Production is frozen for this test-first candidate.

| Responsibility / real consumers | Before and executable counterexample | Required boundary / direct producer |
| --- | --- | --- |
| Detail mutation gate: DebtRoute, ReceivablesRoute, DebtGoalRoute share DebtDetailViewModel | openAction blocks only Adjustment; Repayment, Void, RepaymentVoid, selectKind and already-open submit can use the old fold while a stored adjustment is Pending/InFlight. Done disappears before GET succeeds, so a failed refresh reopens stale writes. | Exercise all four sheet actions, kind changes and already-open submission; after Done require authoritative refresh installation before new writes. Preserve original amount/reason/key/OCC and readable recovery context. Existing DebtAdjustmentViewModelTest is the direct JVM producer. |
| Input publication and stored-payload recovery | save trims reason but only checks nonblank; readSupportedDebtAdjustment accepts reasons the server rejects. Backend schemas/_debts.py applies max_length=500 to the wire string before debt_service/_adjustment.py strips and checks nonblank. Unicode limit is code points, not UTF-16 units. | Fresh 501-code-point reasons must fail before Room/scheduler/API; 500 emoji remain valid after input trim. Existing overlong wire payload must retain original bytes and have no futile Retry/send. Extend existing repository cases, including unsupported Drop; do not repair the stored command in place. |
| Detail + Obligations Sync + Settings Sync Retry | All route through DebtAdjustmentRepository.recover; Failed Retry resolves to Pending but never reaches the existing Outbox onEnqueued → AppContainer outboxScheduler.enqueueOnce hook (this exact head has no schedulePending API). Manual drain in the old test masks delayed execution. | Assert actual onEnqueued scheduler callback after successful requeue, before any explicit test drain. Preserve role/binding/support/Conflict rejection, original bytes and one committed fact. Extend repository and actual Room/UI recovery producer. |
| Confirmed fold and every retained canonical view | RecordDebtAdjustmentDispatcher discards the known-2xx Debt. DebtListViewModel observes only creation completion; ReceivablesViewModel and DebtGoalViewModel refresh on entry/return. StatsRoutes return callbacks fire before a later asynchronous adjustment completion, leaving a visible list/goal stale. There is no inspected Debt-wide notification owner; MainShellState plan/insights revision is a presentation invalidator, not a debt fact store. | Test dispatch after leaving detail with retained real repository/VM consumers. Continue using Room delivery evidence and server Debt/goal queries; do not invent a second financial owner or cascade fresh OCC. Known 2xx stays Done even if later read/notification fails; unknown response never supplies confirmation. |
| Direct qualification and omissions | Android-fast runs JVM/Detekt; Connected runs actual Room/shared detail UI. Tests below are source only at this handoff. | No local Gradle, emulator, Edge or PG; no runtime RED/GREEN claimed. Cloud must execute these exact new tests. Room reopen is not OS process death; synthetic remote/scheduler callback is not WorkManager or backend/notification proof. Backend protocol/schema production and existing key/binding/OCC wire contract remain unchanged. |

Test-first handoff evidence: four existing Kotlin test/fixture files changed, production untouched. The repository test class retains 11 tests/functions; the detail VM class now has nine tests plus setup/teardown (11 functions). The retained-consumer Connected scenarios are separate JUnit tests for the real DebtListViewModel, ReceivablesViewModel and DebtGoalViewModel, each using the actual RepositoryGraph and Room: load 50,000, submit +3,000 through DebtDetailViewModel, cancel that detail scope, dispatch once, require Done and the retained consumer's 53,000 while original key/body/OCC/binding stay unchanged. They establish direct consumer postconditions, not a composed NavHost journey or an actual server database. Existing actual shared-detail text input/Save/reopen/Retry assertions are preserved; its existing asynchronous wait also requires the scheduler callback before proceeding. Both Sync entrances reach the same tested repository, but these edits do not claim two additional independent Sync navigation runs.

Local evidence is limited to diff checks, lexical delimiters, actual signature reads, four single-file Android classifier results and a seconds-scale pure Pydantic probe evaluating the actual schema Field expression: BMP/emoji 500 accepted; BMP/emoji 501 and a raw 500-plus-space rejected. This does not execute a backend route or money validation. Kotlin/JVM/Connected have not run locally; no cloud RED is claimed until root qualifies this test-only SHA. No commit or push.
