# Original debt-adjustment intent

This slice continues the whole-system Internal Beta RC Goal and the capability-first ruling. The final Product contract sections 6.4 and 9 own relationship facts and original-command recovery. The current source shows a missing Android continuity path: every direct `recordAdjustment` call generates a new key, while the backend can recover a committed response only under the original key and body. Closing the form also clears its submitted context. No duplicate financial posting has been demonstrated; the known consequence is loss of the original recovery path after an uncertain result.

## User outcome and owners

An authorized user adjusts one external debt with a signed amount and a reason. Local acceptance retains the original debt identity, label, currency, amount, reason, logical binding, payload revision, OCC and idempotency key before networking. Unknown results, protocol refusals and application restart retain that exact intent visibly. Retrying recovers the existing backend command under its original key; it neither guesses a fresh OCC nor creates a replacement adjustment. A confirmed conflict asks for reconciliation or explicit local-intent discard. The authoritative debt fold changes only through the existing backend adjustment command.

Reuse Room/Outbox, its session and expiry boundaries, the existing debt command, and current sync recovery entrances. The detail page and global recovery explain the original adjustment and distinguish local acceptance from committed financial fact. Retire the Android direct adjustment writer and migrate its real consumers. Other repayment, proposal, forgiveness, void and classification commands are outside this bounded change; their separate continuity gaps remain in the full Goal.

## Minimum proof and integration

First reproduce the present recovery failure through the actual client boundary. Then verify durable publication before networking, original-key response-loss recovery, unsupported/protocol/conflict retention, and unchanged binding/OCC/expiry semantics. A real Room/UI consumer must show original context after reopen and complete the actual save/recovery path. Existing backend adjustment idempotency and fold tests remain the server proof; expand them only if an executable gap requires it. Use short local source/contract checks and exact-candidate cloud Android/backend qualification, followed by bounded review and protected integration.

The branch uses Income candidate `dcf79dae` to reuse its corrected refusal model and read protocol. That parent is still awaiting its own gates; it is not a qualified main baseline. This dependent draft may run its test-first cloud counterexample in parallel, but integration and final qualification must use the actual integrated parent. After the parent merges, bring main forward normally and qualify this slice on its resulting exact head. No local long Gradle/PostgreSQL test, no new financial subsystem, and no held Windows lifecycle work are authorized by this slice.

Current implementation publishes a versioned original adjustment with its signed amount, reason, record currency, debt identity and label, principal binding, original OCC and key. The registered dispatcher owns the only Android network adjustment call, uses the original request unchanged, and does not cascade a fresh OCC. Detail reads and recovery are independent; local acceptance does not replace the canonical Debt. Detail and both global recovery consumers explain the original command, reject unsupported retries and retain protocol refusals. Original-key expiry and binding ownership remain with the existing Outbox.

Observed test-first evidence: source `1f3bd2c5`, CI `34029178586`, Android job `101475482566`, qualification checkout `60bcef60bc51cdcc621d0d669d14dfb57060cf4d`; 2,097 JVM tests with one failed original adjustment recovery case. That direct-writer test is migrated to the new actual repository/drain boundary when the writer is retired, alongside pre-network persistence, original-key replay, protocol/unknown/404 refusal, conflict, unsupported payload, permission and binding cases. New real Room/graph/detail Save and Retry coverage closes and reopens the on-disk database while the canonical GET fails. This is prepared coverage, not yet executed passing evidence.

The test-first candidate also failed repository-weight on its long/complex test function and one unchanged Connected hinge case reported no Compose hierarchy. Those are observed failures, not waived green results or established production regressions. The current implementation brings forward the already-qualified Income `ef54e1a5` predicate-owner placement; parent integration remains pending its new formal month-status finding. No result on either parent substitutes for this candidate's own gates.

## Impact closure rebuilt after the explicit owner-change discipline

This inventory was rebuilt on 2026-09-06 after the user required every business-semantic or owner change to enumerate its entrances, consumers, former success exits, persistence/protocol/recovery paths, and direct verification producers before and after implementation. It reconstructs the earlier implementation; it does not retroactively claim that discipline was followed before the work. The compared subjects are test-first `1f3bd2c5ac1d4b36434dff532e188a17a510af5e` and implementation `379a8ce175c8695f87c520375fde7acfdc00d047`. Line references below describe the latter unless marked otherwise.

| Path | Before | Current owner and consumer evidence | Direct verification / limit |
| --- | --- | --- | --- |
| All Android detail entrances | Debt, receivables, and goal-linked detail shared the direct `DebtActions.recordAdjustment` writer. | `StatsRoutes.kt:177,256,378` all use `DebtDetailHost:315`; its three VM factories (`StatsRoutes:230,362`, `DebtGoalRouteModels:40`) require the same adjustment repository. `DebtDetailScreen:398,417` feeds the existing signed amount/reason form into `DebtDetailViewModel.submit:259`. | `DebtAdjustmentViewModelTest` covers the submitted original fields, local acceptance, and replacement bindings; `DebtAdjustmentRoomContinuityTest:40` drives the real detail Save and Retry. Navigation factory wiring is code evidence, not three separate instrumented journeys. |
| Direct-writer and old-success retirement | `DebtRepository.recordAdjustment` minted a key for each call and returned a folded Debt; the detail VM cleared the form and announced recorded success. | That interface member, implementation, five old tests, and dedicated direct-call probe are removed. `RecordDebtAdjustmentDispatcher.kt:20` is the only Android production `recordDebtAdjustment` invocation. `DebtDetailViewModel:260,357` returns a queued intent ID, retains the canonical Debt, and says the original adjustment is saved locally. The old adjustment success resource has no reachable adjustment submission path. | Migrated `DebtAdjustmentRepositoryTest` has 12 repository/drain cases; the original test-first response-loss failure is recorded above. Other direct repayment/void/kind operations still take the committed-result branch; no replacement protocol was introduced for them. |
| Publication and persisted representation | No Android adjustment command was stored before networking. | `DebtAdjustmentRepository.kt:52` validates binding/role/debt/currency/amount/reason and same-target unresolved work, then stores revisioned subject, origin, original request/OCC/key. `OutboxRepository:380-408` inserts Room state under the binding lease before the scheduler callback. `PendingMutationType:51` adds a wire kind; Room columns/schema and backend request field names are unchanged. `RepositoryGraph:123`, `OutboxAdapterGraph:145`, and `AppContainer:290` connect one repository/adapter/dispatcher. | Repository tests inspect persistence before scheduler and zero direct sends. The Connected fixture reopens an on-disk `AppDatabase` through the production repository graph and compares original persisted fields. This is Room reopen evidence, not a claim of OS process-death qualification. |
| Dispatch, protocol, and backend fact owner | API and Web already shared the backend adjustment command and its idempotency/OCC ordering. | `OutboxDrainWorker:75` reaches the registered engine; `AppContainer:124` uses the persisted binding. The existing runtime interceptor still gates the POST; its only before/current diff moves an unchanged predicate into its class. Dispatcher unsupported/protocol/unknown-409/404 results preserve Failed state; `state_conflict` preserves Conflict; IO retains the original command for bounded retry. Success supplies no fresh token to cascade (`RecordDebtAdjustmentDispatcher:21`). API `routes/debts.py:196` and Web `routes/web_debt_actions.py:225` retain `debt_command_service.py:216`: original-key HIT precedes `record_adjustment`/OCC, and fact plus receipt commit together. | Dispatcher refusal/no-cascade tests and unchanged `backend/tests/test_debt_adjustment.py:169,186,291` cover replay, response loss/read models, and stale OCC. No backend source/test diff exists in this interval. A stale new-key 409 is not evidence of duplicate posting. |
| Detail recovery and new completion exit | Dismissal or reopen could lose the submitted amount/reason/key; canonical GET was the only detail source. | `DebtDetailViewModel:148` observes original commands independently of canonical GET. `DebtDetailChrome:89` renders them even when that read fails. `DebtPendingAdjustments:38` offers original Retry or confirmed local Drop; `DebtAdjustmentRepository:76` rechecks current binding/role/support and never changes OCC. Newly Done records refresh canonical Debt once; historical Done and Done-before-local-ack cases are guarded. Existing `DebtDetailScreen:147` reloads repayment history on canonical row-version change, and the three hosts retain their return-to-list/goal refresh callbacks. | Six VM cases include failed detail read, historical/new Done, early Done, and same-ledger identity changes. Real Room/UI test asserts old balance before dispatch, original-key replay, one fact, then the refreshed balance. No failed/unknown result uses the canonical-success exit. |
| Both global recovery entrances | Shared Sync screen had no adjustment context map or adjustment retry routing. | Obligations Sync (`MainProductNavRoutes:237`) and Settings Sync (`SettingsDestinationHost:378`, supplied by `SettingsRoute:81`) both require `OutboxRecoveryRepositories.debtAdjustments`. `OutboxStatusViewModel:55-76` exposes Pending/InFlight context and Failed/Conflict maps; `:109` routes adjustment Retry through its repository. `SyncStatusScreen:145,156,172` displays original context; unsupported Retry is absent. Conflict Keep Mine remains expense-only (`:229`), so debt cannot obtain a fresh OCC there. Both Drop paths use existing owner/ledger/status-checked Outbox deletion and an explicit explanation that local Drop does not undo a possible server fact. | `OutboxStatusViewModelTest` covers unsupported Retry/Drop and reopened Pending-to-InFlight context. Existing shared recovery UI tests remain producers; dedicated adjustment Connected coverage exercises detail, not two independent Sync navigation journeys. |
| Shared ownership, expiry, and adjacent consumers | Outbox owned binding transitions, same-target ordering, retry age and stale status checks. | Those implementations are unchanged and the new type enters them: `OutboxRepository:694-738,813-832,954-966`; `OutboxDrainEngine:283-338`. Retry preserves original key/body/OCC; unsupported cannot send; viewer cannot publish/requeue; wrong owner/ledger cannot borrow context; expired intent cannot be requeued. Existing creation, income, and recurring summaries remain in the same shared Sync maps and positions. | New repository cases directly exercise role/binding/unsupported/conflict/original bytes; existing Outbox expiry/resolve tests remain applicable through the unchanged shared functions. No independent lifecycle, Windows, or other debt-mutation owner was added. |
| Direct cloud producers and current follow-up edits | The direct-writer counterexample ran and failed at the test-first head; later implementation added real repository/Room consumers. | `ci_gap_trigger_scope.py:11-21,304` includes Android source/tests. `.github/workflows/ci.yml:990` runs JVM tests/count/lint/Detekt; `android-connected-test.yml:145` runs the full Connected task with exact qualification source checks. Repository-weight is also a direct producer: artifact `9988962501` reports the Sync context test at line 152 through class end 232, length 81. The authorized follow-up extracts only its original-context assertions into a helper before the test. Root separately extracts the identical recurring/income/adjustment summaries from both Conflict and Failed render sites into the same-package internal `SyncStatusOriginalIntentSummary` in existing `SyncStatusOverview.kt`; maps, ordering, guards, and Retry/Drop remain unchanged in that inspected diff. | Test helper inlining reconstructs the original test body exactly modulo whitespace; all 44 assertion calls remain, including Pending retry count 0, first InFlight count 1, complete row, key, original request/binding fields, single-row, failed/conflict and busy checks. Static physical span is now 74 under the previous reported boundaries; Lizard/Gradle were not rerun locally. The production summary extraction and this test edit require a new exact-head gate. |

Exact `379a8ce` cloud snapshot: Connected run `34031791504`, actual execution job `101482565554`, and CodeQL run `34031791503` are successful by independently read GitHub status. Root inspected actual Connected output and reports 115 executed, zero skipped and zero failed; this inventory did not independently obtain per-case JUnit for that run. CI `34031791511` is not qualified: backend-contract job `101482566294` failed repository-weight, and root inspected Android-fast job `101482566447` failing Detekt on `SyncStatusPageBody` complexity 17 against 14 after JVM execution. Neither result is waived or replaced by another head. The two follow-up extractions above address those concrete producers without reducing assertions or changing business guards; their new SHA and final CI/CodeQL/Connected results remain pending.

No additional actionable production omission was found in this bounded before/current path review. The recorded limits, parent integration, new-head qualification, protected merge, and independent merge-main qualification remain open; this table is not a full-Goal completion claim. This reviewer changed only this contract and the named JVM test, ran no Gradle/PG/emulator, and made no commit or push.

## Input-validation test consolidation after Android-fast Detekt

Before this test-only edit, source `a027ef9bb0f7d2d9babcc5eadf1c6b1cd7cd9132` was clean. Root reports Android-fast job `101486147691` failing `TooManyFunctions`: `DebtAdjustmentRepositoryTest` contains 12 functions against the configured class limit of 11 (`android/detekt.yml:39`). Source inspection confirms both counts. The impact inventory below was established before this edit and checked against the resulting diff; no production semantic or owner change is included.

| Impact surface | Before | After and evidence boundary |
| --- | --- | --- |
| Verification inputs and consumer | Separate `zeroAmountCannotPublishOrSend` and `blankReasonCannotPublishOrSend` tests call the actual `DebtAdjustmentRepository.save` through `DebtAdjustmentFixture`. | One `zeroAmountOrBlankReasonCannotPublishOrSend` test explicitly supplies `(0L, "  补记借款  ")` and `(3_000L, "   ")`, matching the former explicit/default inputs. Each loop iteration creates a new fixture. Both still assert failure, no persisted rows, no scheduler queue entries, and no API calls; input-specific assertion messages identify the failing case. |
| Entrances, former success exits, persistence, protocol and recovery | Detail and both Sync entrances, local acceptance, Room/Outbox publication, dispatcher, original-key/OCC recovery and backend fact ownership are inventoried above. | Their production files, fixture/adapter/probe, and the other ten repository tests are unchanged. The two invalid inputs retain the same rejection postcondition before publication or sending; no success exit, stored representation, role/binding rule, Retry or Drop path is added or removed. |
| Direct verification producers and risk | Android-fast `.github/workflows/ci.yml:985-990` executes JVM tests, result-count ratchet, lint and Detekt. Two independent JUnit methods previously covered these validation inputs. | The test class now has 11 functions and 11 `@Test` methods; two validation input cases remain. Total JUnit method count decreases by one. The existing result-count minimum, Detekt configuration and suppressions remain unchanged. Static input/assertion and diff checks are the local evidence; no Gradle/PG/emulator ran. New exact-head JVM/Detekt and result-count qualification remain required; the earlier failed job is not a passing result for this edit. |


## Formal P2 owner closure: test-first impact at a033c08b

This inventory is established before the new counterexamples, following the user requirement to enumerate every affected entrance, consumer and former success exit. Root admitted formal T5H/T5I (duplicate Facts ViT/Vib and Gov Ukh/Ukl); the previous bounded-review conclusion does not close these newly demonstrated gaps. Production is frozen for this test-first candidate.

| Responsibility / real consumers | Before and executable counterexample | Required boundary / direct producer |
| --- | --- | --- |
| Detail mutation gate: DebtRoute, ReceivablesRoute, DebtGoalRoute share DebtDetailViewModel | openAction blocks only Adjustment; Repayment, Void, RepaymentVoid, selectKind and already-open submit can use the old fold while a stored adjustment is Pending/InFlight. Done disappears before GET succeeds, so a failed refresh reopens stale writes. | Exercise all four sheet actions, kind changes and already-open submission; after Done require authoritative refresh installation before new writes. Preserve original amount/reason/key/OCC and readable recovery context. Existing DebtAdjustmentViewModelTest is the direct JVM producer. |
| Input publication and stored-payload recovery | save trims reason but only checks nonblank; readSupportedDebtAdjustment accepts reasons the server rejects. Backend schemas/_debts.py applies max_length=500 to the wire string before debt_service/_adjustment.py strips and checks nonblank. Unicode limit is code points, not UTF-16 units. | Fresh 501-code-point reasons must fail before Room/scheduler/API; 500 emoji remain valid after input trim. Existing overlong wire payload must retain original bytes and have no futile Retry/send. Extend existing repository cases, including unsupported Drop; do not repair the stored command in place. |
| Detail + Obligations Sync + Settings Sync Retry | All route through DebtAdjustmentRepository.recover; Failed Retry resolves to Pending but never reaches the existing Outbox onEnqueued → AppContainer outboxScheduler.enqueueOnce hook (this exact head has no schedulePending API). Manual drain in the old test masks delayed execution. | Assert actual onEnqueued scheduler callback after successful requeue, before any explicit test drain. Preserve role/binding/support/Conflict rejection, original bytes and one committed fact. Extend repository and actual Room/UI recovery producer. |
| Confirmed fold and every retained canonical view | RecordDebtAdjustmentDispatcher discards the known-2xx Debt. DebtListViewModel observes only creation completion; ReceivablesViewModel and DebtGoalViewModel refresh on entry/return. StatsRoutes return callbacks fire before a later asynchronous adjustment completion, leaving a visible list/goal stale. There is no inspected Debt-wide notification owner; MainShellState plan/insights revision is a presentation invalidator, not a debt fact store. | Test dispatch after leaving detail with retained real repository/VM consumers. Continue using Room delivery evidence and server Debt/goal queries; do not invent a second financial owner or cascade fresh OCC. Known 2xx stays Done even if later read/notification fails; unknown response never supplies confirmation. |
| Direct qualification and omissions | Android-fast runs JVM/Detekt; Connected runs actual Room/shared detail UI. Tests below are source only at this handoff. | No local Gradle, emulator, Edge or PG; no runtime RED/GREEN claimed. Cloud must execute these exact new tests. Room reopen is not OS process death; synthetic remote/scheduler callback is not WorkManager or backend/notification proof. Backend protocol/schema production and existing key/binding/OCC wire contract remain unchanged. |

Test-first handoff evidence: four existing Kotlin test/fixture files changed, production untouched. The repository test class retains 11 tests/functions; the detail VM class now has nine tests plus setup/teardown (11 functions). The retained-consumer Connected scenarios are separate JUnit tests for the real DebtListViewModel, ReceivablesViewModel and DebtGoalViewModel, each using the actual RepositoryGraph and Room: load 50,000, submit +3,000 through DebtDetailViewModel, cancel that detail scope, dispatch once, require Done and the retained consumer's 53,000 while original key/body/OCC/binding stay unchanged. They establish direct consumer postconditions, not a composed NavHost journey or an actual server database. Existing actual shared-detail text input/Save/reopen/Retry assertions are preserved; its existing asynchronous wait also requires the scheduler callback before proceeding. Both Sync entrances reach the same tested repository, but these edits do not claim two additional independent Sync navigation runs.

Local evidence is limited to diff checks, lexical delimiters, actual signature reads, four single-file Android classifier results and a seconds-scale pure Pydantic probe evaluating the actual schema Field expression: BMP/emoji 500 accepted; BMP/emoji 501 and a raw 500-plus-space rejected. This does not execute a backend route or money validation. Kotlin/JVM/Connected have not run locally; no cloud RED is claimed until root qualifies this test-only SHA. No commit or push.


## First three formal fixes: implementation impact after actual JVM RED

Before production edits, exact 206c023a9ad60f73afac347a2dea2e62925d5f5a was clean. Root personally read CI 34049979354 / Android-fast 101531733741: compilation completed, testGrayDebugUnitTest executed 2,130 cases with six behavioral failures (unsupported payload, invalid reason/amount, Failed Retry scheduling, all-action gate, Done plus failed canonical GET, and already-open form submission). BUILD FAILED after 6m16s is not whole-CI qualification. Root requested cancellation of remaining CI work; completion of cancellation is not yet verified. CodeQL 34049979335 succeeded. Connected 34049979327 / execution 101531730693 remains reserved for the three background-consumer counterexamples; no result is claimed here.

| Owner / direct and indirect consumers before edit | Authorized change and retired success exit | Verification and boundaries |
| --- | --- | --- |
| Existing adjustment payload validator, save, dispatcher, detail and both Sync support projections | One reason rule validates the wire string by Unicode code points and server whitespace; fresh input is normalized before publication, retained payload is never rewritten. Reject unsupported original bytes before dispatch/Retry. | Existing exact JVM RED plus positive 500-emoji/negative 501 cases; preserve original key/OCC/body/binding. Python strip also accepts U+0085 whitespace, which Kotlin's usual trim does not. |
| Outbox onEnqueued → AppContainer enqueueOnce; original enqueue and DebtAdjustmentRepository.recover | Extract the existing best-effort scheduler invocation into an explicitly new internal method and reuse it after an actual same-binding Failed→Pending transition. Do not invent an existing schedulePending interface; do not schedule expired/Drop/Conflict/stale transitions. | Repository callback RED and actual shared-detail Room/Retry test; both Sync entrances route to the same recovery owner. WorkManager itself is not locally executed. |
| DebtDetailViewModel, all three detail hosts, repayment/history Void, kind editor, already-open action sheet | Keep role authority distinct from a single derived write gate. Block until the initial adjustment snapshot is known, while an original command is unresolved, and until a post-Done canonical version is installed. Gate direct methods and actual UI enable/visibility; show the reason for waiting, retain draft and original command. | Three exact JVM behavioral RED cases plus existing historical/new/early-Done and binding tests. Existing state may be split into its own source file without creating a new owner. |
| Global delivery and retained list/receivables/goal consumers | Still observation/design only. This exact OutboxDrainEngine has onAdviceInputReplaySucceeded, not a generic onMutationCommitted/settled hook. AppContainer's expense completion and MainShell plan/insights invalidation are candidate existing seams to inspect, not debt facts. | Connected fixture is RepositoryGraph plus an explicit dispatcher, not full AppContainer. A future fix must migrate the test through the chosen real notification producer rather than impose a fake topology. Fourth-item production remains frozen. |

First-three-fix handoff: the same DebtDetailUiState now lives in DebtDetailUiState.kt; this is a physical extraction, not a new state owner. Its shared gate is consumed by DebtDetailChrome's external action/kind and repayment-void surfaces, DebtDetailScreen's actual Save button, and openAction/submit/selectKind. An already-open kind picker is withdrawn when the gate closes. Pending/Failed/Conflict rows and a not-yet-observed local acceptance block new writes; after Done, the original expected version stays as a refresh barrier until a newer server row is installed. A failed GET leaves that barrier and an explanatory refresh message. Original Room rows are not edited by this projection. Two old open/dismiss unit fixtures now perform their real loadDebt prerequisite; all original assertions remain, and dismissal additionally proves the sheet was actually opened. Unicode coverage adds U+0085 blank rejection and trimming around both existing 500-character positive cases.

The explicitly new OutboxRepository.schedulePending method contains the former enqueueInternal best-effort callback verbatim; enqueue uses it and adjustment recovery calls it only after successful resolution and a fresh same-binding Pending-row check. Expired, stale, Conflict, Drop and unsupported rows do not schedule; existing shared resolution/key/OCC rules and AppContainer WorkManager wiring are unchanged. Backend request schema, API shape, dispatcher and unknown-result mapping were not modified in these first three fixes. Local XML/placeholder parsing, Kotlin delimiter/signature checks, individual Android classifier checks and actual Pydantic Field/Python-strip probes passed; Kotlin execution awaits cloud, not inferred GREEN.

Fourth-fix consumer inventory before editing: DebtListViewModel, ReceivablesViewModel and DebtGoalViewModel retain visible balances; CreateDebtGoalViewModel:65 supplies the remaining-amount candidates rendered by CreateDebtGoalScreen:198; RepaymentDraftInboxViewModel:83/180 retains balance/OCC candidates used by its matching and confirmation flow and rendered at RepaymentDraftInboxScreen:413. All five require the same bound completion projection. Other listDebts callers (BudgetViewModel, LedgerViewModel, IncomePlanViewModel/Edit, CreateSpendingGoalViewModel, SpendingGoalDetailViewModel) only resolve ledger currency; adjustment does not change currency and their balance-independent consumers require no adjustment refresh. MainShell planDataRevision refreshes budget/recurring/income, and insightsDataRevision refreshes reports/data quality; neither is currently the five debt consumers' completion source. No speculative plan/insights invalidation will replace the real Debt query chain.

Root/Parfit verified Connected 34049979327 / execution 101531730693 for source 206c023a (checkout 72f27cbf18e9fdcb801358ad3ed7d140768b76b5, same tree 1b610a3f482a4aa1061131928dd4138efb681fdf), artifact 9994408117: 120 executed, four failures, zero errors/skips. Each retained-consumer test passed initial 50,000, detail submission/scope cancellation, drain.done=1 and persisted Done before timing out on balance=53,000 at line 133. The later direct assertion and exact final balance were not recorded. The fourth failure is original UI Retry waiting for Pending plus scheduleCalls=2 at line 76. This is actual behavior RED, not compile/setup failure; fourth-item implementation is now separately authorized.

## Additional negative-remaining formal: test-first impact at 206c023a

Root separately admitted ft4Vi; no production fix for this additional finding is included in this test-first slice. The existing backend adjustment owner computes the fold and rejects a negative remainder with 422 debt_adjustment_negative_remaining. This is a definite refusal, not an unknown response or proof of another committed fact.

| Real owner / consumers | Before and required postcondition | Direct producer / evidence boundary |
| --- | --- | --- |
| Debt detail shared by DebtRoute, ReceivablesRoute and DebtGoalRoute → existing submit → DebtAdjustmentRepository.save | A decrease larger than the displayed canonical remainder is accepted locally. Reject it before publication without adding signed amounts; retain the open form, amount, reason, sign and canonical debt. An exact decrease to zero remains legal. | Existing DebtDetailViewModelTest decrease case now tries 500.01 against 500.00, then corrects that same form to 50 or 500. Existing repository input case checks -50,001 and Long.MIN_VALUE against 50,000 with zero rows/scheduling/API; the exact-zero case retains the original request and drains once. These tests use APIs already present at 206c and do not depend on the in-progress completion projection. |
| Existing dispatcher refusal → retained Failed original → detail and both Sync support/Retry/Drop | A definite negative_remaining refusal must retain Failed and its key/body/OCC/binding but offer no futile Retry. It must not use a newly queried balance to rewrite the command, and explicit local Drop remains available. | The existing definite-refusal repository case preserves the state_conflict assertions and adds the real 422 code with original-byte, no-scheduling/no-second-send and explicit-Drop assertions. The remote is an explicit refusal probe, not a PostgreSQL proof. |
| Old local-success and direct verification exits | Invalid reduction must not close the draft or emit the local-accepted success exit; valid zero reduction still follows the original intent owner. Android-fast is the direct JVM/Detekt producer, and source paths keep the existing Android classifier scope. | Source-only test-first changes, no local Gradle/PG/emulator execution and no actual RED claimed for ft4Vi yet. The six earlier JVM failures and three retained-consumer Connected failures do not substitute for this new exact test candidate. |

## Fourth-fix implementation closure: bound Room completion and all five consumers

This is the after-impact inventory for the separately authorized fourth fix, superseding the earlier observation-only status. Root committed only the two additional ft4Vi tests as f8813105; that head had no workflow because an Atlas-only merge conflict blocked the PR. Root then normally merged main as d0fdd45071d3a7f2a4bbd127cbdff7272e4b7b4c, tree d7fb26fb56c3e3897481f68ded4d6e7d9da819d2, preserving these uncommitted production edits. The additional negative-remaining production fix still awaits its own actual behavioral RED.

| Owner / complete real consumer set | After, former exit retired, and invariant | Direct producer and limits |
| --- | --- | --- |
| RecordDebtAdjustmentDispatcher → existing OutboxDrainEngine.markDone → bound Room row → DebtAdjustmentRepository.observeCompletionRefreshes | The existing adjustment owner now exposes a required typed DebtAdjustmentRefresh flow. It reads real same-binding Room snapshots with completed rows included. First actual snapshot triggers one authoritative initial query, including historical Done; later newly observed Done ids trigger refresh. Per-collector/per-binding seen ids survive GC emissions and expire with the collector; no persistent second completion ledger, global bus, synthetic initial empty Room snapshot or optional fallback was added. | New DebtAdjustmentRefreshTest drives the actual repository and Outbox with a delayed DAO read, historical/new Done, Failed/Pending, GC, binding replacement and collector reopen. This is JVM source awaiting execution; its fake DAO does not prove SQLite itself. The five Connected consumers below use actual disk Room and RepositoryGraph. |
| DebtListViewModel in both list lenses and Relations composer; ReceivablesViewModel; DebtGoalViewModel | All require the adjustment owner through ViewModelFactories, StatsRoutes, RelationsRoute and DebtGoalRouteModels. Each waits for the real initial snapshot, refreshes its existing canonical query after new Done and invalidates older query generations on binding replacement. Existing return/entry and creation refreshes remain, but are no longer the sole delivery refresh exit. No DebtCreationActions or advice-input ownership was expanded. | The original three actual Connected RED scenarios remain: 50,000 → detail Save and detail-scope cancellation → actual drain/Room Done → retained 53,000 with immutable key/body/OCC/binding. All existing JVM constructors now explicitly supply the same bound adjustment fixture. |
| CreateDebtGoalViewModel → CreateDebtGoalScreen candidate amount, selection and Save | A completion refresh preserves the original name and selected ids while replacing candidates from the server. A selected debt that is no longer open cannot silently disappear from the eventual request. The actual form flags that selection and offers explicit removal of unavailable selections; Save and the direct method refuse until reviewed/current. Original candidate-order request semantics remain. | Added actual-graph/Room retained-candidate Connected case checks 53,000, row version 3 and unchanged name/selected id. The existing JVM success case now also exercises a selected debt closing, no partial request, explicit removal, and unchanged eventual request order; original success assertions remain. |
| RepaymentDraftInboxViewModel → suggested target, opened picker and confirm OCC | The existing query refresh clears obsolete targets/suggestions before waiting, including when draft or debt reads fail. Actual row actions and an opened picker pause during loading. confirm also rejects a stale target/version instead of silently substituting the latest OCC; the user must choose from the new canonical candidates. Existing draft persistence, confirm endpoint and success flash/refetch ownership are unchanged. | Added actual-graph/Room Connected case checks retained target/suggestion version 3 and 53,000. The existing stale-refresh JVM case now triggers adjustment completion, refuses the old callback during/after refresh, then confirms using the explicit fresh choice. Three old confirm fixtures now provide the actual chosen candidate rather than bypassing the picker prerequisite; their original assertions are retained. |
| Persistence/protocol/recovery and other former success exits | Dispatcher, engine, AppContainer and backend schema/request remain unchanged for this fourth fix. Completion refresh runs after Room Done observation, so a later read/observer exception cannot be returned as dispatch Failure. Unknown response remains unconfirmed and never emits Done refresh. First-three validation, shared Retry scheduling, detail refresh barrier and both Sync recovery routes remain as inventoried above. | Actual graph fixtures use the same Outbox as the dispatcher; there is no missing AppContainer-only callback in the counterexample topology. MainShell plan/insights revisions and the expense budget callback remain their original owners. Currency-only listDebts consumers identified above are unchanged because adjustment cannot change their consumed currency. This is not proof of cross-device propagation, OS process death, WorkManager execution or a real backend database. |
| Direct compilation/UI/weight producers | Every production factory and direct JVM/Connected constructor is migrated with a required dependency. The isolated DebtCreateSheetContinuityTest and ReceivablesScreenNavigationTest use one explicit initial-read fixture with unexpected mutations rejected; these are UI boundary tests, not completion-source proof. DebtListUiState/DebtDraftUi are physically extracted unchanged to their own file, as for detail state, to avoid growing an already 503-line VM file. | Existing Android path classifier selects Android-fast and Connected for these source/test paths; backend contracts/weight remain their existing producer. The retained-consumer test class has 11 functions and the shared scenario spans 68 physical lines; no suppressions/thresholds were changed. Local evidence is only source/XML/signature/diff checks. No local Gradle/PG/Edge/emulator and no new-head GREEN claim. |

### Fifth-fix implementation after actual negative-remaining RED

Root and Parfit read d0fdd450 / Android-fast 101538476496 (checkout 27d0cfc235691ad913e8de7b7b56b4267ee21c5d, same tree): 2,130 tests, eight failures, zero errors/skips. The definite-refusal test passed the whole 409 state_conflict iteration, then failed at line 183 because negative_remaining recovery succeeded. The input loop passed zero, then failed on -50,001 at line 232; Long.MIN_VALUE, reason cases and exact-zero acceptance were not reached in that execution. The first 500.01 VM attempt failed at line 308 because validationError was absent; positive corrections to 50/500 were not reached. Those boundaries remain required GREEN evidence, not inferred passes.

| Actual owner / consumers | Implementation and old exit retired | Verification / limits |
| --- | --- | --- |
| Fresh detail form and DebtAdjustmentRepository.save | One adjustment-local comparison checks a decrease against the known remainder without adding signed money. Repository and actual VM validation both reject over-reduction/Long.MIN_VALUE while preserving the draft; exact reduction to zero is valid. This comparison is not used to reinterpret any retained payload against a later balance. | Existing actual RED cases retain every original assertion. No new API/schema, money addition or backend constraint change. |
| Known server refusal → generic HTTP error retention → original adjustment descriptor → detail and both Sync | The existing safe outbox recovery-code whitelist retains the precise debt_adjustment_negative_remaining code. Payload decoding remains supported/readable; a separate canRetry rejects this definite failure and expired/unsupported/non-Failed states. Repository recover is the final guard. DebtPendingAdjustments, both SyncStatusScreen callers and OutboxStatusViewModel use the same permission. Shared original-intent summary and the existing safe error-message map explain the refusal and explicit local Drop; amount/reason/key/OCC/binding remain stored unchanged. | Existing definite-refusal repository case proves original bytes, no further send/scheduling after the definite refusal, and explicit Drop. Existing OutboxStatusViewModel case now covers both an unsupported payload and a readable definite reduction refusal, checking same original row, no queue entry, safe message and Drop. Protocol-upgrade Failed remains eligible under the original retry policy; state_conflict remains Conflict. |
| Retained Retry callback from detail or either Sync | Before: recover described the caller's Failed snapshot, so an older retryable unknown-result callback could requeue a now-definitely-refused row. After: the snapshot only locates the id/target; existing bound activeForTarget reads the current Room row, and its descriptor/status decides recovery. Missing or no-longer-recoverable rows return an explicit state-change error. No new Retry owner or DAO policy was added. | The existing definite-refusal test first drains an unknown refusal, retains that readable snapshot, retries through the actual repository, then drains the same command into Conflict or negative_remaining Failed. Both the current and old callbacks must preserve the final refusal, original bytes and two prior scheduler/API calls without a third attempt; explicit Drop remains. This added stale-callback branch is source evidence awaiting cloud execution. |
| CreateDebtGoalScreen in-place refresh after a completion-triggered read failure | Before: background refresh preserved the draft and blocked Save on a failed read, but the actual page refresh callback invoked reload and erased the name/selection. After: page refresh calls the same existing refreshCandidates method used by completion; first entry and binding replacement retain reload's reset semantics. There is no second draft/reset owner. | The existing initial-load-failure VM case is extended through initial recovery, edited name/selection, completion-triggered failure, blocked Save and explicit in-place refresh recovery. It requires unchanged name/selection and current candidates with no create call. The actual screen callback is statically checked; this added branch awaits final JVM and the existing five-consumer Connected qualification, without claiming a local behavioral RED. |
| Open Goal detail: full canonical list followed by the existing detail GET | GoalsApi and GoalListResponseDto return the same complete GoalDto as detail; backend GoalListResponse.items are GoalResponse, and list_debt_repayment_goals calls the same build_debt_repayment_goal_response serializer, including name/status/row_version/dates/debt_repayment. The newly read matching list Goal now replaces an old selected detail before the additional GET. A failed GET retains that complete canonical value with explicit error feedback; a successful GET still supplies the final value and existing witnessed-achievement logic. Generation/selection guards discard obsolete responses. | Root separately committed only DebtGoalCanonicalContinuityTest as 45d60da5. Actual Android-fast 101541090078, artifact 9995352284: 2,131 tests, nine failures, 5m32s. This case passed detailCalls=2 and the complete fresh list before failing at line 64: selectedGoal remained old name/RV3/50,000 rather than fresh name/RV4/53,000/updatedAt. The error assertion was not reached. Root authorized this production branch only after that behavioral RED. Existing detail-endpoint priority, celebration and generation tests remain; the new test has the mandatory adjustment dependency in the final candidate. |

Final working-copy source receipt at HEAD 45d60da5429f941f107029c78ac87f136225d1c3: 46 changed/untracked files, no staged files. git diff --check, lexical delimiters for 43 Kotlin files, both changed XML files' parsing/unique names/preserved placeholders, and the 45 individual Android-path classifier checks passed. All 110 calls of the five migrated VM constructors have the required argument count. DebtGoalViewModel, DebtAdjustmentRepositoryTest and DebtAdjustmentRoomContinuityTest each retain 11 direct member functions; the extended refusal/recovery tests span 36/38/41 physical lines. No production file newly crosses 500/800/1000 lines; the unchanged DebtList state extraction removes one existing crossing of 500. These are source checks, not Kotlin compilation, Detekt/Lizard, JVM/Connected execution or final-head qualification. No local Gradle, PG, Edge or emulator; no commit/push. Root will qualify the integrated candidate against all original and added behavioral assertions.

## Candidate 907fbe91: ordinary refresh and direct test producers

Actual Android-fast job 101543649431 in CI 34054450393 compiled and ran 2,134 JVM cases: two failures, zero errors/skips (artifact 9995638485). Actual checkout e55559aecd296d27a6ed6db62e1caa4b7383e473 has the candidate tree 3251aabcad4b9896c4331a13cf8523ddb6478829. All original adjustment counterexamples, new completion-source cases, current/stale refusal recovery, selected-Goal continuity and in-place CreateGoal recovery passed at this rung. This is not overall candidate qualification.

Root traced the two failures to the newly overbroad ordinary-loading guard: a background GET disabled a legitimate external-debt repayment even when a canonical debt and actual Room snapshot were already present and no original adjustment was unresolved. The sole canWriteActions predicate now excludes only the applicable write barriers; a generic background read is not itself an unresolved command. Initial debt absence, missing Room snapshot, original pending intent, local-acceptance observation gap and post-Done canonical-version barrier remain. All existing UI/action consumers keep the same predicate; binding, OCC, dispatch and persistence owners are unchanged.

The two external-debt stale-read tests now assert the actual repayment call with original OCC and adopted version 6 before checking stale-result/loading behavior. The adjacent loading-only test had passed without a real write; it is no longer accepted as that proof. The member celebration case previously fabricated direct member repayment success despite the real backend direct-fact guard. Its producer now uses a newer canonical member query to supersede the older cleared snapshot; the no-celebration/OPEN oracle remains and additionally checks version 3 and no direct repayment. These are a bounded production correction and a corrected test model, not weakened stale-result or settlement assertions. New exact-head JVM/Connected qualification remains required; 907fbe91 is not relabeled green.

## Follow-up recovery decision and impact closure at 3ab6faf9

The full product Goal and the Owner's recovery-model instruction authorize this follow-up to the exercised #379 slice. This decision is made by Codex within the existing product-design delegation; it changes client command recovery and derived query readiness, not debt financial facts, principal identity, server protocol or Windows lifecycle. The current three final contracts remain authority. New detail/Inbox JVM and real Room five-consumer counterexamples were submitted as `3ab6faf9a93b4fbfb1d475a7cba2dd790b04d227`; their actual cloud outcomes are pending.

A user can stop retrying a Failed or Conflict adjustment whose server outcome is unknown. Preserve that original Outbox row as an explicit local `Abandoned` terminal, retaining its body, key, OCC, binding and failure context. It means only that this device will not send the command again. It is neither Done nor a server rollback. The existing adjustment recovery owner performs a same-binding/current-status CAS; old callbacks cannot abandon Pending, InFlight or Done. No second recovery queue, mutable debt fold or event bus is introduced.

The existing typed adjustment observation must include unresolved originals and both distinct terminal outcomes. Every canonical consumer waits for the actual first Room snapshot. A newly observed terminal invalidates older query generations and triggers its existing authoritative read; removing the unresolved barrier and installing the refresh barrier is one state update. Done requires a Debt row version greater than the original OCC. Abandoned allows the same version, but only from a request started after that terminal was observed under the current binding. A failed read retains visible recovery and keeps dependent writes unavailable. A confirmed missing target removes its old write entrance; the old dispatcher error marker alone does not prove deletion.

| Before-change responsibility | Required migration and directly affected producers |
| --- | --- |
| Debt detail Save/Retry/Drop, action opening, open-sheet submit and kind selection | Keep the original adjustment repository and actual debt GET. Establish the terminal read barrier without a write window; retain draft input and failure feedback. Existing DebtDetailViewModelTest unknown-result and same-RV/reopen cases are direct producers. |
| DebtList, Receivables, DebtGoal, CreateDebtGoal and RepaymentDraftInbox | Migrate the existing completion-only observation to the one typed Room observation. Preserve current bound query/generation owners. CreateGoal preserves its name/selection while pausing Save; Inbox excludes unresolved targets from suggestions/picker and rechecks the original owner immediately before confirm. Complete Goal projections do not contain Debt RV: their post-terminal full-query generation is the proof, never Goal RV compared with Debt OCC. Existing retained-consumer Connected, Inbox JVM and canonical Goal controls remain required. |
| Workspace Settings Sync and Obligations Sync | Both existing Drop actions must delegate to DebtAdjustmentActions.recover, as Retry already does. Preserve a readable local-stop description with no Retry or sending state. The overview must distinguish stopped local work from confirmed delivery; an empty runnable queue cannot claim that an unknown server result was verified. No new recovery surface or identity selector is introduced. |
| PendingMutationStatus, DAO and Outbox | One additional stored string, no new column or request field. A Debt-only CAS changes state and local terminal time, preserving the original command. Abandoned is excluded from claim, runnable, unresolved FIFO, expiry, retry and stale-InFlight recovery. Only Debt's explicitly requested observation includes it; other command types keep their current sets. |
| Persistence, cleanup and compatibility | Current string decoding already maps future statuses to Unknown. No server API version change is needed for this local-only status. Keep current explicit bound/quarantine cleanup. Any use of the existing terminal retention cleanup must count from the local stop time and must not delete Failed originals. There is currently no live gcCompleted producer; this slice does not invent background cleanup or claim it exists. |
| Old success exits and direct tests | Replace Debt-specific tests demanding physical row deletion with local-stop/original-preservation/non-dispatch assertions. Keep generic PatchExpense Drop deletion, invalid-input zero-enqueue and genuine 2xx Done assertions intact. Migrate all completion-interface fakes/constructors, actual fake DAO CAS, Sync descriptions and overview consumers; no empty-flow compatibility fallback remains. Verify same-target continuation only after the fresh canonical prerequisite, and preserve the existing server OCC/idempotency writer. |

Deleting the row immediately was rejected because Room can conflate the preceding Failed state and retained readers may never learn to invalidate their old balance. Marking it Done was rejected because it falsely asserts delivery. The independent durable recovery queue alternative duplicates the original intent owner. This maintained decision is resolved for implementation; actual RED, implementation, after-impact closure and final exact-head qualification remain pending. A later exact-path counterexample involving lost originals, false delivery, stale write admission or an unresolvable recovery exit reopens this decision within the same Goal.
