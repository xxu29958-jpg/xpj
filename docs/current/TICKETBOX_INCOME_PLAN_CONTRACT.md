# Income plan month and revision working contract

This implements the active whole-product Goal and the Owner's September 6 capability-first ruling. The final Product contract §6.3/§6.5/§6.8/§6.9 and post-G2 Planning gate remain the authority. This contract records the main agent's bounded product choices under that delegation; old code and tests do not decide their meaning.

## Goal and boundary

A household can declare and revise an income estimate, understand the month it affects, inspect the resulting monthly forecast, and recover the original submitted edit. No plan, pay day or forecast is a confirmed receipt, cash balance or financial transaction. FinancialFacts and actual expenses remain unchanged.

Allowed changes are the existing IncomePlan owner and its revision/query model, API/Web/Android consumers, actual edit publication and recovery, and directly affected budget/Advisor estimates. No income-accounting subsystem, automatic arrival confirmation, external provider call, new host setting or Windows lifecycle work is authorized by this slice.

## Product meaning

- Both `monthly` and `one_time` are declared estimates. The Web claim that a pay day proves actual receipt retires, including its one-time labels and summaries.
- Each command carries an explicit accounting month M, shown when the user submits and preserved with the original intent. Android opens a new form from the confirmed server month for its current binding, not the device timezone; an already opened form keeps its original M. It is a whole month; there is no daily proration or future scheduled-change editor in this slice.
- A new monthly plan begins at M. An ordinary edit appends a revision effective from M. Archive stops forecasts from M; restore resumes from M and does not fill archived months. Earlier monthly forecasts retain their earlier revisions.
- One-time creation retains its explicitly chosen target month, including an explicitly entered past estimate. A correction to a one-time estimate revises that selected month; moving it corrects the old and new target months. The form must describe that consequence. This is an explicit correction of a plan, with revision history, and does not claim an earlier confirmation or earlier knowledge of the estimate.
- Existing frequency conversion remains available. Monthly to one-time ends the monthly schedule at M and requires a target month at or after M. One-time to monthly starts the monthly schedule at M and retains any earlier single-month estimate. It cannot silently convert an earlier monthly forecast into a receipt.
- Management lists show the current plan head and OCC token. A month-specific query reads the applicable revisions, even when the current head is archived. A latest row and its `row_version` are not a substitute for revision history.
- One query owner supplies whole-month expected income and the portion scheduled through the current accounting date. A future month's whole-month estimate can be nonzero while its scheduled-through-today amount is zero. A pay day beyond a short month's final day falls on that month's final day.
- Monthly budget and Advisor planning use the whole-month estimate and identify it as an estimate. An income summary may separately show the scheduled-through-today amount. Clients do not rebuild monthly totals from the current active management list.

## Revision and delivery ownership

The existing IncomePlan service remains the sole command owner. It atomically claims the current head with ledger scope/OCC, appends an immutable typed revision, updates its current projection and settles the original idempotency claim. Failed or stale commands leave no revision or success receipt. Replays retain the original key, month, payload, principal binding and OCC; the replay clock cannot choose a new month.

The revision sequence determines the last applicable declared state for a queried month. Ordinary monthly changes cannot move behind a later established change; an explicitly described one-time correction is the bounded exception for its selected month. Revision metadata retains actual recording time separately from the declared accounting month.

Archive and restore retain their existing keyless same-status no-op only when the latest revision for that ledger and plan represents the current head's revision number, target status and original declared month M. A missing or undated revision, another head/status or a different month returns `state_conflict`; it cannot become success merely because the current status matches. The first read and the fresh read after a failed OCC claim use the same rule. No-op success appends no revision and changes no timestamps. This is a proved existing monthly state, not an original-key receipt. Edit delivery retains its separate fingerprinted key claim and returns a saved success before checking later head/OCC/month changes.

Before this slice, Android's actual edit entry called the direct update method; the uncalled offline helper was not a product capability. This slice connects that entry to durable intent before dispatch and retains readable original plan/month/amount recovery, including when the following GET fails. Local acceptance is not server confirmation. Conflict requires reviewing current facts before a new command; an unknown result retains the original command. Explicit local discard does not undo a server change.

An old queued payload without a declared month must stay preserved in a visible recovery state. It must not acquire a month from replay time, infer a month from an unrelated timestamp, silently change keys or dispatch with default fields. Both global sync entrances require the IncomePlan recovery owner. Unsupported formats remain readable with explicit local discard; the screen and recovery actions do not offer or execute a futile retry.

Month-bearing commands use protocol epoch `2026-09-06`, distinct from the previous monthless `2026-08-02` protocol. Authenticated old or headerless IncomePlan mutations receive `409 client_upgrade_required` before required-month validation, including income restoration from the shared recycle bin. Current Android also stops a mutation when negotiation returns a different protocol. Read access and unrelated recycle kinds retain their existing contracts. No compatibility bridge fabricates missing intent.

Protocol refusal is not completion. A compatible worker preflight can be followed by a different per-command negotiation result or a server upgrade rejection. These results, and unrecognized 409 errors, must preserve the original command in visible recovery with the same key, month, payload, OCC and identity. They must not become DONE, generic business conflicts or repeated blind network retries. The existing dispatcher error classification is the responsibility boundary; all affected registered consumers must adopt the corrected default. Known domain refusals such as `expense_reversal_required` and `items_sum_not_in_mismatch` also do not prove the original command was fulfilled. This is a directly demonstrated horizontal integrity gap within the current slice.

## Existing state without history

There are no production users, databases or production-history obligations. Existing Owner Internal Beta data and offline intent still cannot be silently discarded. A schema transition may preserve the current plan as an explicitly undated baseline; it cannot invent prior revisions from `created_at`, `updated_at` or the current value. Current/future estimates may use that declared baseline. A past query whose plan history is unavailable must say so, instead of returning a fabricated historical amount or an unexplained zero. New revision history is authoritative from its recorded effective months onward.

For an undated one-time baseline, changing only the label/source metadata or resubmitting unchanged financial values does not establish knowledge of its past target month. That query keeps its unknown-history marker through successive metadata edits before their declared intent months. An actual amount, pay-day or target-month change remains an explicit financial correction for the old/new target months. Already established dated one-time history retains its existing correction projection, and current management heads remain editable.

## Consumer and retirement map

| Responsibility | Real consumers | Retires |
|---|---|---|
| IncomePlan command and immutable revision | Existing API; Web management; Android create/edit/archive/restore | Overwrite-only expression of plan changes |
| Month-specific forecast | Income summaries, discretionary planning and Advisor inputs | Active-head aggregation used as historical or whole-month truth; Android's duplicate total formula |
| Original edit intent | Actual Android editor, Room/dispatcher and visible recovery | Direct-first edit publication and uncalled alternative save owner |
| Estimate meaning | Web/Android navigation, form, summary and status copy | Automatic received-income claims |

## Done checks and bounded evidence

1. A plan with a past pay day never renders a received-income claim without a FinancialFact. The current Web template supplies the initial RED.
2. Monthly August 100 → September 120 → October archive → November restore yields monthly estimates 100, 120, 0, 120; actual expenses remain unchanged. Creation does not populate earlier months. Stale OCC and original-key replay cannot append extra revisions.
3. One-time correction and both existing frequency conversions follow the declared month effects. Whole-month and scheduled-through-today totals agree across real consumers, including a future month and a short month.
4. The real Android edit entry persists the original September intent before network; an unknown result, failed GET and October replay retain September and publish once. A changed binding or an old monthless payload never dispatches under guessed meaning.
5. Real API/Web permissions, idempotency and forecast consumers pass the final candidate's PostgreSQL lanes; Android runs in current-head cloud qualification. Local work uses narrow source/template or pure-state checks. The Owner additionally authorizes short, targeted physical-device verification where needed; ten-minute-scale local tests remain excluded and broad PostgreSQL/Android/installer qualification stays in the cloud.
6. One bounded independent review, admitted targeted regression, final-candidate CI/CodeQL/actual Connected and independent merged-main qualification close this slice. Final Setup/APK, process interruption and the whole Internal Beta rehearsal remain the original Goal's later delivery gates.

Status: revision/query owner, month-bearing API/Web/recycle/Android consumers and durable edit recovery are implemented for candidate qualification. Candidate `4fcaaeab` passed CI, CodeQL, every PostgreSQL lane, Android JVM tests, APK builds and Windows packaging. The actual Income Room/UI continuity case passed; Connected qualification still failed at the unchanged Debt Save case. The subsequent protocol and recovery corrections below require their own exact-head cloud qualification. No runtime closure or full RC claim.

Bounded main review on `92071386` admitted the device-month defect, also raised as a formal P1. The actual Android editor could disagree with the server accounting month. The real screen now passes its confirmed forecast month into the edit session; create also waits for that confirmed month. Original open/submitted intent stays frozen. The original diff is outdated and the production fix candidate is independently verified; the formal review thread remains unresolved. The first cloud candidate exposed stale migration-head assertions, an undated monthly Advisor fixture and two incomplete Android test constructors; these are migrated without weakening their original assertions.

The first actual Room/UI run reached the original-intent retry and exposed a fixture error: its September-to-October jump was 31 days, beyond the existing seven-day retry cap. The cross-month case now crosses September 30 to October 1 within that cap; the production cap is unchanged. The Income recovery surface also respects the existing expired marker, keeps the original record readable and asks for reconciliation rather than offering a dead retry. Windows packaging must bind its release manifest to the new schema head; only that manifest value changes. One unchanged Debt consumer test also timed out after Save in the first emulator run; its cause is not established and #372 remains deferred.

The Owner corrected the test-maintenance approach during qualification: changing literal dates or copying the new migration version into old assertions is insufficient. Current-head integration expectations now derive from the actual migration graph or the exact generated program; historical C02/C07 targets retain their specific meaning. Income service and real HTTP revision tests now control the command/query clock, and the Advisor month-filter test uses the same fixed clock for creation and projection. Pure month and Android Room cases already carry their full clock explicitly and do not depend on the machine calendar. These corrections retire the repeated moving-head constants; they do not add an assertion against a value read back from the database under test.

Candidate `ed749f64` passed every PostgreSQL lane, Backend contracts, Desktop, APK builds, CodeQL and Windows packaging. Its real Room/UI income continuity test passed among 112 instrumented cases; the unchanged Debt Save case alone still timed out. That test now reports bounded submission preconditions on failure so a missing touch and a refused command can be distinguished. No Debt production layout or #372 work was applied. Android fast reached and passed its JVM tests, then rejected the newly lengthened sync-page composition; quarantine rendering is now a separate section with the same permission and action semantics. The next candidate still requires its own full gates.

Formal review of `4fcaaeab` admitted a second P1: the new required month had retained the old runtime protocol epoch. An independent review confirmed that a version bump alone would still let the old interceptor send an unversioned request. Real-router tests reproduced the old/headerless 422 path before the fix. Targeted regression review then found that the shared recycle gate must use the same whitespace normalization as its existing dispatcher; this also reproduced RED and was corrected. All 19 isolated protocol cases now pass with database connections prohibited. Current-client negotiation refusal and both global sync recovery corrections have targeted Android tests awaiting cloud execution. The two formal recovery P2s and both P1 threads remain open until supported formal closure; a local fix or an outdated diff is not a resolved review.

Candidate `f942609d` passed CodeQL, all PostgreSQL lanes and Android JVM tests. CI failed at the six-parameter FailedCard static limit; Connected failed at Debt Save and two Debt recovery cases. The latter fixture still supplied a loud proxy for the now-required Income recovery owner; it now supplies the real repository. The card now expresses retry availability through its nullable action. Debt Save keeps one real touch and additionally observes geometry in the actual injection scope; its root cause remains open.

The Owner identified a missing assertion in the protocol fix: stopping HTTP publication did not prove durable intent retention. Independent fixed-head review confirmed 16 registered dispatchers classify otherwise unrecognized 409 responses as Discarded, which the drain persists as DONE. Test-first candidate `f4580339` connects real negotiation, Retrofit, Income dispatch and drain with fake transport/DAO for local mismatch, server upgrade refusal and an unknown rejection; it also requires same-command recovery after compatibility returns. Cloud CI `34023740863`, job `101460952470`, ran 2,090 JVM tests; exactly these three failed with `expected failed, actual done` at the persistence assertion. The downloaded JUnit report confirms this failure, rather than a fixture or compilation error. Connected `34023740827` failed to compile the added Save observation because the touch `click` extension import was missing; the import is corrected and no touch evidence is claimed for that run.

The follow-up retires all 16 duplicated HTTP parsers/classifiers in favor of one structured-error classification using the existing NetworkErrorHandler. Unknown/domain/protocol refusals become visible Failure; only explicit state conflict and in-progress codes select their respective recovery paths. Refusal text cannot select a conflicting code through substring matching. Existing positive terminal and success handling remains separate. Recurring occurrence also preserves protocol/unknown refusal as Failure instead of advising the user to replace the original association as a business conflict. Added regressions cover these domain errors and original-command recovery. These source corrections require the next exact candidate's cloud qualification; no broad local Android/PG tests were run.

Candidate `e5c1c5a4` passed all three protocol-retention/recovery cases, both shared-error cases and all four occurrence cases in cloud JVM execution. Connected `34024505197`, job `101462982390`, passed all 113 tests, including both previously failing Debt recovery cases and the actual Save submission/freeze/local-acceptance path. Save uses one real touch on the clickable control and checks positive injection geometry; the earlier zero-bound trigger is still unproven. JVM qualification had one real regression among 2,093 tests: the shared classifier had dropped the `rule_category_deleted` marker used by the sync UI to explain restoration before retry. That known marker is restored, and both protocol-refusal markers now drive an explicit compatible-version recovery prompt in the actual sync screen. A real UI test checks that explanation and original-row retry. Main and targeted review had missed this consumer detail; the valid regression test is retained. The new follow-up still needs its own exact-head gates.

Candidate `e7ba9166` passed CI `34025215158` and CodeQL `34025215155`. Its Connected run passed 113 of 114 cases, including Room month continuity, unsupported-income recovery and protocol-refusal explanation with original-row retry; Save alone failed. All seven formal review threads, including the category-deleted recovery P2, were subsequently resolved and read back after targeted independent regression review. That formal closure does not close the failing integration gate.

Diagnostic candidate `e51ce797` located Save's first empty clipping ancestor: it was visible before capture, but the viewport later shrank while the same button remained below its edge. The test had relied on a visibility assertion made before this window update. Save now scrolls into the current viewport after capture, asserts display/enabled state and injects one real touch before checking submission, frozen fields and one local acceptance. The temporary ancestor tracing retires. This corrects the test's stale sequencing without changing the production layout; the specific system trigger, including the IME hypothesis, is unproven. That diagnostic run also had one LedgerHeader missing-Compose-hierarchy error; it is retained as an observed failure, not silently classified as a production defect. The corrected candidate must pass its own gates.

Candidate `ce192c10` completed all 114 Connected cases in run `34027335166`, actual job `101470554385`, and passed CodeQL. Successful Connected runs upload previews rather than JUnit XML, so individual-method XML proof is unavailable. Before integration, formal review found two read-compatibility gaps: the legacy aggregate had changed from scheduled-to-date to whole-month expected income, and current Android decoded required forecast fields from an older server without read negotiation. The frozen-date route regression reproduced the first failure locally without database access (expected 0, actual 10000); real Retrofit and shared-error tests now require explicit version guidance before decoding old income responses and preserve current read-only access. Android execution of these new counterexamples is pending in the cloud. These findings remain unresolved; no merge or full Goal completion is claimed.

Test-first `344ae656` ran 2,096 JVM cases in cloud CI `34028226150`, job `101472941987`; exactly the two new read cases failed. JUnit confirms the older-server failure lost `runtime_version_mismatch` (actual null), and the current read bypassed negotiation. The correction preserves the legacy scheduled aggregate and adds the explicit `expected_amount_cents` field, consumed by the real Android forecast summary; its unused duplicate total state retires. Only income-list GET joins existing epoch negotiation, and matching-epoch reads remain available without write permission or currency activation. The schema snapshot was regenerated by the actual generator. All five fixed-date forecast/route cases now pass locally without database access. Final Android and full-candidate qualification remain pending.

Formal review of `ef54e1a5` found that archive/restore returned success for a delayed September command after another client had established the same status for October. Independent source review confirmed both the initial-status and failed-OCC exits skipped proof of the original month. Real owner tests with database connections forbidden reproduced false success at all four cross-month exits and with missing or mismatched head history: 12 failed and four same-month cases passed before the fix, then all 16 passed. The shared check now reads the actual latest immutable revision and requires head/status/month agreement. Added fixed-clock PostgreSQL cases exercise both actions, both read phases, same-month no-op and cross-month refusal with unchanged history and forecast; the original edit receipt case now replays September after an October edit. These PostgreSQL additions and the final candidate's full gates await cloud execution. The formal finding is not resolved by this local evidence.

## Impact baseline rebuilt under the new closure discipline

This is the September 6 baseline reconstruction requested after the Owner added the impact-closure discipline, not a claim that earlier reviews had already covered every consumer. Source comparisons use qualified main `5d4606267dcdccd5fb514cccdaec559b755e8eb1`, the old same-status candidate `ef54e1a5ca0dd51a4046e78d55ebf54b883507dc`, and `fed1a02fcb8d40670f84aba0ef1872e4e08962b9` (tree `b89e56d0bd291a37a2d320aed0758f995663f3de`). References below are repository paths at `fed1`; production was read through Git objects. A shared implementation or unchanged source establishes impact routing, not execution qualification.

| Boundary | Entry, owner and real consumer | Previous exit and current obligation | Direct verification producer |
|---|---|---|---|
| Status commands | `backend/app/routes/income_plans.py:122/141`; Web `web_income_plans.py:204/239`; `recycle_bin_service.py:103` all call `income_plan_service/__init__.py:215/261`. Android edit archive (`IncomePlanEditViewModel.kt:262`) and list restore (`IncomePlanViewModel.kt:357`) use `IncomePlanRepository.kt:124/132` with original M/OCC; Web forms and recycle items supply their rendered M. | The four `ef54` exits at owner lines 227/246/271/290 returned by status alone. `fed1` lines 230/249/276/295 all call `_history.py:48`, which selects the actual latest tenant/plan revision and requires head/status/M agreement. Keyless same-month no-op remains distinct from an edit receipt. | `test_income_plan_status_replay.py`; `test_income_plan_revision_delivery.py:110`; existing `test_income_plan_service.py:259` and `test_income_plan_optimistic_concurrency.py:245` create real revisions for same-month replay. The old `test_currency_binding_marker.py:213` fixture was an admitted missed consumer, corrected below. |
| Create/edit and stable delivery | API create/PATCH at `income_plans.py:87/108`; Web creates at `web_income_plans.py:159`. Android `PlanRoute.kt:34` and `StatsRoutes.kt:129/139` use the real Income ViewModels; editor submission `IncomePlanEditViewModel.kt:238` → `IncomePlanRepository.kt:105` → Room intent → `UpdateIncomePlanDispatcher.kt:17` → `_delivery.py:17` → existing command owner. | Direct-first Android editing and the unused alternative save path retire. Create/archive/restore remain their existing online commands; only edit uses this durable keyed delivery. The claim HIT at `_delivery.py:38` returns the saved response before later OCC/month checks; failures publish no revision/receipt. `_delivery.py` is byte-identical between `ef54` and `fed1`. | `test_goals_income_plan_idempotency.py`, `test_income_plan_optimistic_concurrency.py`, `test_income_plan_revision_delivery.py:16`; `IncomePlanIntentContinuityTest.kt:24/74`, `IncomePlanRoomContinuityTest.kt:36`, editor guard tests. Old direct-2xx/no-enqueue and IOException-only fallback assertions describe the retired publication model and must not be restored. |
| Physical history and ownership | `models/income_plan_revision.py:24`, parent tenant key in `financial_planning.py:49`, migration `20260906_0002_income_plan_revisions.py`; `_dataset_restore_security.py:76` preserves the new table. | Typed revisions have tenant/plan FK, revision uniqueness, field constraints and update/delete rejection. Migration preserves existing heads as undated baselines; it does not invent old monthly facts. Release manifest follows the schema edge. No Windows restore or lifecycle qualification is opened. | Existing migration-head/authority/restore-security tests and PostgreSQL lanes; revision delivery and month-semantics tests exercise actual persisted history. Physical database execution cannot be inferred from the local pure status test. |
| Forecast readers | `income_plans.py:74`, `web_income_plans.py:119` → `income_forecast`; budget API `budget_advisor.py:62`, Web `web_budget_advise.py:110`, Advisor `_inputs_builder.py:183`, radar `insight_radar_service/__init__.py:140` all pass an explicit month to the same revision query. Android Plan/Income summaries consume response forecast fields. | Current-head aggregation and Android's duplicate total retire. The legacy response total still means scheduled-to-date; the new expected field supplies whole-month planning. These readers are semantically affected by the PR even where their source did not change; none bypasses the revision owner. | `test_income_forecast.py`, `test_income_plan_month_semantics.py`, `test_budget_advisor_aliases_and_guard.py`, `test_budget_advisor_openai_compat.py`, `test_insight_radar.py`, `IncomePlanMappersTest`, Income ViewModel tests, `ApiClientIncomeReadCompatibilityTest`. Their final-candidate execution remains a gate. |
| Protocol and authorization | `auth.py:171` first depends on existing writer authorization; Income writes and income-only shared recycle restore then require the current epoch. `ApiClientBuilders.kt:143` adds Income GET negotiation to existing write negotiation; matching-epoch reads branch before the write-capability check. | Old/headerless Income writes cannot reach required-month parsing under guessed intent. N−1 Income reads receive upgrade guidance before new DTO decoding. Unrelated reads and non-income recycle kinds retain their existing routes; the global write interceptor is an affected horizontal consumer, not an unaffected file. | `test_income_protocol_boundary.py` includes all four Income writes plus shared recycle normalization; `ApiClientIncomeReadCompatibilityTest` checks old-server refusal and current read-only access; existing auth/runtime negotiation tests remain required. |
| HTTP refusal → persistent state | The 16 registered adapters listed below call `OutboxHttpError.kt:14`; `AppContainer.kt:173` is the actual registry. `RecurringOccurrenceDispatcher` separately uses the same structured error meaning. Drain `OutboxDrainEngine.kt:336/340` persists Failure as FAILED and Discarded as DONE. | Unknown/protocol/domain 409 no longer settles an unfulfilled command. Explicit `state_conflict` remains CONFLICT; in-progress remains retryable. The existing `rule_category_deleted` and protocol markers remain observable. Other registered create/debt/offset/recurring adapters already default unknown 409 to Failure; their owners were not rewritten. | `OutboxProtocolRefusalTest` traverses real negotiation/Retrofit/Income dispatcher/drain and original-row retry for three refusal codes; `OutboxHttpErrorTest` covers domain/unknown codes and misleading message text. Existing individual adapter tests and occurrence tests consume the same classifier; this is not a claim of 16 separate end-to-end executions. |
| Durable recovery and two global entrances | `RepositoryGraph.kt:112` and `OutboxAdapterGraph.kt:133` share the typed adapter. Local `IncomePlanPendingEdits`/ViewModel recovery; All Debts → `MainProductNavRoutes.kt:235`; Settings → `SettingsDestinationHost.kt:374` both require Income via `ViewModelFactories.kt:351`. | Typed format 1 retains label/amount/M and origin metadata. The exact binding guard, owner/ledger filter and Room row retain key/payload/OCC. Unknown/monthless FAILED or CONFLICT rows stay readable with explicit discard; UI and direct recovery actions reject futile retry. A supported FAILED retry uses `FailedResolution.Retry()` without a fresh token, preserving the original key/OCC; Income conflict exposes reconciliation/discard, not generic overwrite. | `IncomePlanIntentContinuityTest.kt:51/74`; Room continuity; `SyncStatusScreenConfirmTest`; Income editor/ViewModel tests and migrated constructors in Debt/DataQuality connected harnesses. `OutboxRepository`, drain and `PendingMutationDao` are byte-identical main→`fed1`; DAO retry is owner/ledger/status-scoped and leaves payload/key/OCC unchanged. Existing expiry/quarantine/cleanup behavior remains its owner. |
| Qualification producers | `.github/workflows/ci.yml:676/751` owns ordinary/real-db PostgreSQL execution; Android fast owns JVM execution; `android-connected-test.yml:145` runs actual Connected tests. The slice also affects schema/OpenAPI/manifest consumers and their existing checks. | Per-method source assertions, aggregate lane results and checkout/source SHA evidence are separate. No prior SHA's green result qualifies the fixture correction or a later candidate. | The controller reported all three `fed1` real-db shards and Connected successful, but ordinary job `101482560401` failed the missed currency no-op fixture and Android fast `101482560414` failed before a JoinFamilyLedger test. This reviewer did not independently download those runs. New-head full gates remain open. |

The shared HTTP-classifier consumers are AcknowledgeItemsMismatch, ConfirmExpense, CorrectExpense, DeleteCategoryRule, DeleteMerchantAlias, MarkNotDuplicate, PatchExpense, RecognizeText, RejectExpense, ReplaceItems, ReplaceSplits, RetryOcr, UpdateCategoryRule, UpdateGoal, UpdateIncomePlan and UpdateMerchantAlias dispatchers. Their 16 duplicated classifiers retire. Existing positive 2xx handling remains separate. Existing missing-target/404 Discarded exits and offset's explicit `expense_offset_not_active` exit were identified, not used as proof that arbitrary refusals are safe; this bounded 409/protocol correction does not qualify every pre-existing terminal domain case.

The admitted missed currency test now supplies a typed same-month `IncomePlanRevision` representing each current head, with fixed January intent and February clock. It still fails immediately if any terminal action enters the currency writer gate and asserts no add/commit. The unchanged old fixture reproduced `object has no scalar` with `--noconftest` (one failure, 1.18 seconds); the corrected fixture passed (one case, 1.04 seconds), with Ruff and diff checks passing. This is local pure evidence with a mock Session, not a PostgreSQL execution. Only this test and this existing contract changed during the impact reconstruction.

Open evidence: `ef54`→`fed1` changes only the status owner/helper, its two tests and this contract; Android source is byte-identical. The reported Android failure establishes an uncaught IO exception crossing into a test without a set/reset Main fixture; its originating producer is not established, so no unrelated Android change is justified from that observation. The controller is classifying it. A later exact source/checkout must pass all gates; changed-source equivalence, the local fixture pass and reported successful shards do not close that obligation.

The direct producer scope was then measured at `fed1`, one path per actual `ci_gap_trigger_scope.classify_ci_paths` call: 122 changed app/migration/test paths including the currency fixture correction, plus three unchanged direct Android owners. The existing classifier ran in two short calls (0.57/0.58 seconds); no build, database, emulator or new scope framework was used. Connected calls the same `ci_scope.py` at workflow line 61 and uses its Android output at line 70; Android fast uses that output at `ci.yml:916`. This is individual-path evidence, not inference from the aggregate PR diff.

| Direct producer group | Paths individually measured | Selected scopes | Consumer conclusion |
|---|---|---|---|
| Income command/query and API schema | Four `income_plan_service` modules; `auth.py`; Income/recycle API routes and schemas; `recycle_bin_service.py`; income revision migration (11 paths) | PostgreSQL + frozen backend; Android/Connected false | The existing backend execution owners are selected. These Python files are not executed by the current synthetic-remote Income Connected fixture. |
| Models and runtime contract | `models/__init__.py`, `financial_planning.py`, `income_plan_revision.py`, `runtime_compatibility_contract.py`, `_dataset_restore_security.py` (5) | PostgreSQL + frozen backend + Windows; Android/Connected false | Existing exact dependency rules preserve their backend/Windows consumers; no Android execution claim follows. |
| Android production graph and test fixtures | All 75 changed Android paths, including `RepositoryGraph`, `OutboxAdapterGraph`, API/DTO/dispatcher/ViewModel/recovery code, JVM fixtures and all six changed Connected files; separately `AppContainer`, `MainScreenFactory`, `PendingMutationDao` (78 total, including two retired test paths) | Android true; both fast and actual Connected selected | Each real graph or fixture path independently selects the current Android jobs. Room fixture lines 31/56/69 use real Room/graph with synthetic remote transport, so backend-only paths need not select this Connected case. |
| Backend tests and fixtures | All 25 changed/current test paths, including `_runtime_protocol.py`, `_infra/budget_advise_fixtures.py`, status/history/protocol/recycle tests and `test_currency_binding_marker.py` | PostgreSQL; Android/Connected false | Direct backend verification remains selected even for a test-only change. |
| Shared Web consumers | Income/recycle Web routes and four changed Web templates (6) | PostgreSQL + frozen backend + Desktop; Windows false | The native Windows consumer bit is the admitted shared-Web scope omission already assigned to the Desktop slice. Its existing `_SHARED_WEB_DESKTOP_PREFIXES` owner receives one correction there; no duplicate Income scope rule is introduced. |

No additional necessary scope change was established in this bounded producer set. The shared-Web Windows correction and the final candidate's actual gates remain separate obligations; classification is not test execution.

## Review follow-up: one displayed month and accurate protocol parameters

Before changing production at `3aebc234`, two direct counterexamples ran against the actual owners: the recycle row displayed September but serialized October, and the generated Income POST contract exposed two optional parameters for the same protocol header (2 RED / 19 PASS, 0.54 seconds). The first uses two actual recycle row/DTO projections with an advancing accounting clock; the second resolves real generated parameter references. The existing runtime cases still prove headerless/old-version rejection precedes month validation for all four Income commands and both normalized recycle income kinds.

| Impact scope | Before-change evidence and required closure |
|---|---|
| Recycle list entries and restore consumers | Web and API both consume `_archived_income_rows`; API DTO carries `detail` and `restore_intent_month`, Web uses the same item in its text and hidden form, and Android displays/sends that response. Capture once before building this listing so every displayed and submitted month agrees. Restore owners, OCC and immutable revisions remain authoritative; no request-time month substitution is permitted. |
| Protocol owner and old success/error exits | Only the four Income writes depend on `get_current_protocol_writer_context`; its runtime optional declaration intentionally produces the custom 409 before body validation. Keep that behavior while declaring its contractual requirement in the generated schema. Shared recycle restore checks the epoch only for income kinds, so its optional parameter must describe the condition rather than require it for unrelated kinds. |
| OpenAPI consumers and direct producers | Existing `main._custom_openapi` already owns runtime header/error corrections and adds shared refs. Its ref-only deduplication misses an inline parameter with the same name; generated clients would see contradictory duplicate optional entries. Keep one parameter per identity, preserve unrelated runtime/currency and idempotency headers, regenerate the checked-in schema using the actual generator, and run existing protocol/schema checks. No Android DTO/body or persisted payload changes are needed: this follow-up changes parameter metadata and one projection snapshot, not the wire fields or epoch. |

These are narrowly admitted review corrections. The separate legacy forecast-history finding is being investigated against the same candidate; no final qualification or merge is claimed from the earlier green head.

Root's post-change closure: the existing OpenAPI owner now consumes the required-header metadata and preserves a single inline parameter rather than appending a conflicting shared ref. The protocol-header operation is a named same-file part of that owner; the exact `3aebc234` weight artifact `9989457391` recorded the old `_custom_openapi` at CCN 15, so the additional decision is not piled into that already complex function. Actual regeneration changed only four Income writes and conditional recycle restore; all schema components and request/response bodies are identical, all 354 operations have unique parameter identities, and no private marker escapes. A separate agent independently confirmed these facts and the Web/API/Android recycle consumers with no new actionable defect. The combined runtime/schema/forecast/status suite passed 48 cases in 1.38 seconds, with only two existing dependency deprecation warnings; Ruff passed. No PostgreSQL or Android runtime result is inferred from this local proof.

Each changed backend production path selects PostgreSQL/frozen-backend individually, with `main.py` also selecting Windows; each changed backend test selects PostgreSQL. The snapshot's Android producer omission was found in this measurement and is corrected below. The final new head still requires its own PG/JVM/Connected/Windows and weight gates. The earlier same-status/month P1 `PRRT_kwDOS5LrfM6frQn9` was formally resolved and independently read back at green `3aebc234`; the three new review threads remain open pending this candidate's qualification.

## Review follow-up: undated one-time history survives metadata edits

Formal P1 `PRRT_kwDOS5LrfM6fruMX` is confirmed on `3aebc23418bddf0c5631fee7926459e9c5d38a84`. An August undated single-month baseline followed by a September label-only PATCH was treated as known August income. The real `_forecast.forecast_from_revisions` counterexample produced `DID NOT RAISE AppError` (1 RED / 8 PASS, 0.27 seconds). This is a forecast-owner execution, not a PostgreSQL or full HTTP result.

| Impact scope | Before → correction and direct verification |
|---|---|
| Actual edit publication and historical success exit | `IncomePlanUpdateRequest` accepts partial metadata; `_delivery.update_income_plan_idempotently` forwards omitted values as absent, and `update_income_plan` retains their current values before appending a full revision. `_forecast._applicable_revisions` previously let every one-time edit replace its past target, including an undated baseline. Only that unconditional known-history exit retires: before the edit's intent month, a previously unknown projection now requires a changed amount, pay day or target month to replace the marker. The check uses snapshot values because immutable revisions have no submitted-field mask; mere presence of an unchanged Android field cannot prove new financial knowledge. |
| Query consumers and current projections | API `income_plans.list_plans` → Android `IncomePlanRepository.listActive`/`IncomePlanViewModel.refresh` and Web `web_income_plans`; budget discretionary API/Web → `total_monthly_income_cents`; Advisor `_inputs_builder` → `list_applicable_income_plans`; radar cashflow → the same total owner all converge on `query_income_forecast`. Month-specific historical calls now retain the existing unavailable result for this case. Android's management fetch supplies no historical month and remains a current-month consumer. Current management rows still come from `list_income_plans`; dated history and actual financial corrections retain their original projection. The pure suite covers consecutive label/source edits, each financial field independently, a later rename, old/new moved targets and existing monthly conversion history. No separate consumer total formula is introduced. |
| Persistence, permissions and original intent | Current head/OCC claim, immutable revision insertion, baseline rows, idempotency receipts, runtime epoch, currency/tenant authorization and Android Room payload/recovery are unchanged. The correction selects an existing revision; it neither rewrites history nor adds a migration/marker column. `test_legacy_single_month_label_patch_preserves_unknown_history_until_financial_correction` seeds the exact typed undated-baseline shape with the normal currency capability, then exercises real partial PATCH, historical GET, current management GET, explicit amount correction, original-key replay and the preserved baseline. |
| Direct verification producers | `test_income_forecast.py` now passes all 10 pure cases in 0.12 seconds after the minimal `_forecast.py` correction. The new real-db case in `test_income_plan_revision_delivery.py` collected successfully but awaits exact-candidate cloud PostgreSQL execution. Actual per-path classification selects PostgreSQL + frozen backend for `_forecast.py` and PostgreSQL for both test files; no scope policy changed. These paths are not executed by the synthetic-remote Android Connected fixture. Ruff and diff checks passed. No PostgreSQL, Gradle, emulator or product backend ran locally. |

The local pure entry used the existing safe `check_api_contract._load_app_openapi()` import stub and pytest `--noconftest`; collection of the real-db case deliberately did not run fixtures. These bounded results do not qualify a later head or resolve the formal thread by themselves. Root owns the separate protocol/recycle correction and final candidate integration.

## Direct producer follow-up: the OpenAPI snapshot selects Android

The preceding producer inventory omitted `docs/architecture/openapi_contract.json`. At exact base `3aebc23418bddf0c5631fee7926459e9c5d38a84`, this path is not in `_ALWAYS_ON_CONTRACT_PATHS`; the ordinary `docs/` prefix maps it to no heavy scope. An actual single-path classifier test reproduced `android=False` where `True` is required (1 RED in 0.47 seconds). This is a demonstrated verification-selection failure, not a claimed DTO mismatch in the current parameter-only schema edit. Android `OpenApiContractGateTest.kt:20-29,359-369` directly reads this file's schemas and drives real Moshi DTO name/type and round-trip checks, so an independently regenerated snapshot body change must also select its JVM consumer.

| Impact surface recorded before the scope edit | Before → corrected responsibility and direct proof |
|---|---|
| Snapshot entrance and former successful gate exit | A change containing only the snapshot selected no Android execution; the always-on backend API contract job could check generator/snapshot consistency without running the DTO consumer. The existing exact-path map now gives this one file `("android",)`, before the unchanged general docs rule. It does not depend on another Android file happening to change in the same PR. |
| Direct verification consumers and producer wiring | The unchanged Android test reads the checked-in snapshot at runtime and fails if it cannot locate it. The existing Android scope selects the fast/JVM gate and its associated Android lanes, including Connected and CodeQL; no new lane or trigger framework is introduced. The backend-contract job at `ci.yml:77-88` remains independent of heavy scope selection and still runs `check_api_contract.py:184`. |
| Existing scope assertions and unaffected paths | The existing Android-source scope test now checks source and snapshot separately through the same expected-scope helper. All five scope assertions remain for each input. There was no old snapshot-specific assertion to exempt or retire at this base. The existing docs-only and always-on contract tests remain; direct calls confirm `docs/runbook/CI.md` and `README.md` still select no heavy scopes, while the snapshot selects only Android. |
| Product, persisted intent and recovery boundaries | No API body/schema, runtime epoch, DTO, Room payload, permission, OCC, forecast/recycle owner or recovery exit changes in this follow-up. Root's protocol/recycle edits and the other agent's forecast edits remain intact. The old incomplete producer inventory is supplemented here rather than claimed complete retrospectively. |

After the one-line production rule, all 19 existing scope tests pass in 0.71 seconds using `--noconftest -p no:cacheprovider`; Ruff and the scoped diff check pass. Pytest's exit cleanup reported one permission warning for an older temporary-directory tree; no cleanup action was added and no test failed or skipped. These are pure classifier/workflow tests, not Gradle, PostgreSQL or Android execution. This contributor changes only `ci_gap_trigger_scope.py`, `test_ci_scope.py` and this contract addition, with no commit or push. The integrated exact head still requires its own cloud qualification.

## Current adoption refusal: test-first impact closure

Formal P2 `PRRT_kwDOS5LrfM6fsA0r` is admitted against `0cc43f59c9bbf3b3f966f77feda9af479bd376a3`. The current handshake can truthfully report the current API epoch, `owner_action_required` and no currency request binding. `RuntimeNegotiationInterceptor` then forwards an unversioned Income command because `canWrite` is false. The new Income protocol guard returns `client_upgrade_required` before the actual currency owner can return `currency_adoption_required`. The result is a refusal rather than a financial commit, but it directs the household user to the wrong recovery action.

This inventory is recorded before production changes under the Owner's impact-closure discipline. The current contribution changes only direct JVM tests, the bounded backend protocol probe and this section; it does not claim that the defect is fixed or that a new candidate is qualified.

| Entrance / consumer / former exit | Required behavior, owner and direct proof |
|---|---|
| Income create, edit, archive and restore; Income recovery through the shared recycle endpoint | `IncomePlanRepository` calls the four typed `IncomePlanApi` writes (edit through its dispatcher). `LedgerRepository.restoreRecycleBinItem` supplies the retained recycle kind, month and OCC to `RecycleBinApi`. The new `ApiClientIncomeAdoptionTest` executes all four real Retrofit methods plus normal and whitespace-normalized `income_plan` recycle kinds against the existing auth/negotiation client. A current adoption handshake must retain its true API version on the single outgoing request, without inventing currency evidence. The HTTP fixture returns the protocol rejection when that header is missing; with the confirmed current version, it returns the required backend owner refusal. A separate real currency-owner probe below tests that required precedence rather than assuming it is already implemented. |
| Direct success and error consumers | Income create clears its draft and publishes its success flash only on `Result.success`; failure retains the draft with `toUiText`. Archive/restore use `handleSimpleResult`; recycle removes the item and increments its change revision only on success. The existing `NetworkErrorHandler` and `ErrorUiText` already map `currency_adoption_required` to the instruction to ask the installation owner to confirm the home currency on Desktop, then retry. This work must retire the false upgrade guidance without creating a new success exit, changing role checks or changing that owner action. |
| Persisted edit, worker preflight and the per-command race | `AppContainer.outboxWriteCompatibility` already projects `owner_action_required` to the currency adoption block; `OutboxDrainWorker.runCompatibleDrain` returns RETRY without dispatch when `canWrite=false`. Existing `OutboxDrainWorkerTest` verifies this no-dispatch path. The extended real-client/Retrofit/`UpdateIncomePlanDispatcher`/`OutboxDrainEngine` test covers a compatible worker preflight followed by an adoption handshake at the write: the row stays Failed, has no completion time or commit notification, retains identical key, original month, payload, OCC and binding, and remains active for its target. After owner resolution, explicit Retry must send the original command and reach Done once. No Room schema, retry cap, Drop policy or new state owner changes are proposed. |
| Shared `canWrite=false` consumers that must not acquire a new global gate | The same client also serves expense creation/upload/notification drafts, detail/items/splits/state/corrections/media, bill splits, budgets/advice, debts/repayments/proposals/drafts, goals, recurring items/occurrences, categories/rules, merchants/aliases, tags, ledger/membership/invitation/device management, dashboard settings and background-task cancellation. Their actual backend owners decide whether currency readiness is relevant. A blanket adoption refusal would newly block the real ledger-switch route, whose credential/membership transaction does not require currency proof. The new test exercises that typed switch successfully and preserves an actual non-Income recycle permission refusal; no path or body scan is introduced by these tests. All remaining production request/dispatcher behavior is unchanged at this test-first checkpoint, not newly runtime-qualified by this inventory. |
| Reads, authentication and other compatibility conclusions | Current Income GET remains readable with both `read_only` and `owner_action_required`, each without currency binding, using the existing complete expected/scheduled/month assertions. Other GETs, unauthenticated requests and `/api/auth/` keep their existing negotiation exclusions. An old API epoch during adoption still produces `runtime_version_mismatch` before dispatch. Existing session-header tests retain successful negotiated JPY proof, missing-handshake no-send and currency-binding revision race checks. Other `canWrite=false` conclusions must not be silently reclassified as adoption. |
| Evidence boundary and implementation choice | Merely forwarding the API header cannot fix the unchanged backend: `_currency_write_expectation` rejects an absent currency binding before `resolve_write_capability` reaches `ADOPTION_REQUIRED`. The handshake deliberately has no binding to copy, so a guessed/default binding is forbidden. The bounded candidate is to preserve the real negotiated API version in the shared Android owner and let the existing backend currency owner return its adoption refusal for that known-current, proofless adoption request. ACTIVE/EMPTY with missing proof, an old epoch, malformed proof and explicit expected-proof validation remain closed; internal and legacy branches keep their existing semantics. This avoids a new Android Income/recycle classifier, a global ban on independent writes and rewriting received backend errors. No production implementation is included in this test-first checkpoint. |
| Direct verification producers | The new JVM test and extensions to `ApiClientIncomeReadCompatibilityTest` and `OutboxProtocolRefusalTest` select the existing Android fast/JVM producer; Android source changes in the eventual fix also require the existing APK/Connected/CodeQL gates at that exact candidate. The backend probe in `test_income_protocol_boundary.py` directly runs `resolve_write_capability` with controlled binding reads and no database connection. Actual result: 1 RED / 8 PASS / 0.34 seconds; the sole failure is current API + ADOPTION_REQUIRED + no binding producing `client_upgrade_required` instead of `currency_adoption_required`. The other cases cover ACTIVE, EMPTY, old epoch, legacy, internal origin, explicit expected proof, malformed proof and supplied proof; refusal cannot claim the first binding, grant writer proof, add or commit data. Two existing import/dependency warnings were reported. No build configuration, OpenAPI snapshot or backend source changes here. JVM tests have not been compiled or executed locally: Gradle and emulator execution remain prohibited, so root must obtain the actual test-first cloud failure before production work. Previous `0cc43f59` green results do not cover these additions. |

The short backend invocation used the existing safe OpenAPI import setup, pytest `--noconftest -p no:cacheprovider` and the isolated qualification temporary root. It did not start PostgreSQL or execute its fixtures. The existing real PostgreSQL runtime-compatibility cases remain the direct producer for negotiated EMPTY first-claim, legacy JPY rejection and changed initialization offers; their successful state transitions are not claimed by the pure refusal probe. Existing route dependencies still authenticate/authorize writers before the unchanged currency service call chain.

## Adoption refusal implementation: confirmed RED and bounded owner change

Production work starts from clean `bc6a03d0d248ec623e163cdecae0ee3346bdfdcb`. Its CI `34037693700` / Android fast `101498743671`, JUnit artifact `9990805336`, actually executed 2,100 tests with exactly two failures: the real Income client and Outbox adoption cases received `client_upgrade_required` instead of the required owner refusal. The other three parameterized Outbox refusals passed. The earlier `f35b8e5c` compiler failure was only a nullable-list expected-type error; root corrected that type without changing assertions before obtaining this runtime RED.

Before this production edit, the drift case is added to the same direct JVM consumers: `canWrite=false` can still carry a real request binding when the persisted currency and configuration disagree. Both ordinary Income methods and the actual Outbox dispatcher must transmit that exact binding and preserve the backend's configuration refusal. The pure currency-owner table now has ten cases; rerunning it before production gives 1 RED / 9 PASS in 0.33 seconds, with the sole failure still the proofless current-epoch adoption case. The proposed change retires the shared client's `canWrite=false` headerless forwarding exit, not backend write authorization, and supplements the existing currency owner's missing-header refusal precedence. The previously recorded consumer/permission/persistence map remains the construction boundary; post-change proof is recorded below after validation.

| Boundary after implementation | Actual change or preserved owner | Direct producer and evidence limit |
|---|---|---|
| Common transport and every authenticated mutation family listed above | `RuntimeNegotiationInterceptor` now forwards the confirmed current API version even when `canWrite=false`. It copies only the actual handshake binding, including the real binding accompanying configuration drift; an absent binding stays absent. The old headerless forwarding exit retires. Existing auth/ledger headers, payloads and idempotency keys are retained. No Income/recycle path classifier, synthetic currency proof, client-side owner refusal or response-error rewrite is introduced. | `ApiClientIncomeAdoptionTest` executes six real typed Income/recycle commands for adoption and drift, asserting one request, exact negotiated evidence, original auth/ledger and the existing owner guidance. Its independent ledger-switch success and non-Income recycle permission refusal remain. Shared transport routes the other listed mutation families through their existing server owners; this inventory does not claim separate end-to-end execution for every family. |
| Currency refusal, permission and existing success exits | The new same-file `_required_http_currency_binding` helper rejects a wrong epoch first, passes supplied proof to the existing parser, and changes only current-epoch/no-binding/actual-ADOPTION_REQUIRED to `currency_adoption_required`. Explicit expected-proof, malformed proof, ACTIVE/EMPTY without proof, legacy and internal branches retain their previous boundary. Writer authentication still precedes the Income protocol dependency. `resolve_write_capability`, first-claim locks, writer proof, currency metadata and all other existing backend definitions are AST-identical to `bc6`. Income edit receipt HIT still precedes its original OCC/currency owner, while new commands can succeed only through that owner. | The real currency-owner table covers ten refusals without claiming, granting proof, adding or committing data. All 31 cases in `test_income_protocol_boundary.py` pass in 0.40 seconds after the fix. Existing PostgreSQL runtime/marker cases remain the direct producer for successful EMPTY first-claim, legacy JPY rejection, binding revision races and internal writes; no PostgreSQL execution is claimed locally. |
| Original edit and visible recovery | Existing worker preflight still defers every blocked compatibility without draining. If compatibility changes between preflight and the actual write, the same classifier/drain persists adoption or drift as Failed, keeps the complete original row and target active, and publishes no success notification. After owner resolution, explicit Retry retains key, body, original month, OCC and binding and reaches Done once. Create drafts, archive/restore feedback and recycle rows still change only on their existing success exits. Existing Chinese adoption guidance asks for Desktop owner confirmation; drift preserves the actual server guidance. | `OutboxProtocolRefusalTest` adds drift to the four existing cases without dropping their original-row, completion, publication or retry assertions. It now checks the real JPY request binding even with `canWrite=false`. These new JVM assertions await exact-candidate cloud execution; `bc6` is the test-first RED, not post-change evidence. No Room, retry cap, Drop policy, recovery UI or persistence schema changes. |
| Reads and non-Android callers | Matching-epoch Income GET still bypasses write capability and requires no binding; old epochs still stop before DTO decoding. Other GET/auth exclusions are unchanged. `database/_core.py` continues to mark Web/Owner/upload-link requests as server runtime, and direct internal sessions still have no HTTP marker; Desktop-proxied Web forms therefore keep the unchanged internal branch. Legacy headerless API requests also keep their branch. Desktop pairing/adoption owners and Windows lifecycle are outside this modification. | Existing `ApiClientIncomeReadCompatibilityTest` covers both current read-only and owner-action reads, and the old protocol test still asserts no mutation. The backend pure table checks legacy/internal adoption refusal; the untouched real PostgreSQL producers own their successful write transitions. These are explicit unaffected-source/branch observations, not new physical-device or Desktop execution claims. |
| Schema, scope and qualification | No request/response DTO, public schema, migration or protocol version changes. Actual in-memory OpenAPI generation still matches the committed snapshot. Per-file classifier execution selects Android for all three changed Kotlin files, PostgreSQL plus backend-frozen for the currency owner, PostgreSQL for its direct test, and no heavy lane for this contract. This isolated delta does not select Desktop/Windows; the unchanged internal/legacy callers above are not covered by an invented Windows result. Existing Backend contracts remain always on, and Android source/test scope also reaches the actual Connected workflow. | Actual local results: 31 pure protocol cases PASS, OpenAPI comparison PASS, targeted Ruff PASS and `git diff --check` PASS. Source inspection counts 494 physical lines in the currency owner, a nine-line new helper and an unchanged 69-line `resolve_write_capability`; cloud weight/Detekt results must qualify the final head. No local Gradle, emulator, PostgreSQL, whole-repository weight scan, commit or push was performed. Root owns the next exact-head CI/CodeQL/Connected evidence and integration. |


## Active plan wording: before copy correction

This bounded copy follow-up starts from clean `ce047e2cfd4e43bcb039a3dbf07573075dbe5c8c`. The controller found the still-valid non-inline P2 in the `0cc43f59` review body: active consumers describe income plans as recorded income or arrival. The separate adoption formal thread has been resolved/read back by the controller; that does not close this wording finding. Its reported earlier cloud results are not qualification for this new copy delta.

| Impact surface before this edit | Actual consumers, old exit and bounded correction |
|---|---|
| Android entries, management, summary and list states | Plan navigation and Stats entry converge on `IncomePlanScreen`; the Plan overview already says income plan. The active screen consumes `strings_misc_screens.xml` for title, active section, forecast plan count, empty/loading/error states and each row's date. Replace record/arrival implications with plan/expected wording. Preserve forecast values, `expected_amount_cents`, scheduled-to-date caption, month/date format arguments and all list filters. |
| Create/edit/archive/restore and shared form | `IncomePlanScreen`, `IncomePlanEditorSheet` and `IncomePlanDraftForm` consume add/edit titles, amount labels and validation. `IncomePlanViewModel` emits the existing create/restore success resource IDs; the editor emits existing local-acceptance and archive IDs. Say a plan was added/archived/restored, without moving any success exit or converting local edit acceptance into confirmed income. Existing expected-month, expected-frequency/date, intent-month and correction hints remain. |
| Recovery and derived planning consumers | `SyncStatusScreen` maps UpdateIncomePlan to the old `修改收入记录` label; the same shared screen serves both sync entrances. `BudgetAdviceScreen` describes the plan input's date as an arrival date. Change only those active labels. `RecycleBinScreen`'s generic empty hint names income ambiguously; name income plans. The actual backend recycle label, expected-month detail and `收入计划已恢复。` result are already correct. The existing recycle VM fixture will use that actual producer wording while retaining original month/OCC, ledger refresh and zero-confirmed-stream-change assertions. |
| Web create/read/status and service refusals | `income_plans.html` already distinguishes whole-month prediction, scheduled-to-date and non-arrival; its active/archived aria labels, create amount/title and viewer empty body still need plan wording. `web_income_plans.py` create/archive/restore flashes and invalid-month message, plus `income_plan_service` month/day validation errors, are actual shared Web/API/Android text consumers. Only display/error strings change: form names, hidden month/OCC/CSRF/ledger values, validation predicates, transaction/receipt ordering and redirect destinations remain identical. |
| Direct verification producers and omissions | Existing forecast semantics, Income ViewModel resource-ID assertions, real Income Room edit/recovery tests and Web semantic/disclaimer tests remain. The two existing backend validation-message matches must migrate with the same missing-month/invalid-day cases, not be removed. No copy-mirroring tests are added. XML parsing, exact resource names/attributes/format arguments, reference checks, existing literal-test impact and a string-only backend AST comparison are the short evidence. Gradle, emulator and PostgreSQL are not run locally; the new exact candidate still needs its own cloud gates. |

Main-source reference inspection found no active readers for the old `income_plan_fab_add`, `income_plan_intro_body`, `income_plan_empty_body`, `income_plan_updated`, old Settings income entry resources, or `IncomeFrequency.displayName`; the actual form uses `incomeFrequencyLabelRes`. They are not changed as a speculative retirement exercise. The active expected/scheduled and history/recovery wording retains its existing meaning; no financial amount, currency, month, protocol, schema, state, authority or persisted intent is changed.


After the copy edit, 22 actively referenced Android strings across four existing resource files use plan/expected meaning: screen/section/count, empty/read errors, create/edit titles, expected amount/date, validation and create/archive/restore feedback, shared Sync mutation label, advice privacy date and recycle empty text. The actual Web create/readonly/archive labels and successful redirects now use the same plan wording; both Web parsing and shared month/day validation errors say expected month/day. Existing two validation-message matches and the recycle VM fixture were updated to the actual producer wording, with all inputs, assertions and state-transition expectations preserved. Correct forecast/scheduled, intent-month, local-edit acceptance, retry/drop and actual recycle success wording is unchanged.

Actual short checks: four XML files parse; every resource name/attribute and formatting placeholder matches `ce047e2c`, and all 22 changed keys have current Kotlin references. Jinja parses and its expression/control-token stream is identical. The two backend source files and existing backend test are AST-identical after excluding string literal values, so conditions, field names, error/status codes and transaction/call structure are unchanged. Per-path classification selects Android for the four resources and existing JVM fixture, PostgreSQL for its existing validation test, PostgreSQL/frozen-backend for the Income service, and PostgreSQL/frozen-backend/Desktop/Windows for the actual Web route/template; the contract selects no heavy lane. This follows the current direct-producer map rather than assuming Android alone proves the Web copy. No new mirror test, Gradle/emulator/PostgreSQL execution, commit, push or review-thread resolution was performed. The non-inline review finding has a source correction; new-head runtime/CI qualification remains with the controller.
