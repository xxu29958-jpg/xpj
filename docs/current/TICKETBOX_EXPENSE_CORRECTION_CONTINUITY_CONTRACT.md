# Expense correction continuity working contract

This implements the active whole-product Goal and the Owner's September 6 capability-first ruling. The final Product, Windows/data-lifecycle and post-G2 contracts remain the authority. This document records the controller's admitted Facts decision and its production impact before implementation; existing code, tests and ADRs are evidence, not product authority.

The source comparison is qualified main `5d4606267dcdccd5fb514cccdaec559b755e8eb1` to the explicit dependency `3aebc23418bddf0c5631fee7926459e9c5d38a84`, tree `05dc25c802566bbe42864a9c62fa3b1e804da01a`. The latter is the Income PR #377 candidate, not independently qualified main. Work starts in `codex/expense-correction-continuity-20260906`. No prior candidate's cloud result qualifies this slice.

## Working boundary

| Item | Current contract |
|---|---|
| Goal | A household writer can submit one composite correction to a confirmed expense, leave or lose the first response, and recover the same original submission without losing its reason, fields, collections or identity. Local acceptance precedes outbound work and remains distinct from a published fact revision. |
| Allowed Changes | The controller authorized the complete vertical production slice after the four actual cloud counterexamples: existing correction owner, versioned payload, shared Outbox policies for this mutation, detail/global recovery consumers, required wiring, completion effects and direct tests. No commit or push by the implementation agent. |
| Forbidden Surface | No new fact writer, correction endpoint, queue/scheduler framework or identity owner; no adjacent Debt/capture/offset command rewrite, exchange calculation change, automatic merge of conflicting corrections, or general HTTP-classifier fork. Fresh G2 stays CLOSED; Windows lifecycle and public Stage 2 HOLDs remain closed to this slice. |
| Done Checks | Before production: exact dependency and consumers mapped, compilable behavior counterexamples submitted, actual cloud RED classified by the controller. Before vertical completion: persist-before-dispatch, immutable command identity, real Room reopen, readable recovery, all completion consumers, physical retirement and exact-head Android/backend evidence at their actual execution layers. |
| Evidence | The controller obtained exact `2f5384f5` cloud RED in run `34035339522`, Android job `101492278860`, JUnit artifact `9990088883`: 2100 executed tests, four failures matching the predictions below (row 0, token 8, two forbidden actions true). No local Gradle, PostgreSQL, application or emulator execution. Production changes still require new exact-head cloud execution. |

## Product and command meaning

The user explicitly submits a correction while viewing a confirmed fact under the current logical binding. Viewer remains read-only; the backend remains the final ledger/role authority. The submission is frozen and stored before local acceptance or scheduling can imply it has been kept. A suspended first request, process interruption, subsequent failed GET or screen recreation must not require the user to reconstruct the original correction or create another key.

The complete command includes the target fact and original parent OCC, idempotency key, owner/server/data-generation/ledger binding, reason, scalar changes, original currency and minor amount, and the entire requested items/splits replacements. Omitted collections mean unchanged; an empty collection is an explicit replacement. The existing time/score adapters distinguish absent, value and explicit null. The child request DTOs contain no child OCC: their whole replacements are protected by the reviewed parent OCC. Retry must not recalculate money from current FX/settings or rebuild missing fields from a newer fact.

The controller has adjudicated storage compatibility: new submissions use a versioned, verifiable complete original command. Its typed request retains the original OCC, and the stored row's target/token/binding must agree with that command before sending. Necessary original display context remains available without a successful GET. The existing Room row and its key remain the command's durable publication; no second queue or parallel writer is introduced.

Old normalized payloads contain `expected_row_version: 0`, while their row token may already have been changed by cascade. That lost predecessor cannot be reconstructed from the current server token, clock or timestamps. Such an old intent retains its original stored bytes and key and is visibly marked as requiring review. Unknown or unverifiable formats are not automatically sent, blindly retried or marked DONE. The user may inspect and explicitly discard them; a replacement is a new, explicit submission after reviewing the current fact. The historical reader has no authority to repair the old token or publish the old command as a supported new format.

For a supported original command, transient/unknown-result retry preserves key, full body, original OCC and binding. A conflict requires current-fact review; generic KeepMine or Retry with a fresh token cannot mutate the original command into an overwrite. This must hold at the action owner as well as in the UI. Existing expiry, stale-IN_FLIGHT recovery, target ordering, account/ledger isolation and explicit quarantine/sign-out cleanup retain their current owners. Switching a ledger does not grant permission to send a saved command under another binding.

The backend's existing claim-before-OCC and atomic correction/revision/claim commit remain authoritative. A replay HIT returns the original command's revision together with the current authoritative Expense; it does not promise a byte-identical historical Expense head. Pending local intent must not invent a confirmed revision or replace the authoritative fact with an unconfirmed composite projection.

## Impact closure before production

Implementation decisions after actual RED: the correction action port exposes a current-binding observation, one bound local submission and one bound recovery operation. Outbox and the typed correction adapter are required constructor dependencies in the actual graph and its fixtures. The request retains its original positive OCC; version, target, owner/ledger, binding provenance, key and token are checked before dispatch. The display envelope retains original merchant/currency/amount; old DTO fields are readable when present without inventing missing original context. No extra queue, optional sender or projection writer is retained.

Only a supported original command receiving a real 2xx can become delivered. CorrectExpense locally narrows shared 404/Discarded classification to visible failure; other mutation classifiers are unchanged. A post-2xx cache failure cannot turn delivery into network failure. DONE observation latches delivery while fetching current fact/items/splits/revisions; a failed read keeps a delivered-but-refresh-pending message and the last authoritative fact. Queued input appears only in the original-submission summary, never in canonical fact, items, splits, money or revision fields. Budget uses the existing confirmed-cache callback, and Advice retains its existing enqueue/completion invalidation.

Legacy/unknown DONE is unproven and displayed for review, with explicit local discard, not a sent badge. Existing `gcCompleted` has no live production caller at this baseline; no speculative Room migration or retention rewrite is added. Existing expiry, binding-scoped reads and explicit cleanup remain owned by Outbox. The correction identity cases are moved into a focused test file because their additions pushed the generic OutboxRepositoryTest over 800 lines; assertion strength and generic controls remain intact. Real Room reopen, response-loss replay and actual detail Save/Retry Connected sources are required and must execute in the controller's cloud lane.

Line references in this table are at `3aebc234`; Android paths are relative to `android/app/src/main/java/com/ticketbox/`. This is a bounded call-graph inventory, not proof that every listed path has executed.

| Boundary | Actual producer, owner and consumer | Required disposition |
|---|---|---|
| Four fact-entry groups, one correction form | `ui/navigation/LedgerRoute.kt:105`, `PendingRoute.kt:141`, `SearchRoute.kt:35` and `MainProductNavRoutes.kt:114` (recurring occurrence via `RecurringOccurrenceRoute.kt:33`) call the common `MainShellState.kt:321` navigation. `MainNavGraph.kt:112` enters `ExpenseEditRoute.kt:86`, which hands confirmed facts to `ExpenseFactRoute.kt:20`. `FactSheetHosts.kt:112` submits through `ExpenseFactViewModelCorrection.kt:155`. | All four groups reach the same detail/form and one repository correction call. Retain this convergence; do not restore the retired confirmed legacy editor or create a second correction surface. |
| Current publication and its exits | `ExpenseRepository.kt:157` delegates to `ExpenseCorrectionRepository.kt:37`. Current code calls the API at lines 54/65; missing Outbox/adapter or zero token uses direct fallback, the ordinary path is network-first, and only IOException enqueues. A prior unresolved target also enqueues. | Retire both direct call sites and the IOException-only publication decision. Store the complete bound command once, then let the existing dispatcher send. Cancellation and cache/read failures cannot erase an accepted intent. A missing required dependency/token is not permission to bypass persistence. |
| Schema and actual wiring | `domain/model/ExpenseRevision.kt:26`, `data/remote/dto/ExpenseCorrectionDto.kt:91`, `ExpenseItemDto.kt:50`, `ExpenseSplitDto.kt:43`, `data/repository/ExpenseCorrectionMappers.kt:39`. `RepositoryGraph.kt:80` injects the correction adapter; `OutboxAdapterGraph` supplies its wire adapters; `AppContainer.kt:179` registers the real dispatcher. | Introduce only the typed command payload/reader needed by this correction. Preserve scalar/null/collection/currency meanings; update real wiring and all affected constructor/test consumers together. Do not leave optional direct-send compatibility. |
| Durable scheduling, ordering and cascade | `OutboxRepository.kt:378` binds and inserts at line 397, then calls `onEnqueued`; `AppContainer.kt:96` uses the existing one-time Worker. Dequeue at `:477` orders and blocks unresolved same-target rows. `OutboxDrainEngine.kt:293` cascades success through `OutboxRepository.kt:558` and `PendingMutationDao.kt:392`. Only VoidExpenseOffset is currently protected. `CorrectExpenseDispatcher.kt:31` overwrites its request token from the mutable row. | Protect CorrectExpense's original command from cascade, including legacy bytes. Preserve ordering and other types' established cascade behavior. One network sender remains: CorrectExpenseDispatcher under the shared drain/claim/binding lease. |
| Three recovery surfaces | Detail has no durable CorrectExpense observer today; `ExpenseFactViewModelCorrectionOutcome.kt:14/42` handles direct Synced or an in-memory Queued projection. Global All Debts at `MainProductNavRoutes.kt:235` and Settings at `SettingsDestinationHost.kt:374` use `ViewModelFactories.kt:351`, both already supplying ExpenseRepository. `SyncStatusScreen.kt:220` currently enables KeepMine for every `expense:` target; `OutboxStatusViewModel.kt:70` obtains a fresh parent token. | Connect detail plus both global entrances to the same readable original-command description and supported/legacy recovery decisions. Restore local state even when GET fails; show waiting, sending, conflict, refusal, expiry and delivered-but-refresh-pending truth. Direct action guards must agree with the UI. |
| Persistent recovery mutation owners | `OutboxRepository.kt:640` KeepMine and `:694` failed resolution can replace OCC/key; `PendingMutationDao.kt:253/280` applies those changes. Plain `retryFailed:320` preserves body/key/token. `OutboxRepository.kt:813` can observe completed rows; stale claim recovery is five minutes, age cap is seven days, and `:954` refuses expired requeue. | A supported original retry uses no fresh token. Block both generic rebasing actions for CorrectExpense. Unsupported legacy/unknown data stays readable without futile retry; explicit discard clears only the original local intent, not a server fact. Completed observations must not miss a fast PENDING-to-DONE transition. |
| Two completion effects and fact/list readers | Direct `ExpenseRepositoryCore.kt:245` invokes `onConfirmedCommitted` -> `AppContainer.kt:368` -> `notification/budget/BudgetOverspendChecker.kt:71`. The current dispatcher cache callback at `AppContainer.kt:179–188` only upserts Room. Advice already invalidates via `OutboxDrainEngine.kt:141/301` and `AppContainer.kt:392`. `ExpenseFactRoute.kt:45` forwards completion into the existing edit/list refresh path. | Preserve the existing budget overspend check when direct success retires, and preserve advice invalidation. Adopt the authoritative Expense, reload items/splits/revisions, and refresh existing list consumers. Cache/GET failure must remain visible without minting a replacement submission. |
| Existing backend and independent Web/batch consumers | `backend/app/routes/expense_corrections.py:94/108/119` resolves a scoped writer, claims before OCC and reads the original revision on HIT. `expense_correction_service.py:102` commits correction/revision/claim atomically; `idempotency.py:104–115` includes original OCC in the fingerprint. Web GET/POST at `web_expense_correction.py:79/388` uses `_web_correction_command.py:67/114` and the same owner; the batch category/tag route at `expense_corrections.py:49` is separate. | Keep one backend fact writer and its permissions/transaction. These Web/batch callers do not call the Android repository; no consumer migration is needed there if their API/service/schema remain unchanged. Their shared service tests remain evidence obligations, not assumed runtime qualification. |

The target changes two current Android send owners (repository and dispatcher, three API call sites) into one sender, with one existing Room publication owner and one unchanged backend correction transaction. It adds durable observation to one detail and enriches two existing global recovery entrances. It does not add a fact owner, endpoint, queue or scheduler.

## Actual dependency changes since qualified main

The correction repository, correction DTO/domain/mappers, OutboxRepository, PendingMutationDao, AppContainer and backend correction route/service/idempotency owners are unchanged between main `5d460626` and dependency `3aebc234`. Their preimplementation counterexamples therefore still apply.

`CorrectExpenseDispatcher` has changed: PR #377 physically removed its duplicate HTTP classifier and calls `OutboxHttpError.mapOutboxHttpException`. Structured `state_conflict` remains CONFLICT, in-progress is retryable, and unknown/protocol 409 remains visible FAILED. This slice must inherit that implementation and the existing error markers; it must not copy the former generic 409-to-Discarded branch. The shared classifier's other domain decisions are not redefined here.

The two global route calls, factory, ViewModel, sync/drop UI now require and describe Income recovery, including unsupported-format retry refusal. Those are real changed consumers to preserve when adding correction recovery. `RepositoryGraph`'s Income wiring comment and PendingMutationEntity/DispatchResult documentation changed; the correction injection, Room columns, generic cascade/resolve implementation and drain behavior did not. The Income payload and its month semantics are not a correction schema to copy: this correction specifically requires proof of its original parent OCC.

## Test-first counterexamples and retirement

At test-first head `2f5384f5`, four tests extended three existing JVM files. They use the real ExpenseRepository/OutboxRepository/OutboxDrainEngine as appropriate, with the established fake DAO and synthetic transport/preceding dispatcher. They introduce no new test framework. The controller subsequently observed all four listed failures in exact-head cloud JUnit; the production candidate has not yet executed. The two direct identity tests now live in `ExpenseCorrectionOutboxIdentityTest`, while their original test-first locations remain in the table for evidence traceability.

| Test | Concrete counterexample and expected baseline failure |
|---|---|
| `ExpenseCorrectionRepositoryTest.composite correction is persisted before scheduling without waiting for a direct request` | Submit original reason, currency/amount, scalar clear/value changes, one item and one split. The transport signals entry and suspends instead of throwing IOException. The test awaits either local acceptance or that real transport boundary, then requires one saved row and completed local acceptance, with the scheduler observing that already-inserted row and no direct request. Baseline is expected to fail `expected 1 row, actual 0`; unexpected submission errors propagate, and cancellation in finally prevents a hung test. |
| `OutboxDrainEngineTest.successDoesNotRebaseQueuedCompositeCorrection` | An earlier same-target Patch succeeds from V=7 to V=8 while a complete legacy correction waits. Only the earlier row runs. The waiting correction must retain recorded token 7, original key/body/owner/ledger; baseline is expected to fail `expected 7, actual 8`. Existing Patch cascade and offset-token controls remain intact. |
| `OutboxRepositoryTest.compositeCorrectionKeepMineCannotReplaceOriginalCommandIdentity` | A direct generic KeepMine call supplies token 8 for a conflict containing the original token 7/key/body. It must refuse, leave the original visible and not requeue. Baseline returns true and replaces token/key. No fresh-fact review has occurred. |
| `OutboxRepositoryTest.compositeCorrectionFailedRetryCannotReplaceOriginalCommandIdentity` | The same attempted replacement uses the separate FAILED Retry(freshToken) action after a protocol refusal. It must also refuse and preserve the original record. Baseline returns true and replaces token/key. |

The last three tests intentionally use the old persisted representation: the newly adjudicated conservative reader cannot justify changing those recorded bytes or treating a fresh token as recovered original proof. Passing these tests will not alone prove recognition or replay of the new supported format.

Production retirement must also migrate the existing direct-success, IOException-fallback and direct-cache-publication assertions in ExpenseCorrectionRepositoryTest and the corresponding Synced/Queued ViewModel tests. Their meaningful assertions about reason/OCC, authoritative revision, binding, refresh failure and frozen-FX/composite coherence must move to the new persistence/dispatch/recovery boundary; they must not be deleted as inconvenient regressions. Existing supported Patch KeepMine/cascade tests remain valid controls. New-command replay needs actual dispatcher/adapter round-trip assertions for the full request and key, not a test that merely finds a field name in JSON.

The completion gate further requires real disk Room close/reopen with the actual repository/adapter graph, the three recovery entrances under failed GET and viewer/changed binding, unsupported/expired action refusal, and a server-committed/lost-response replay that produces one revision. Existing `ExpenseCorrectionDtoContractTest`, `OutboxPayloadCodegenAdapterTest`, `ExpenseFactViewModelCorrectionOutcomeTest`, binding/drain/advice tests and backend `test_expense_corrections.py:155/223/325/357/405` are direct verification consumers. JVM fake-DAO evidence does not substitute for SQLite queries, process death, backend authorization or PostgreSQL transactions.

## Implemented candidate and retirement

The following references describe the authorized working diff on test-first HEAD `2f5384f5581bcfe5ffaf320b5c934c56d02b901e`; they do not label that HEAD as containing these uncommitted changes. Production Android paths remain relative to `android/app/src/main/java/com/ticketbox/`.

| Responsibility | Current implementation and direct consumer |
|---|---|
| Store original submission before scheduling | `ExpenseCorrectionRepository.kt:41` binds the reviewed original fact, refuses unresolved/unproven predecessors, builds the complete command and calls the existing durable enqueue. `ExpenseCorrectionPayload.kt:12/46` retains the typed original request and validates its version, positive OCC, target, key presence, owner/ledger and origin provenance. `OutboxRepository.kt:399/404` schedules only after insert. |
| One sender and immutable recovery | `CorrectExpenseDispatcher.kt:19` is the sole Android caller of the correction API; it sends the validated original request/key and narrows only CorrectExpense's Discarded outcome to visible failure. `PendingMutationDao.kt:263/290` blocks both fresh-token actions atomically; `:401` and `OutboxRepository.kt:571` preserve correction OCC during cascade. `ExpenseCorrectionRepository.kt:61` rereads the binding and stored status before retry or explicit local discard. |
| Detail and both global recovery consumers | `ExpenseFactViewModelCorrectionRecovery.kt:10/45/53` observes durable original submissions and delivery while independently refreshing fact, bundle, items, splits and revisions. `ExpenseFactScreen.kt:80` renders the shared submission card outside the successful-GET branch. `OutboxStatusViewModel.kt:54/121` and `SyncStatusScreen.kt:190` use that same observation/recovery owner. All Debts uses `MainProductNavRoutes.kt:242`; Settings uses `SettingsRoute.kt:28/51` and `SettingsDestinationHost.kt:384`. Both open the existing exact fact route. |
| Canonical data and successful consumers | A pending submission never changes cached fact money, collections or revision. Only validated 2xx marks delivery; later read/cache failure keeps a delivered/refresh-pending card. After the bounded review below, `CorrectExpenseDispatcher.kt:36–53` owns cache publication and the required confirmed callback; `AppContainer.kt:179–187` supplies cache upsert and the existing budget callback separately. Cancellation remains cancellation; ordinary cache/notification failure cannot undo delivery. Existing drain Advice invalidation remains wired. `ExpenseFactCorrectionAllocation.kt:30` retains the pre-submit frozen-rate allocation check as an amount-only validator, without a canonical projection writer. |
| Required graph and constructor migration | `ExpenseRepository` and its Core now require `ExpenseOfflineMutationWiring`; that wiring requires Outbox, the supported `JsonAdapter<ExpenseCorrectionPayload>` and the display-only legacy DTO adapter. `RepositoryGraph.kt:77` is the real production facade factory, and `OutboxAdapterGraph.kt:54` retains the legacy reader alongside the new codec. JVM fixtures use actual adapters through `OutboxTestFixtures.kt:51/60`, whose fake DAO binding follows the test's supplied session. DataQuality Connected uses actual Room wiring. `SyncStatusScreen`/`SyncStatusActions` and the Settings navigation chain require the existing `onOpenExpense` callback; all current production and test consumers were migrated. |
| Physical retirement | Repository direct send/fallback and IOException-only enqueue are removed. `ExpenseCorrectionOutcome.Synced/Queued`, `ExpenseFactViewModelCorrectionOutcome.kt`, the canonical `projectCorrection` writer and Core's optional correction adapter are removed. CorrectExpense no longer reaches the generic sync KeepMine/fresh-token UI or action. Its wire/display validation reuses existing Moshi/Room/Outbox owners; no schema migration, duplicate owner or workflow framework is introduced. |

The source signature pass covered all current constructor/factory consumers, including 12 direct ExpenseRepository calls, 11 ExpenseOfflineMutationWiring calls, four SyncStatusScreen calls and seven SyncStatusActions calls. The two positional repository/wiring consumers were checked against their exact signatures. This is source evidence, not Kotlin compilation. Capture PR #381's separately added `ExpenseUploadBindingTest` is absent from this dependency; its required ExpenseRepository wiring must be migrated when the controller integrates that branch, alongside Capture's `expectedBinding`/`currentUploadBinding` additions.

The new Connected sources are `android/app/src/androidTest/java/com/ticketbox/data/repository/ExpenseCorrectionConnectedFixture.kt` and `ExpenseCorrectionRoomContinuityTest.kt`. Five cases now cover actual disk Room close/reopen and graph wiring; real detail Save/Retry plus a lost-response replay; protected SQLite cascade/recovery and viewer/changed-ledger refusal; the real shared global sync consumer; legacy DONE review/discard; and the protocol-guidance recovery added below. The transport models a committed response loss and deduplicates the full request/key. It does not execute backend PostgreSQL or actual process death. The global tests exercise the common consumer; the two navigation entrances are established by their shared source wiring, not separate executed navigation ceremonies.

Repository/dispatcher/ViewModel tests now distinguish local acceptance from canonical fact and cover full typed request round-trip, tri-state/collection/currency retention, unsupported formats, expiry, immutable identity, 2xx cache failure with the budget callback, and delivered state across failed reads/recreation. Existing supported Patch/offset controls remain. The two identity tests moved with their assertions into a 59-line responsibility-specific file; OutboxRepositoryTest is back to 781 lines. This removes the observed local source cause of `files_over_800: 43 -> 44`; the actual weight gate still needs the new candidate's cloud result.

## Direct verification routing and current evidence

The final short source probe invoked the existing `ci_gap_trigger_scope.classify_ci_paths` individually for every Android path in dependency `3aebc234` to the current working diff, including untracked additions, the deleted outcome consumer and the already committed test-first drain case. All 63 Android paths independently select `android=true`, with all other heavy scopes false; there are no missing routes. The only other changed path is this contract. The probe and source checks exited 0 in 0.689 seconds: 61 present Kotlin files had balanced lexical delimiters, and the changed strings XML parsed. `git diff --check` also passed. These are narrow source checks, not a compiler or behavior result. The earlier 18-path preparation measurement remains superseded by this full direct-producer measurement.

Both actual workflow consumers use the existing ci_scope.py result: Android fast at `.github/workflows/ci.yml:916` and Connected scope/consumer at `android-connected-test.yml:61/70`, with actual connected execution at line 145. No scope or workflow change is needed for this measured candidate. Selection does not prove execution of any new method. The existing Python environment lacks lizard; no package was installed and no weight result was inferred from these checks.

This handoff contains the complete uncommitted implementation and test sources for controller review and cloud qualification. No local Gradle, PostgreSQL, application, emulator, commit or push was run by the implementation agent. Current compile/JVM/Connected behavior, exact-head weight/contracts, dependency qualification and final independent review remain open. The actual four RED observations at `2f5384f5` and prior Income evidence do not qualify the new working diff or the whole-product RC.

## Independent review: preserve the refused command's recovery instruction

The bounded independent review inspected the complete 64-path working candidate on `2f5384f5581bcfe5ffaf320b5c934c56d02b901e`; before/after file fingerprints were identical. It admitted one current P2, accepted by the controller: removing CorrectExpense from the old global FailedCard also removed its safe protocol-error explanation. A supported correction receiving `runtime_version_mismatch` or `client_upgrade_required` remains Failed with its original command, but the new shared card displays only generic retry advice. The household user loses the required instruction to update the matching client/server version before retrying.

| Pre-change affected boundary | Minimal correction and direct evidence obligation |
|---|---|
| Detail `FactCorrectionSubmissions` and both global sync entrances all render `ExpenseCorrectionSubmissionCard`; its status branch never reads the saved error | Reuse the existing allowlisted `friendlyLastError` projection in that shared card. Specific protocol guidance replaces the generic retry sentence; the original-command Retry button remains available after the user resolves the version mismatch. Unsupported/expired/conflict presentation and explicit discard keep their current semantics. Never render raw stored errors or transport diagnostics. |
| Dispatcher → Failed row → real shared card → original Retry | Extend the existing Room/graph Connected producer before changing presentation: both protocol codes originate from the controlled HTTP transport, traverse the real dispatcher/drain, and must appear as the existing upgrade guidance. Actual Retry must return the same row to Pending with identical payload, key, OCC, owner and ledger; no revision or DONE is invented. The new test is source-only at this checkpoint: local Gradle/Compose execution is prohibited, so no observed RED is claimed. |
| Production wiring and other evidence layers | In the reviewed 64-path snapshot, AppContainer source invokes the confirmed/budget callback in the cache-publication finally block, while JVM/Connected fixtures duplicate that callback with counters. Those counters prove the fixture's effect seam, not execution of AppContainer or a real budget notification. Real Room, repository, ViewModel, common UI and dispatcher paths are exercised by the Connected source, while backend commits/process death and the two complete navigation journeys remain outside that fixture. The controller separately authorized the verification seam correction below; no extra product-domain blocker was established by this bounded review. |

## Review follow-up: one safe projection and one completion-effect owner

The shared submission card now uses the unchanged safe `friendlyLastError` projection for supported, retryable Failed rows. The existing protocol message replaces the generic retry sentence and says to update the matching version before retrying; the action still retries the saved command, without a fresh key/token or silent discard. Unknown errors keep the safe generic fallback. Unsupported, expired, conflict, Pending/InFlight and delivered states retain their original presentation. Detail and both sync entrances use this same card.

The presentation function and its allowlist move unchanged into existing `SyncStatusOverview.kt`, remaining in the same package and owner. This also removes the candidate's direct `SyncStatusScreen` source-count regression: its new correction section had raised the file from 11 to 12 functions against the configured limit of 11. Final source counts are 11 in Screen and five in Overview; no suppression, new framework or classification change is introduced.

The new `sharedCorrectionRecoveryExplainsProtocolRefusalWithoutChangingOriginalCommand` test was written before the presentation change. Each of the two real HTTP refusal codes goes through `CorrectExpenseDispatcher` and Room into the actual shared sync card. The test requires the upgrade guidance, absence of raw code/transport text and absence of the contradictory generic sentence. Each explicit UI Retry preserves payload, idempotency key, OCC, owner and ledger and leaves Pending; no fact revision, confirmed callback or Advice completion is published. This is new Connected source awaiting cloud execution, not an observed local RED/GREEN.

The controller also accepted the budget verification gap. The sole dispatcher now requires `onConfirmedCommitted` and invokes it after known 2xx in the same production effect block that owns cache publication. Ordinary cache or notification exceptions still return Success; cache/notification cancellation still propagates, and a notification failure cannot mask an earlier cancellation. AppContainer keeps its original `ExpenseRepository.onConfirmedCommitted` → `BudgetOverspendChecker.checkAfterConfirmedWrite` chain. AppContainer and Connected cache lambdas now contain only upsert/failure injection, with no copied finally. All ten constructor calls are explicit: one production, eight JVM and one Connected.

`CorrectExpenseDispatcherTest` now uses the existing real `CheckerHarness`/`BudgetOverspendChecker` and asserts budget source invocation, decision key/overspent amount and the recording store's sent key. Its notification-error and cancellation cases cover the production effect owner. The Connected counter remains only callback-invocation evidence, not proof that AppContainer ran or an OS notification was sent. No real budget HTTP, OS notification, Gradle, emulator or PostgreSQL execution is claimed by this follow-up.

The updated per-path classifier measurement covers the complete dependency-to-working candidate: 65 paths, including 64 Android paths, all individually selecting only Android with zero omissions; the other path is this contract. This supersedes the earlier 64-path source inventory. Both Android fast and Connected retain their existing required execution producers. Exact-head cloud compilation, behavior and code-weight results remain pending; the controller owns commit/push and qualification.

Actual final short checks passed: the moved error projection and complete allowlist match `2f5384f5` exactly apart from the required internal visibility; the five touched presentation/Connected Kotlin files have balanced lexical delimiters; `git diff --check` passes. The new Connected regression spans 42 physical lines, and its class file has nine function declarations. These source checks do not compile Kotlin or execute the new behavior. Comparing the independent review fingerprint with the final working state changes only the six authorized presentation/Connected/contract files plus the other collaborator's three completion-effect files; no other worktree content was changed by these follow-ups.


## Root integrated recovery closure

Before merge resolution: Facts `b308286a` is combined with Debt `663ab431`, which contains Income `ce047e2c`, Web `1a3bbf9a` and merged Capture main `6376bde3`. The two production conflicts are the shared Sync screen and Outbox status model: correction observation/filtering and original recovery must coexist with the Debt adjustment observation, summaries and required actions. The relocated safe-error owner must retain the incoming Debt error mappings, without restoring a second error renderer. Both global entrances and detail keep their domain-specific original-intent recovery; no generic fresh-token or successful-discard fallback may replace either. Capture's added upload-binding fixture and Facts' new global-recovery Connected consumers also need the newly required wiring, using the existing session-bound fixture/required repositories. All original payload/key/OCC/binding, known-2xx, notification and schema owners remain; direct cloud producers must execute the integrated factories, actual Save/Retry and Room cases on the resulting SHA.

After resolution: the correction observer and Debt combined stream independently update their own state fields; both special recovery branches return before generic retry. Generic conflict/failed lists exclude CorrectExpense while retaining Debt waiting and original summaries. One safe renderer now preserves all 14 exact mappings and three prefix mappings from both parents; Screen has 11 top-level functions and Overview six. An independent comparison of the three merged production files found no executable regression. The Capture upload test now uses the existing session-bound expenseRepositoryFixture, and two Facts Connected factory consumers explicitly receive all four recovery repositories from the same graph. Static call checks found 12 ExpenseRepository/one Core, ten four-argument correction dispatchers, eight three-argument recovery factories and eight four-owner recovery repository constructions; original test assertions remain. These source/lexical/classifier checks are not compilation or runtime PASS. The resulting exact candidate still requires cloud JVM/Connected and combined parent qualification.


## Legacy DONE overview exit: before correction

At exact integrated `56710a4a970b478189a422a7d35e2c994652b989` (clean before this follow-up), the controller admitted a remaining success-projection defect. An old normalized CorrectExpense row can remain physically DONE while its supported-intent check fails and the correction reader correctly reports `delivered = false`. Generic Outbox counts omit DONE, so the same Sync page presents a review-required original card alongside the settled caption. This follow-up starts test-first; no production change or executed RED/GREEN is claimed here.

| Impact boundary | Current producer / consumer and required disposition |
|---|---|
| Sole overview call and both entrances | `SyncStatusPageBody` in `SyncStatusScreen.kt:143` passes only `state.status` to `SyncStatusOverviewSection`. All Debts (`MainProductNavRoutes.kt:235`) and Settings (`SettingsDestinationHost.kt:375`) reach that same screen and required recovery factory. Carry the correction reader's narrowly defined review count into this existing overview owner; do not introduce another state owner or per-entrance rule. |
| Old successful exit and card | `syncStatusOverview` in `SyncStatusOverview.kt:35` counts only queued/conflict/failed/quarantine; `isSettled` is therefore true for this legacy DONE case. `overviewCaption` selects `sync_status_overview_caption_settled` (`strings_settings_core.xml:224`). The same screen's correction section already renders the shared `ExpenseCorrectionSubmissionCard` as unsupported, with current-fact review and explicit local discard, without Retry. The overview must agree with that card. |
| Persistence / protocol / recovery | Only corrections whose original delivery is unproven and whose stored status is DONE should add `reviewRequiredCount`; Failed/Pending/InFlight/Conflict already have existing count owners and must not be double-counted. Verified supported DONE remains settled. Do not change a stored status, create quarantine, infer whether the server committed, change payload/key/OCC/binding, or trigger any dispatch/notification. Discard remains the existing explicit local action. |
| Wording boundary | Add explicit current-fact review guidance which leaves delivery unproven. Do not reuse `sync_status_overview_caption_needs_action`, whose promise that processing will enter the ledger falsely implies non-delivery. Proposed one-row caption: `1 笔旧提交需要核对当前事实，送达情况尚未确认。` |
| Direct verification producers | Preserve the existing three overview cases in `SyncStatusFailureTest` (counts, true settled empty state, adoption guidance). Extend the existing disk-Room legacy-DONE case in `ExpenseCorrectionRoomContinuityTest` through the actual repository graph, required global VM factory and shared Sync screen: review card without Retry, no settled/non-delivery caption, explicit review count, unchanged stored command and zero API effects; explicit discard may then restore the settled caption. The current pure function cannot receive correction observations, so this executable Connected regression supplies the first behavior counterexample without a made-up overload or compilation-only failure. Pure review-count and non-double-count cases follow the production signature after actual RED. Android fast and Connected remain the direct compilation/execution producers; no local Gradle, emulator or PostgreSQL is authorized. |


### Connected compiler failure before the overview counterexample

Exact test-first head `1b04fc73279c2e559394250701ab48260bc4ee96`, Connected run `34042071717` / job `101510560743`, failed `:app:compileGrayDebugAndroidTestKotlin` at `ExpenseCorrectionConnectedFixture.kt:127`: `expenseFactBundle` overrides nothing. The actual failed job log was read; this is a fixture signature failure, not an executed overview behavior RED. Same-head CI `34042071757` and CodeQL `34042071733` were queued at the single status snapshot, so no Android-fast compiler or test result was available.

The existing `ExpenseCorrectionApi.expenseFactBundle` takes a String path ID; the real `ExpenseOffsetRepository.fetch` already calls it with `expenseId.toString()`. The fixture incorrectly declared Long. This tests-only correction changes that parameter to String and checks it matches the fixture's current expense ID before returning the same synthetic bundle. The real facade/fetch/DTO/cache consumer is now the method being overridden; no production signature, financial value, callback, summary/caption or existing assertion changes. The previously added Room/shared-Sync legacy-DONE counterexample remains unchanged and awaits a compilable candidate's actual Connected execution. No local Gradle, emulator, PostgreSQL, commit or push is performed.


### Projection complexity: before bounded extraction

The same `1b04fc73` Android-fast job `101510558646` in CI `34042071757` is now completed with failure; the controller's actual log identifies only Detekt `projectedCorrectionParent` complexity 15 against the unchanged maximum 14. Its diagnostic artifact `9992141233` was downloaded and parsed: 368 JUnit suites, 2,134 actual tests, zero failures/errors/skips. In particular, `ExpenseFactViewModelCorrectionTest` ran 9 cases, `ExpenseFactViewModelCorrectionOutcomeTest` 8, and `CorrectExpenseDispatcherTest` 8, all passing. This is pre-extraction JVM evidence and does not qualify the still-unexecuted Connected overview counterexample.

| Existing responsibility / producer | Minimal extraction and unchanged boundaries |
|---|---|
| Loaded correction validation | `ExpenseFactViewModelCorrection.kt:147` calls `wouldOverallocateLoadedSplits`, which uses `projectedCorrectionParent` only to reject a known-invalid split total before enqueue. Extract the home-currency identity lookup (persisted raw code, otherwise enum only when the raw code is absent) into one private helper in `ExpenseFactCorrectionAllocation.kt`. Its call remains at exactly the old evaluation point. |
| Original command and known/unknown calculation | Preserve money-field presence, direct home amount priority, both required original fields, unknown raw-currency refusal, same-currency amount, original-currency/time/FX guards, positive frozen rate, decimal exponent/rounding/range and caught conversion exceptions. Preserve existing split null versus replacement handling and checked sum overflow. Nothing publishes a canonical amount or changes the command, receipt, Room, backend or recovery owner. |
| Direct tests and qualification | The existing real VM cases reject both lowering an amount below loaded splits and an offline switch to home currency below those splits before repository calls. Existing non-amount/unknown-currency and queued-versus-canonical assertions remain. No mirror test, threshold change or suppression is added; compare the helper expression and re-inline it to prove source-equivalent evaluation, then use the unchanged JVM producer and final-head Detekt in cloud. |


After extraction, `projectedCorrectionParent` calls `correctionHomeCurrency(expense)` where the original expression was evaluated. The helper retains that exact raw-code/absent-only fallback expression; re-inlining it reproduces the original file body exactly. All subsequent same-currency/original-currency/FX/rate/conversion branches and the caller's split aggregation are untouched. Local lexical and diff checks pass; the file has three functions and its single-path classifier selects Android. The earlier fixture String-ID repair remains; these two code files plus this contract are the only uncommitted paths. The exact post-extraction Detekt/JVM and post-signature Connected results remain unexecuted locally and must come from the next cloud candidate. Overview production is still unchanged.

### Executed overview RED and real input targeting: before implementation

Exact head `4bc14c987d6f1617322c82ad287dde889c83ff8d`, Connected run `34043040947` / execution job `101513183166`, produced artifact `9992433070` (`connected-test-reports`). The downloaded JUnit XML contains 122 tests, two failures, zero errors and zero skips. `legacyDoneSurvivesReopenAsReviewRequiredAndCanOnlyBeExplicitlyDiscarded` fails at line 215 because the actual shared Sync screen still contains `没有等待处理的离线改动。`. This is the executed behavior RED for the overview responsibility above; the prior fixture compiler failure was not that evidence.

The other failure, `realDetailSaveReopensOriginalSubmissionAndRetriesLostResponseWithoutForgingFact`, is an input-targeting failure at line 56, before Save. The selected `更正原因（必填）` node exposes only Text actions and no RequestFocus. In the real `AppTextInput`, this label is separate from its `BasicTextField`; `CorrectionReasonSection` supplies the sheet's first editable field, followed by the amount field. Correct only the test selector to require the real SetText action, using the existing editable-order pattern for the first field and editable text for the amount. Keep text replacement, actual Save/Retry clicks, Room reopen, original key/body/OCC/binding and canonical-versus-pending assertions. Do not bypass the production callbacks or infer a financial defect from this targeting failure.

The implementation remains within the previously listed overview impact boundary: its sole caller must explicitly pass the existing correction descriptors, only unproven stored DONE contributes review-required count, and the dedicated caption must not claim delivery or non-delivery. The existing three overview cases retain explicit empty descriptor inputs; narrow positive and negative count cases must include supported DONE and the already counted statuses. The two shared Sync entrances, detail submission card, persistence/protocol/recovery owners and both direct Android cloud producers remain as listed above.

After implementation, `SyncStatusOverviewSection` and `syncStatusOverview` both require the correction descriptor list; the sole `SyncStatusPageBody` caller passes `state.correctionObservation.corrections`. The original repository projection remains the authority for `delivered`. Only `!delivered` with stored `Done` adds `reviewRequiredCount` to `needsActionCount` and prevents `isSettled`. Its dedicated review caption takes precedence so the overview never uses the queued/non-delivery promise for this case; the existing queue/conflict/failure/quarantine metrics and sections remain. Both current navigation calls (`MainProductNavRoutes.kt:243`, `SettingsDestinationHost.kt:383`) still consume the same Sync screen. No repository, Room status, command, dispatcher, binding, retry, discard or notification code changes.

The three original overview cases now pass explicit empty lists and retain their old assertions. Two additional cases cover unproven DONE, supported DONE returning settled, and Pending/InFlight/Conflict/Failed retaining their existing counts without another review count. The real Room/shared-Sync regression still requires the unknown-delivery caption, an unchanged saved row, no Retry or API/notification effect, and settled only after the original explicit discard. The detail test now chooses the first actual editable for reason and an editable with the original amount text; the same real text input, Save and Retry actions continue through the production form callbacks.

Final narrow checks on this six-file working diff passed: all four Kotlin files have balanced lexical delimiters; their function-declaration counts are 9 (Connected), 6 (Overview), 11 (Screen) and 11 (pure test). XML parses, all 267 old resource values remain unchanged, and only the one integer-placeholder review caption is added. Reversing just the two selector replacements and their imports reproduces the original Connected file exactly, including every Save/Retry, original-command and Drop assertion. Each of the five Android paths independently selects only Android through the existing classifier; the contract selects no heavy scope. `git diff --check` passes. This is source/producers evidence, not Kotlin compilation or a post-fix behavior result. No local Gradle, emulator, PostgreSQL, commit or push was run; the next exact cloud candidate must establish JVM/Connected/Detekt GREEN after the actual 122-test/two-failure RED above.

### Historical delivery observation: before implementation

The controller accepts formal P2 `PRRT_kwDOS5LrfM6ftViK` at fixed `201f87824e386d2c77250cb1e5a9194a75b77e0b`, tree `411afd81a237945ee230d4ff5cc189d3d88af257`. `ExpenseCorrectionRepository.observe` includes completed rows, while a new `ExpenseFactViewModel` starts with an empty completion set. The first supported DONE snapshot is consequently treated as a new completion: it starts the five refresh operations and sets the return-path advice flag from historical request fields. Reading an unchanged fact and pressing Back can therefore clear an already obtained advice result. `BudgetAdviceViewModel` does not automatically request another result; the user must generate it again. This is a source-supported counterexample, not an executed JVM result.

| Direct boundary at this subject | Required behavior and verification producer |
|---|---|
| Ledger, Inbox, Search, recurring occurrence and both global Sync fact links converge on `ExpenseEditRoute:85` -> `ExpenseFactRoute:27` -> the same fact VM. `ExpenseEditViewModel:157` may supply either a remote fact or its local fallback. | A newly opened detail keeps historical supported DONE visible without reporting a new edit. Add the historical-snapshot case to the existing `ExpenseFactViewModelCorrectionOutcomeTest`; it checks the actual VM return flag and repository-read effects, not source text. |
| `ExpenseFactViewModel:180-192` starts Room observation and initial reads independently; `ExpenseFactViewModelCorrectionRecovery:29-50` currently treats every first DONE as a new event. | Separate initial recovery state from a new completion without losing an authoritative read. A delayed first observation can arrive after initial reads used an old snapshot and the background command completed. The second case releases that observation only after publishing the new server fact/timeline. It permits initial reads to wait for the first observation; blindly ignoring first DONE while keeping old reads must fail. |
| Live Pending -> DONE, repeated emissions, failed reads and reopening; original binding and payload ownership. | Retain the existing live-completion and failed-GET/reopen cases unchanged. A real new completion refreshes the authoritative fact and timeline; repeated historical observation does not. Keep the original command/key/OCC, `delivered` descriptor, generation checks, binding isolation and explicit review/retry/drop. No GC, migration, second queue or delivery-acknowledgement store is authorized. |
| Advice and publication exits: `ExpenseFactViewModelCorrection:164-173`, `ExpenseFactRoute:45`, `ExpenseEditRoute:90-92`, `MainNavGraph:116-123`; `OutboxDrainEngine:285-302` -> `AppContainer:395-396`. | Retain current enqueue invalidation and the shared engine's real replay-success invalidation. Historical observation must not masquerade as another successful write. Preserve the existing merchant-only control and do not add another advice owner. |
| Detail delivered/refresh-pending card; global Sync observation and overview; original sender and Room continuity. | Keep the existing descriptor and recovery consumers. The original `ExpenseCorrectionRoomContinuityTest` Save, Room reopen, Retry, read-failure, binding and immutable-command assertions remain unchanged; these two JVM cases do not claim actual Room or process-death execution. |
| Direct cloud producers and this test-first boundary | Only the existing outcome-test file and this contract change. The Android test path selects the existing Android-fast producer through `ci_gap_trigger_scope`; the contract selects no heavy lane. No production code, local Gradle, PostgreSQL, device, commit or push is authorized in this phase. The controller must obtain actual exact-head JVM RED before production work. |

The test-first source is now prepared: the historical supported-DONE case checks delivery remains visible, Back reports no new advice-input change, and the already current root does not start another completion read group. The delayed-observation case uses the real VM with only its repository observation held behind a `CompletableDeferred`; the first emitted value is DONE after the fake server's fact and revision timeline have advanced. It asserts the final authoritative bundle/timeline and unchanged original intent/key/OCC/owner/ledger, without requiring the old initial reads to start before the first observation. The existing eight test bodies compare unchanged against `201f8782`; the file now has ten tests and 299 lines. The actual per-path classifier selects Android alone for the test and no heavy scope for this contract. `git diff --check` passes. No Kotlin/JVM execution has occurred here: the historical case is expected RED against this source, while the delayed case is a positive control against an incorrect first-DONE shortcut, not a second claimed runtime failure. Production and Connected sources remain unchanged.

### Executed historical-DONE RED and initialization correction

The controller read the actual Android-fast output for CI `34050107914`, execution job `101532311204`, source `1672a84d6c046659583d9fa713e29d81e2d3ddf0`, tree `f990532156bdcc0ea638808d04ca911b5ccb2494`: compilation reached the test task; 2,141 tests completed with seven failures. The new historical-reopen case is the sole Facts failure; the other six are the independently owned Debt counterexamples. The delayed-first-DONE control and the prior Facts cases did not fail. The controller subsequently read all four requested cancellations as completed/cancelled, including the remaining test-first CI, CodeQL `34050107893` and Connected `34050107879`. Cancellation is not qualification. New production GREEN must come from the final candidate, not this intentionally failing source.

| Changed or preserved responsibility | Implementation and direct-consumer closure |
|---|---|
| One fact initialization owner | `ExpenseFactViewModel` passes its original initialization body as the required `observeCorrectionSubmissions` first-snapshot callback. Conditional root/bill-split lookup, categories, thumbnail, items, splits, fact bundle, revisions and revision-member names all remain in their original order. They begin after a real observation with current access has established the historical DONE baseline. The callback uses the current retained fact, not an old constructor snapshot after a cleared identity. No initial read group remains outside this owner. |
| Historical snapshot versus live completion | A null completion baseline means no current-binding snapshot has been established. First delivery records the baseline and starts the normal reads without an edited flag. Later newly delivered rows alone enter `refreshNewCorrectionCompletions`, which retains the original request-field advice predicate, summary invalidation and five authoritative refresh calls. Identical DONE rows do not create another completion event. The extracted helper owns the real completion effects; it is not a second observer or a forwarding wrapper. |
| Binding loss, switch and read recovery | Existing binding comparison, correction draft reset, five generation increments and state reset remain. Binding loss clears the completion baseline; reacquiring a binding establishes its historical baseline and invokes one full authoritative refresh without treating its history as this page's edit. Current null access is not substituted with a fake empty successful snapshot. Delivered state, failed-read flags and the existing explicit refresh/retry/drop actions retain their original owners. |
| Required wiring, including offline access | The sole production constructor is still `ExpenseFactRoute` -> `expenseFactViewModelFactory` -> `ExpenseRepository`. The required `observeCorrections` implementation delegates to `ExpenseCorrectionRepository`, whose current access comes from `ApiServiceProvider.observeActiveLedgerAccess` over the local session store, then the binding-scoped Room query; it does not require a successful network GET. Full Android source search finds only that production implementation, the existing emitting `FakeExpenseFactActions`, and the explicitly delayed test delegate. Their signatures need no fallback or constructor change; the sole initialization call supplies the new mandatory callback. |
| Existing successful exits and readers | `submitCorrection` enqueue effects, `CorrectExpenseDispatcher` 2xx/cache/confirmed callback, `OutboxDrainEngine` replay-success advice hook and its `AppContainer` installation are unchanged. Advice remains process-lifetime cached state; opening historical delivery does not own another invalidation. The Back chain, global Sync descriptor/overview, detail delivered/refresh-pending card, immutable Room command and backend owner are unchanged. No GC or persistent notification marker is added. |
| Direct verification and exclusions | The ten existing outcome cases, all other JVM Facts cases and every original Connected assertion remain byte-for-byte unchanged in this production phase. The real Room/screen-reopen producer remains separate from the controlled JVM observation race. Only the two fact VM owner files and this contract change; both code paths select the existing Android lane, while this contract selects no heavy scope. No Debt, factory, repository, API, dispatcher, AppContainer or Windows code is changed. No local Gradle, PostgreSQL, Edge, device, commit or push is performed. |

Local source checks for this uncommitted candidate pass `git diff --check` and the actual per-path classifier. The initialization call sequence compares equal before/after; the VM retains 11 declared functions, and both Kotlin files have balanced lexical delimiters. These are source checks, not Kotlin compilation or behavior GREEN. A diff check over all JVM/Connected tests, UI, repositories, `AppContainer` and the fact factory is empty. The candidate is returned as exactly the two fact owner files plus this contract against `1672a84d`; the controller owns final-head cloud execution and integration.

### Initialization consumers: supplemental closure before implementation

The controller's source review accepts three directly affected missing reads in the first working fix: a binding reset used only the five completion refreshes, leaving custom categories and timeline member names at their reset defaults; an authoritative bundle could install an eligible confirmed root after root GET failed without starting its first sent-invitation list. These are admitted source counterexamples, not additional executed RED results. The earlier three-file scope/check receipt above describes that first working fix, not this supplemental candidate.

| Entry / owner / consumer before this correction | Required minimal closure and direct producer |
|---|---|
| First valid Room observation and every replacement binding -> fact initialization callback; category selector and timeline member-name projection | Reuse the complete callback for each new binding, preserve the historical DONE baseline, and do not call completion/advice effects for that history. Extend the existing `ExpenseFactCorrectionBundleTest` with same-ledger, different-owner/session binding and custom categories/member names. |
| Root GET or accepted authoritative bundle -> the existing sent-invitation list owner -> fact invitation panel and sheet remaining amount | Start only the necessary first read once per initialized projection. Repeated/current or rejected stale bundles must not cause another first read. Explicit list refresh and send/cancel refresh keep their existing behavior. The added bundle case fails root GET, adopts the real-shaped bundle root, and checks exact fact filtering, loaded status and one list read through repeated/stale bundles. |
| Categories, timeline member names and sent invitations -> bound repository call -> late VM response | Existing repository guards reject changed bindings, but the old sent-list failure still reaches the VM. Capture the initializing binding and reject its late publication at each original VM owner. The binding control releases old category/member successes and an old sent-list binding failure after the new family is visible, asserting that neither values nor load state are overwritten. |
| Verification and old success exits | These two cases are supplemental closure controls written before the corresponding production changes; they have not executed locally or in cloud. The sole executed Facts RED remains the historical supported-DONE case above. Preserve all original outcome, bundle, BillSplit and Connected assertions and the sole dispatcher/engine/AppContainer success owner; no new observer, delivery marker, GC or persistent state. |

The supplemental implementation now reuses the same required callback for every null-to-established completion baseline, including a new binding after state reset; the local first-ever-snapshot flag and its five-read-only replacement-binding branch are retired. Category and revision-member lookup capture the current logical binding before launch and check it before publication. The sent-list owner checks that binding both before its repository call and after its result, so a rejected old call cannot replace the new family's loaded list with Failed.

`loadBillSplitSent(onlyIfUnknown)` retains its explicit-refresh default. Initial root loading, a retained initial root and an actually adopted bundle use the first-read mode; the owner claims Loading synchronously before launch, so competing initial root/bundle callbacks start one request. Bundle generation and monotonic row-version rejection stay in the original owner; only the atomically adopted root can trigger that first read. The list remains filtered to that fact. Explicit fact Retry/live-completion root refresh, direct sent-list refresh and send/cancel success still call the original forced-refresh path. An existing Failed first list is not silently retried by another bundle. The only additional state is the existing list load state; no second list owner or observer is introduced.

The complete affected consumer set is the correction category selector (`ExpenseCorrectionSheet`), timeline display names (`FactTimelineSection`), sent invitation panel (`ExpenseFactScreen`), and the invitation sheet's known remaining projection (`ExpenseFactSheetHosts`). The other two `fetchSplitMembers` consumers are the explicit correction-split editor and invitation-member chooser; their request/command implementations are unchanged by this initialization correction. The existing item/split/bundle/revision read group, supported-delivery descriptor, Back return flag and dispatcher/engine/AppContainer success chain retain their ownership. This claim does not close the separately admitted root-versus-collection cohort defect.

Source checks on this seven-file initialization candidate pass `git diff --check` and the actual changed-path classifier (each Kotlin path selects Android only; this contract selects no heavy lane). The main VM still declares 11 functions. The original bundle test body and the complete Outcome/BillSplit/Connected tests, UI and repository owners are unchanged in this phase. These checks are not Kotlin compilation or behavior GREEN. The initialization production is frozen before the following independent test-first additions.

### Root/collection cohort and reason limit: admitted test-first boundary

The controller accepts P1 `PRRT_kwDOS5LrfM6ftsRf` and P2 `PRRT_kwDOS5LrfM6ft6BV`. The former is a fact-client defect present on the CSV merge ancestry, not a new CSV writer responsibility. Source `1672a84d` retains old item/split values while their reload is Loading or Failed; even a successful old collection response may carry an older `parentRowVersion`. `canStartCorrection` only checks root readiness and outstanding submissions. The form freezes those old collections with the new root and the composite request uses that root's OCC. The item/split collection's parent tag is not checked on editor entry, draft construction or submit. A later completion while an older form is already open likewise has no submit-time baseline guard.

| Before-implementation impact boundary | Required behavior and direct verification producer |
|---|---|
| Root GET and bundle adoption; items/splits query owners and their independent responses | Reuse the existing `ExpenseItems.parentRowVersion` and `ExpenseSplits.parentRowVersion` with root identity/version. The backend `receipt_item_service.list_expense_items` and `expense_split_service.list_expense_splits` supply the parent version used for each collection response; writes already advance that parent under OCC. Do not invent a composite API, independent collection version or DB writer. Loaded alone is insufficient. |
| Summary correction action -> `openCorrectionSheet` -> collection buttons and their original editors -> `buildCorrectionDraftOrMessage`/`submitCorrection` | Loading/failed/mismatched old collections cannot enter a replacement edit under a newer root, nor pass the submit boundary. Leave independently valid scalar work possible instead of making an unavailable optional collection permanently block every change. A changed root/completion while a form is open must preserve the draft and require current review before another local command is published. |
| Explicit refresh, readonly and recovery consumers | The existing refresh action can restore matching authoritative root/collections and re-enable the collection editors. A Loaded version mismatch must have an explicit recovery path, not an automatic unbounded refresh loop. Existing readonly/binding/OCC/immutable-command/Room-delivery behavior and original assertions stay in force. The new `ExpenseFactCorrectionCohortTest` exercises real VM observation, query suspension/failure, old successful parent tags, explicit refresh and an already-open form through the original methods. |
| Reason form -> mapper/repository save -> serialized command -> supported-payload reader -> recovery and dispatcher | Backend `_expense_revision.py` limits reason to 500 Unicode characters. Reject 501 before local acceptance; retain the entered reason and other draft fields. Accept 500 BMP or supplementary characters without interpreting UTF-16 length as the API limit. A previously stored impossible reason remains byte-identical and visible as an unresolved command; it cannot Retry, manufacture another key/OCC or be called delivered, and explicit discard remains available. |
| Direct reason producers and test-first staging | Two cases in existing `ExpenseFactViewModelCorrectionTest` cover 501 refusal and 500 acceptance through actual form methods; two in existing `ExpenseCorrectionRepositoryTest` cover real repository/Outbox save and reopening an injected original payload followed by refused Retry and explicit Drop. No new production API is referenced. These three test files plus this before-impact record may be staged independently of the dirty initialization production. No existing test assertion is weakened and neither new FIX has production changes in this phase. |

These cohort/reason cases are prepared source counterexamples and controls, not executed RED or GREEN. The only executed Facts RED remains the historical DONE failure in job `101532311204`. The controller owns the next exact test-first cloud execution, then any authorized production correction. No local Gradle, PostgreSQL, Edge, commit or push was run.

### Executed cohort/reason RED and complete request admission

The controller's independent XML readback now establishes source `1d1e067fdd57c0de10f54e0eab50c708cd2dca0f`, tree `34d29331baa4c2f237844b050a8fa34b81e008c4`, Android-fast job `101538585651` (checkout `fff16796…` has the same tree): 2,147 tests, 14 failures, zero errors/skips. The cohort case fails because Loading items still enter the editor; the already-open-form case publishes a second command (`correctCalls` expected 1, actual 2). The reason Repository/VM cases fail on their first BMP-501 vector; the stored invalid command is still supported. The separate 500-character VM control executes both BMP and emoji rounds successfully. Later vectors hidden by an earlier failed assertion are not additional observed REDs. The controller authorizes cohort/reason production; this run does not qualify the candidate.

The same request-admission FIX now covers the directly reachable deterministic schema/storage rejections: `schemas/_expense.py:139-147` sets merchant 255, category 64 and raw tags 500; `:367` sets item name 255; `_expense_revision.py:69` limits a composite replacement to 200 items. `tag_text.py:7-19,38-45` uses Python whitespace normalization and checks both normalized name and casefold key against 64. Backend normalization and financial/business rejection remain their existing owners.

The three existing test files are extended without new production APIs: the Repository boundary table has 31 explicit positive/negative vectors covering BMP/emoji limits, raw tags 500/501, tag names 64/65, ß/ẞ keys 64/66, U+0085 collapse/strip, item names 255/256 and 200/201 nonempty items. The VM checks representative reason/merchant/tag rejection, keeps the entered fields, then changes only the offending field and submits; the existing cohort test file adds the actual item editor's name rejection/correction flow. The original 500-character positive control and cohort counterexamples remain. The old invalid-envelope test additionally requires its matched original request (reason, merchant and items) to remain display-only, while original bytes/key/OCC are preserved and Retry remains refused. `legacyRequest` or its equivalent narrow display path must not disappear merely because a decoded envelope is no longer admissible; it must never become a replay or repair writer.

Only the nine selected tag vectors were executed locally against the actual pure `tag_text` helper, all with the expected outcomes; no app/DB startup occurred. Android table cases remain unexecuted at this extension. The approved length-only strategy may inspect normalized segment/key lengths but must not generate, persist or replace the backend's canonical key. The Java 17 lower/upper/lower observation is not an Android proof; the existing Connected fixture must exercise the selected ß/ẞ/emoji/U+0085 cases on the final Android candidate. No local Gradle, PostgreSQL, long test, commit or push was run.

### Executed complete-admission RED and final production closure

The independent actual XML readback establishes source `5033829b0611efd5d67f66975778ef0dc7cebe10`, tree `0b8b9c6ab8c2ac8d534bfb90c9a468c9986be4a1`, Android-fast job `101541915797`, artifact `9995449808`; checkout `e355eaba571bb1b1b55890264af8cafb49441957` has that same tree. Compilation reached the tests: 2,148 tests, 15 failures, zero errors/skips. The Repository table's first merchant-256 vector accepts an impossible request; the actual VM merchant entry publishes once instead of zero; the actual item editor's name-256 draft likewise publishes once instead of zero. The later table vectors, reopened-item assertion and legal-255 controls are hidden by those failures, not additional executed results. The independent 500-character reason control passes both BMP and emoji rounds. This evidence authorizes the existing admission owner correction; it does not qualify the working production.

| After-implementation entry / owner / direct consumers | Closure, retired behavior and verification producer |
|---|---|
| Required Room observation -> first fact initialization -> authoritative root/bundle, categories, member names and sent invitations | The first current-binding snapshot establishes historical delivery before the original complete initialization callback runs. Accepted bundle roots also reach the existing first sent-list reader. New bindings clear the old form, frozen collections and read generations, then use that same full callback. Category/member/list responses check the captured binding before publication. The historical-DONE Outcome case, delayed-first-DONE control and supplemental Bundle cases retain the original completion and authority obligations. No second observer, GC or persistent completion marker is introduced. |
| Root/items/splits query producers -> fact-version context -> summary, readonly collection cards and delivered-refresh status | `ExpenseFactCorrectionContext` is the single comparison of root identity/version with each collection's existing parent tag and Loaded state. Independent responses retain those tags; no query API, backend parent-version owner or collection writer changes. Readonly cards and the delivered card no longer describe a stale successful collection as current. A Loaded mismatch offers the existing explicit complete refresh; Loading/Failed retain their existing states and retry paths. No automatic refresh loop is introduced. |
| Summary action -> correction sheet -> item/split editors -> draft construction -> publication | Only matching optional collections are frozen. Missing collections do not permanently block independent scalar changes; the amount precheck also excludes stale splits. Editor entry, collection replacement construction and the submit boundary reject an old fact/binding rather than borrowing the latest OCC. A completion while a form is open preserves its fields and blocks publication until the user reviews the current fact and explicitly starts another correction. The split-member response checks the frozen baseline/binding before installing a draft; reopening an adopted invalid item retains the original item draft. The original cohort tests remain unchanged. |
| `FactSheetHosts` -> sole `ExpenseCorrectionSheet` and actions constructor; `ExpenseFactScreen` -> sole `FactLinesSection` | Every new required action/eligibility argument is supplied at the sole real call site. Collection recovery text/button appears only when the form context is still valid and the actual current collection is unavailable. Readonly, another outstanding submission or an old frozen baseline uses the context error; it does not falsely promise that repeated refresh will make that old draft publishable. Existing sheet dismissal/reopening is retained; there is no rebase or replacement-key action. The three added resources describe these actual states. |
| Actual form and child editor -> existing `toRequest` -> repository enqueue | One pure `correctionAdmissionError` validates the explicitly admitted reason/merchant/category/raw tags/individual tag/item-name/item-count limits before local acceptance. The VM uses this same mapper for visible retained-form errors, and the repository repeats the same admission at its actual boundary. Unicode counts are code points. A private whitespace predicate shared by new reason trim, original-request blank detection and tag measurement includes U+0085; the existing blank-reason VM case now also retains and rejects that input. No input is truncated. Tags remain the original wire text; lower/upper/lower is used only to inspect expansion width, never to author canonical keys or deduplicate tags. |
| Bound serialized envelope -> supported reader -> dispatcher and three shared recovery-card consumers | The same validator rejects an already stored impossible request for replay without changing its bytes, original key, OCC or binding. A separately decoded envelope is display-only only when its target/version/key/owner/ledger match the original row; its reason, merchant and items remain visible through the existing `legacyRequest` display slot. Detail and both global Sync entrypoints already share that card. Unsupported is distinct from an admissible command receiving a business/server refusal; expiry, permission, unknown delivery, Retry and explicit Drop keep their existing owners. No old-envelope repair writer is added. |
| Existing success exits -> Room receipt / sender / canonical cache / advice and return consumers | Local acceptance still closes only the submitted form and leaves the canonical fact unchanged. The original dispatcher 2xx/cache/confirmed callback, engine advice hook, `AppContainer` success installation and global recovery paths are untouched. Only later new supported completions enter the existing fact refresh/advice effect; historical first-snapshot delivery does not make Back report a new edit. No stable command field or success classification changes in this phase. |
| Existing JVM and Android verification producers | The 31-vector Repository boundary table and complete cohort test file compare unchanged with the published RED source. Existing item/split test fixtures now supply explicit parent versions matching their initial facts rather than a fake Loaded value with no version; no stale response is automatically retagged. Original Connected assertions are unchanged. One added method in `ExpenseCorrectionRoomContinuityTest` uses the real repository/disk Room fixture for four ß/ẞ/emoji/U+0085 positive/negative pairs: refusal has no row/schedule/network call; acceptance reopens the original body/key/OCC/binding from disk. Fixture cleanup isolates each pair without fabricating a delivered state. This is prepared Android execution, not an Android Unicode or process-death PASS claim. |

The final working scope is 23 tracked files plus the new `ExpenseFactCorrectionContext.kt` (24 total): 17 main-source/resource paths, five JVM test/fixture paths, one existing Connected file and this contract. Against controller-integrated HEAD `a35526df81bb9252f5e5b176b6fc6ffaf9e2a716`, the actual changed-path classifier selects Android for all 23 code/test/resource paths and no heavy lane for this contract. Existing `ci.yml` Android-fast invokes compile, JVM tests, count assertion, lint and Detekt; `android-connected-test.yml` still invokes the real `connectedGrayDebugAndroidTest` execution and XML qualification. No workflow, test threshold or assertion is weakened. `git diff --check` and resource XML parsing pass; the required sheet/action/readonly-section call sites are all supplied, and the main VM still has 11 declared functions. These source checks are not Kotlin compilation or runtime GREEN. The index remains empty; the parent-owned Outbox and both Sync merge files are untouched by this working diff. No local Gradle, PostgreSQL, Edge, device, commit or push is performed. Final candidate cloud execution and bounded independent review remain the controller's responsibility.

### Reopened split editor: admitted async-owner test-first boundary

The controller accepts the bounded review's P2 on the frozen 24-file Facts candidate at integrated HEAD `622d1f07888d43df396a2d2243cccf6641d28528`, tree `13ca33efe25ef156767486739047c6303e017f06`. The current split-member completion guards compare the logical binding, frozen root and whether the editor is open. Closing and reopening the same editor can satisfy all three for an older request. A suspended request A can therefore return after request B has loaded the reopened editor and the user has changed an amount, rebuilding `splitDrafts` from the original splits and erasing that input. The executed `5033829b` RED above did not exercise this sequence.

| Before-implementation entry / owner / consumer | Required minimal closure and direct producer |
|---|---|
| Confirmed fact -> `openCorrectionSheet` -> `openCorrectionSplitsEditor` -> `fetchSplitMembers` | Retain the existing valid binding/root/collection checks. Within that same context, only the current editor request may install member drafts; root identity alone cannot identify an editor attempt. No new repository or membership owner is needed. |
| `dismissCorrectionSplitsEditor`, `adoptCorrectionSplits`, whole-form close and subsequent reopen | Ending an editor attempt must retire its in-flight publication rights, including when the same frozen root is reopened or adopted drafts are reopened without another fetch. Keep the existing distinction between dismissing the child editor, adopting its local draft and submitting the whole correction. |
| Member success/failure -> `splitDrafts`, `splitMembersLoading`, message/tone -> `FactSheetHosts` / `SplitsEditorSheet` | Both obsolete success and failure must leave the current editor's amount, inclusion, loading and error state intact. The real sheet allows dismissal while loading and sends its amount changes through `updateCorrectionSplitDraft`; this is a reachable input-loss path. The primary counterexample below executes an obsolete success; obsolete failure and adoption are source-identified affected exits, not separately executed REDs. |
| Adopted draft -> existing draft construction/admission -> repository envelope/Room -> Outbox dispatcher | These submission, OCC, key, binding, persistence and delivery owners remain unchanged. The fix must prevent a stale read from replacing local input before submission; it must not introduce another command, repair an original payload or claim a locally edited draft is a financial fact. |
| Existing `ExpenseFactCorrectionCohortTest` -> Android-fast JVM execution | Add one case, `closed split editor response cannot replace an amount entered after reopening`, through the real VM. Initial metadata reads complete normally; A is suspended, the editor is dismissed, B finishes after reopening, and the actual update method sets an included member to `7.00`. Releasing A must preserve that complete edited draft. Assertions establish both editor requests, B's loaded state and the entered amount before the final preservation assertion. All three original cohort cases and the staged production remain unchanged. |

This supplement changes only that existing JVM test file and appends this before-impact record; it does not implement the production fix. The controller will stage the test-first candidate and obtain actual Android-fast RED before authorizing production. No local Gradle, PostgreSQL, emulator, test execution, commit or push is performed; compilation and the predicted amount-preservation failure remain unobserved until that exact cloud run.


### Executed reopened-editor RED and request-lifetime closure

Root independently read the original Cohort XML from artifact `9996132049`, CI `34056161107`, Android-fast `101548439644`: source `61aa4f7e94667778e82584a3ba878e30eef9d38a` and actual checkout `e96bcef0332ddafd86507e87a77638f3e30d2e5e` have tree `f48dede7db7362ecaed3ff65dfdf3df9d6c12042`. Compilation and JVM execution completed: 2,155 tests, one failure, zero errors/skips. The sole failure is line 173 of the new reopened-editor case, expected `7.00`, actual empty. Both reads, dismissal, B's successful completion and the actual amount/inclusion update passed before it; the following complete-draft assertion was masked. All earlier admission, cohort, outcome and supplemental initialization cases passed at this JVM rung. The original ZIP and selected XML remain in the external qualification directory; this failed source is not relabeled green.

The normal integration of debt squash main `67f0f1cb` into local Facts `a1735d91` resolved only duplicate-history conflicts in the existing Outbox/Sync owners. Its complete tree remains exactly `f48dede7`; the new async test was not changed to obtain RED. That history merge introduces no second helper, scheduler or recovery owner.

The existing VM now identifies each split-editor read attempt with one in-memory generation. Opening a whole correction or child editor starts a new lifetime; child dismissal, adoption, whole-form close and binding reset retire the previous attempt. Reopening adopted drafts still performs no member request. A queued obsolete request does not start, and both late success and failure check the same generation, binding, frozen root, open state and current correction context before publishing. Closing/adopting also clears that attempt's loading flag. The locally accepted submit exit already closes the form; subsequent form/editor opening advances the same generation, so a previous reply cannot gain rights again. VM destruction retains ordinary viewModelScope cancellation. No collection version, repository API, membership owner, payload, Room row, idempotency key, OCC or dispatcher success path changes.

The original real-VM response-order case now retains all its original prerequisites and assertions while adding two directly affected controls: an obsolete failure must not replace current loading/error/input, and an adopted draft can reopen without another fetch before the old success returns. Each checks the complete current UI state after the obsolete reply. Only the first success case is an independently executed RED; the two added controls and production correction await final exact-head JVM qualification. This bounded rework changes four original VM owner files, the existing Cohort test and this contract; no new observer or persistent state, no local Gradle/PG/emulator/long test, and no expanded review scope.


### Final-candidate structural failures: bounded rework

Source `7c2a2f8f421a2c2bbeca0887177a27dfaece955b`, tree `919513b47b2008d27955fe71d206c4c4f8334518`, executed CI `34057114144`: original Android-fast XML artifact `9996425551` contains 2,155 tests with zero failures/errors/skips. The reopened-editor success, obsolete-failure and adopted-reopen controls all passed. CodeQL `34057114084` and actual Connected `34057114098` / `101550945370` also passed (128 tests; no successful per-case XML). This candidate still failed structural checks and is not qualified. Detekt reports the split-editor function CC 19 above 14 and the sheet's six parameters above five. The actual weight artifact `9996376052` reports Kotlin test long-functions 19 -> 20. Pinned Lizard 1.24.0 attributes an anonymous range spanning adjacent tests as 92 lines; the actual three-case race body is 61 lines. No threshold, classifier or suppression change is authorized.

Before this rework, `openCorrectionSplitsEditor` repeats the same attempt-validity conditions at its success and failure exits, with only generation checked before starting a queued request. The existing correction-context owner will supply one current-attempt predicate to all three exits; the same generation/binding/frozen root/editor-open/current-context conditions remain mandatory. The sole `FactCorrectionHost` currently passes submission eligibility, both collection eligibility decisions and the context error separately into `ExpenseCorrectionSheet`, which forwards three of them to its collection entries. Replace those parallel inputs with one immutable availability projection produced by the existing VM predicates. It is a render value, without storage, observation or independent decision rights; the real action methods still enforce their current guards.

The test's three controlled response scenarios will call a named scenario function with explicit TestScope, fake repository, member list and old-result/adoption inputs. Keep every request-order prerequisite, real editor action and complete-state assertion. The existing Android-fast, Detekt, Connected and weight producers remain unchanged. These changes affect the editor's queued/read-publication boundary and the sole UI host/sheet/collection-entry chain; draft adoption, command construction, backend/API schema, original Room bytes/key/OCC/binding, dispatcher and recovery consumers retain the same owners and representations. A new exact candidate must qualify the resulting source.

After rework, the same current-attempt predicate is required before the queued member request and at both result exits. `FactCorrectionHost` supplies the four original decisions in the same evaluation order through `correctionAvailability`; the sheet and collection entries consume that immutable value, and no old parallel-argument call remains. This retires duplicate request classifiers and UI argument plumbing without adding a mutable owner or changing command/recovery semantics. Control-flow duplication and boundary coupling decrease; durable state, resource writers and asynchronous lifetime obligations remain as above.

## e176 qualification and newly verified recovery closure

Source `e176a0d2db2a9ebc7cd7e3610938364867c373b2`, tree `275de4637bcb7072ac4a5f2898c97e5993b2477a`, has actual successful CI `34058771832`, CodeQL `34058771822` and Connected `34058771844`. Actual PR checkout `ffbc1b188e06c7eee0aaab1260716722627d8702` has that same tree. Android fast executed JVM/count, both Detekt tasks and lint; Connected finished 128 tests. Successful jobs did not publish individual JVM XML, so this does not inherit 7c's 2155 XML count. Weight artifact `9996875442` reports HEALTHY GROWTH and no failures. These results qualify that exercised candidate, not the new counterexamples below or merge readiness.

The live formal review set contained 20 unresolved threads. The earlier shared recovery-card test exercised the UI callback but omitted the actual nested navigation owner. That was a controller topology miss: the global card being usable did not establish that either real entrance could reach the root expense destination. The following fixed-e176 findings are FIX, with new test-first evidence still required before production rework. Historical Debt #379 main qualification remains valid for its exercised claims; the newly discovered Debt recovery/candidate counterexamples are explicit follow-up work in this same dependent candidate.

| Responsibility / before-change impact closure | Existing entries, consumers and old successful exits | Required production outcome and direct verification producer |
| --- | --- | --- |
| Root fact navigation | `MainNavGraph` alone registers EXPENSE_ROUTE on runtime.navController. `addWorkspaceRoute`, `addObligationRoutes` and the Recurring route pass the nested controller to openExpense; Pending/Ledger/Search already use the outer controller. Workspace's Settings → Sync and Obligations → Sync share the recovery card but have independent real navigation wiring. Recurring opens an existing payment fact from the same nested product graph. | All three affected entries use the actual outer expense owner, preserve the nested page on return, and reach the real detail. A Connected test must use the production MainNavGraph, route builders, factories, repository graph and actual click; a custom probe graph with copied callbacks does not close this gap. There is no protocol, persistence or writer change. |
| Image request lifetime | Initial expense load / thumbnail Retry / full-image button → existing repository media calls → BoundLedgerRequest before/after-binding check. The latter already turns a late old-binding result into failure, but both image VM failure exits can overwrite the new binding's loaded image. FactMediaSection consumes that state directly; reset currently invalidates other reads but not these two. | Capture and check the current image request lifetime at queued, success and failure exits, invalidate on binding reset. `ExpenseFactCorrectionBundleTest` must complete B then fail A and preserve all of B's image state. This is verified stale-failure pollution, not proof that repository binding validation leaked A's bytes. |
| Derived submissions from the root snapshot | Fact screen → repayment capture / bill-split open and send → current action ports. Repayment can create a stable-key draft from old amount/OCC before the pending correction wins; new split invitations save old source amount/currency. An already-open split form is another entry. Items acknowledgment and offset actions already have same-target FIFO at their repositories; canceling an existing invitation uses that relation's identity. | One existing current-fact decision covers new snapshot-derived entry and final action while correction is unresolved, or delivery is known but authoritative root has not been adopted. Preserve history/media reads and existing-invitation cancellation. `ExpenseFactViewModelRepaymentTest` covers all unresolved states, in-flight/failed DONE refresh and successful fresh continuation; `ExpenseFactViewModelBillSplitTest` covers open/send and the existing relation control. Existing ItemsAck/Offsets controls remain required and their separate query failures must not be generalized into a new protocol gate. |
| Collection admission / original replay | UI split roster can contain more than 100 members; the replacement body and legacy display decoder carry the full list. Backend `_expense_revision.py` and `_expense.py` cap splits at 100, but current `correctionAdmissionError` checks only item count. The same admission decides first publication and supported replay. | Refuse an impossible 101-entry replacement before scheduling; accept 100 unchanged. A persisted oversized original stays readable and explicitly discardable, without Retry or rewriting payload/key/OCC. `ExpenseCorrectionRepositoryTest` adds both first-save and stored-original counterexamples; existing scalar/item/Unicode boundary controls remain. |
| Definitive target refusal | Actual dispatcher converts its 404/Discarded result into Failed(correction_target_unavailable), preserving the original rather than forging Done. PendingExpenseCorrection.canRetry currently excludes only expiry, so detail and both Sync consumers can repeatedly requeue the same immutable target. | Review/local-discard only for this marker in the shared predicate, actual recovery owner and visible status. Keep unknown delivery/protocol-upgrade recovery distinct. A repository test runs the actual dispatcher/drain into the Failed row, then verifies no requeue, no additional transport, unchanged original bytes and explicit local discard. A 404 marker does not prove the server entity ceased to exist. |
| Drop and canonical recovery | Detail's successful drop starts queries; both global entrances call the same repository operation and currently delete the row without updating Room. A Conflict or lost response can leave the old confirmed root visible after the only recovery card disappears. ExpenseDetailRepository.fetchExpense/core cache adoption, ExpenseDao confirmed roots/offsets and the bound Outbox delete are the existing owners. | The decision below applies to supported unresolved correction Drop at both detail/global action owners. `ExpenseCorrectionGlobalRecoveryTest` uses the same real repo/outbox through OutboxStatusViewModel.dropMine/dropFailed, delayed reads, failure then continuation, same-RV success and authoritative non-confirmed state. No second queue, server write, new idempotency key or reconstructed command is allowed. |
| Debt adjustment dependents | RepaymentDraftInbox observes Done but admits unresolved targets through suggested confirm, picker candidates and an already-open picker. Debt detail Drop removes a Failed row without authoritative refresh; global Sync directly uses generic Drop and bypasses the adjustment recovery owner. Retained DebtList, Receivables, DebtGoal, CreateDebtGoal and Inbox readers also consume the disappearing pending barrier. | Route recovery through the original adjustment owner, fence stale balance/OCC until current queries succeed, and let unknown-outcome recovery adopt the same RV. Extend existing DebtDetail/Inbox JVM and actual Room/consumer Connected tests. New views require their authoritative initial read; an initial empty pending snapshot cannot establish prior Drop recovery. Do not reinterpret unknown delivery as Done or require a version increment to recover. |

### Maintained semantic decision: correction Drop does not certify a financial fact

Decision owner is Codex under the current full Goal's explicit product-design/implementation delegation and the user's instruction to correct recovery models. This is a reversible client recovery policy within the existing no-silent-loss and authoritative-fact boundaries; it changes no server financial fact, identity, destructive retention policy or public protocol. The allowed actions are contract/test/implementation edits, cloud verification and protected integration, with the existing sensitive-data and Windows HOLD exclusions. Source authorities remain Goal → the three final 2026-08-26 contracts; e176 code and formal review are counterexample evidence, not policy authority.

For a supported command with Conflict or unknown delivery, accepting Drop as settled requires a same-binding authoritative root read and cache adoption before deleting the original outbox row. A pending or failed read retains the row, its body/key/OCC and an actionable recovery failure. The user can retry that recovery; a successful same-RV read is sufficient because the original may never have committed. A current non-confirmed response requires retiring this target's old confirmed cache and stream offsets, rather than treating `cacheIfConfirmed`'s no-write return as cache adoption. A binding change or concurrent row-status change cannot authorize deletion in another context. No success message may imply undoing a server commit.

Unsupported historical input and the explicit correction_target_unavailable marker retain explicit local-discard semantics; their removal does not assert any server commit, reversal or entity deletion. These exceptions preserve the existing readable historical path and prevent an unresolvable target refusal from trapping the user. They do not supply a fresh financial snapshot. The chosen policy replaces the earlier implicit global "row removed, therefore settled" exit. Keeping that exit was rejected by the lost-response/cached-old-root counterexample; introducing another persistent recovery queue or marking unknown work Done would duplicate authority or forge delivery. This paragraph is the maintained decision consumed by implementation, not an authorization to weaken current oracles.

Status: resolved-and-persisted for the specified policy; production and runtime qualification remain pending. The first actual counterexample run and final candidate are the review window. Codex revisits the policy if exact user-path evidence shows an original can be lost, bound cache adoption is incomplete, an unavailable target is trapped, or the recovery feedback cannot continue; unrelated existing green lanes cannot override those failures.

### Rejected review and remaining boundary evidence

Formal `fu-D1` is REJECT: its assertion that Kotlin Char.isWhitespace excludes U+00A0/U+2007/U+202F confuses it with Java Character.isWhitespace. The project's pinned Kotlin is 2.3.21. A 1.72-second Java 17 probe called the actual cached kotlin-stdlib-2.3.21 method: all three returned true; Java Character.isWhitespace returned false and Character.isSpaceChar true. U+0085 returned false in Kotlin, which is precisely why the existing explicit NEXT LINE addition is needed. The [official Kotlin isWhitespace contract](https://kotlinlang.org/api/core/kotlin-stdlib/kotlin.text/is-whitespace.html) also includes Unicode SPACE_SEPARATOR. No redundant production guard is added, and this JVM probe is not Android device qualification.

The admission impact check traced all other current collection producers instead of assuming omitted fields were unaffected. Correction item editing changes name/kind/amount; quantity/category/raw text/confidence and split note are copied from the matched authoritative collection. PostgreSQL columns constrain quantity/category to 64 and split note to 200. The request writer validates raw text at 1000 and confidence in 0..1. The sole ParsedReceiptItem producer in `receipt_parse_service.py` normalizes whitespace, bounds the name at 80, emits fixed confidence 0.72 and passes monetary input through the 64-character major carrier; persisted quantity is also bounded at 64. Its entire raw line is therefore below the request's 1000 limit. A seconds-scale attempted 1001-leading-zero amount was rejected by that producer, not persisted as a long raw line. No new metadata normalization or duplicate guard is introduced from an unproven hypothetical payload.

One additional reachable input was found in this same trace: `computeCorrectionSplitsChange` accepts the existing nonnegative amount parser's zero for an included member, while ExpenseSplitRequest requires positive minor units. Add zero/one admission controls to the same repository boundary cases; the shared first-save/replay admission must refuse zero without altering the user's draft. Member identities come from the bound member/current-split readers; date, score and currency/money inputs retain their existing typed/scalar validation. This does not broaden the server money or allocation policy.

This new test-first wave must complete before/after impact closure and actual cloud RED/GREEN. No formal thread is resolved merely by this classification or by e176's green gates. No local Gradle, PostgreSQL, emulator or ten-minute test is permitted; bounded source/pure checks do not substitute for those cloud producers.

The actual pinned Lizard 1.24.0 producer executed only on the five changed Kotlin paths in 0.44 seconds. It reports no over-80 function or over-15 complexity in those paths; the ordinary named scenario function is measured, not omitted: lines 149-207, 59 lines, complexity 2, five explicit parameters. A source comparison confirms every original scenario statement/assertion is preserved after normalizing indentation and the explicit same-TestScope scheduler receiver. No runtime behavior result is inferred from these measurements. Independent readback of the full actual main-source Detekt findings confirms both rules above; the failed run did not execute unit-test Detekt, which remains an obligation of the next unchanged cloud workflow.

### Actual 3ab6faf9 test-first failure and bounded test repair

Source `3ab6faf9a93b4fbfb1d475a7cba2dd790b04d227`, tree `f335aec7c4ca96543c2dc27380863116bc1c0622`, ran at checkout `4ffe7e1067c86fb147e0a0ee9a749e5c8a8520f9` with the same tree. Android fast `101564251589` failed during unit-test compilation: the new repayment map comparison lacked an explicit nullable String value type. No JVM tests or XML were produced. The next test candidate adds only that explicit expected type, preserving the original assertion. Backend contract job `101564251571`, artifact `9997860446`, separately reports the sole weight failure `files_over_1000: 25 -> 26`: two new adjustment-recovery cases grew DebtDetailViewModelTest from 889 to 1005 lines. Move those cases into the existing focused DebtAdjustmentViewModelTest using its captured-GET fake, preserving the user-path and mutation-argument oracles; no threshold or assertion is weakened. These are test-construction failures, not production behavior RED. The independent Connected result remains separate.

The before-change cache/delete trace adds two direct race controls to ExpenseCorrectionGlobalRecoveryTest. A second surface may retry the original while Drop is waiting on its authoritative GET; the old Drop must not delete that new Pending attempt, and its final delete must bind the complete original logical session. Also, confirmed-cache writers already install monotonic row versions while GETs are in flight. A delayed non-confirmed response may retire only a root no newer than that response and its offsets in the same DAO transaction; it must preserve a newer confirmed root and its offsets. These controls exercise the existing global recovery entry, actual repository, original Outbox and DAO projections. They introduce no new financial writer, queue or retention policy. Their new test source is awaiting execution and does not inherit any prior RED.

The same `3ab6faf9` Connected producer has now completed: run `34062065027`, job `101564251582`, original artifact `9997965013`, XML `connected-62.xml`: 132 tests, four failures, zero errors/skips. Three real navigation entries fail with `expense/42` absent from the inner `product/inbox` graph at MainProductNavRoutes lines 72/115/243; each actual entry click was reached, while detail/return assertions were masked. The fourth actual RED is DebtAdjustmentRoomContinuityTest line 147: after all five initial 50000 reads, a real lost-response drain with one server result at 53000, preserved original fields and global Failed-card removal, the retained CreateGoal/Inbox write barrier never arrives. Later refresh/selection/no-second-send assertions were not reached. The other 128 Connected tests passed. CI `34062065068` failed at the separate compilation/weight prerequisites; CodeQL `34062065036` passed. These outcomes authorize the corresponding navigation and Debt recovery production work, not the still-unexecuted Facts JVM cases.

The test repair also respects the actual Detekt 2.0.0-alpha.3 class-function gate: lifecycle methods are counted. Debt recovery now has a focused test class with the existing shared transport fixture. In the correction tests, unchanged bound-repository construction/seeding is separated from the scenario class in the same file, redundant scenario wrappers are inlined, and the bill-split scenario helpers no longer implicitly belong to its JUnit class. Every original behavioral assertion remains; only the two explicitly documented cache/delete races add scenarios. No baseline, suppression or threshold changes are made. Source checks are not a substitute for the next exact-head runtime result.

### Navigation production closure after the three actual Connected failures

The Workspace Settings/Sync callback, Recurring payment callback and Obligations Sync callback now call `runtime.navController.openExpense`, the controller on which MainNavGraph registers the real expense destination. Their nested `onBack` callbacks and page state retain their existing owner; the expense detail still pops the outer stack. The three erroneous inner-controller expense callbacks are physically gone. Pending, Ledger and Search already receive this outer controller and do not require another route/bridge. No repository, persisted intent, protocol, permission, factory or command owner changes in this fix. The same three actual-graph Connected cases must now reach the real detail, return to their original page and preserve the original recovery row; only their preceding failure is currently qualified.

### Correction recovery production closure after 06294fc8

Actual source `06294fc87b52004aa5d83d0caa771505f82b93bd`, tree `900b068f28aef5614f13b574fdc900218136f3fc`, executed at same-tree checkout `3605995e460b5629eeeea34bb187da4e1545dd94`. Android JVM artifact `9998242326` contains 2174 tests / 19 failures / zero errors or skips. Repository admission/replay/404, repayment, bill-split and stale image failure assertions were independently reached. Global recovery failures also exposed a fake DAO that did not invalidate failed/conflict observers and tests that treated virtual scheduler draining as completion of real IO. Those fixture failures are being corrected without weakening their original assertions; their failed assertions are not all claimed as product RED. Connected artifact `9998323575` has 132 tests / two failures: all three navigation/detail/Back paths were reached, Obligation and Recurring completed, while Workspace's later original-card assertion was blocked by its session projection fixture and readiness assumptions; Debt's recovery barrier still timed out.

The controller implements the already maintained correction Drop decision above. Supported unresolved Drop now reads the authoritative root through the captured bound request, adopts it into the existing Room projection, and only then attempts deletion of the original correction row. Confirmed adoption uses the existing cache owner and confirmed-commit callback. Non-confirmed adoption uses one ExpenseDao transaction to retire this root and its offsets only when no newer root has been installed during the request. A failed read/cache operation retains the original and returns the recovery error. A concurrent Retry changing the row status prevents the old Drop from deleting it.

`OutboxRepository.discardCorrection(boundRequest, row)` replaces `discardUnprovenCorrection`; it checks the full current logical binding under the existing transition lease, the correction type, row owner/ledger and original allowed status before the existing status-conditional delete. Detail, Workspace Sync and Obligation Sync all reach it through the same correction recovery owner. Their former generic Failed/Conflict deletion exits are physically removed from that owner. Unsupported historical commands and `correction_target_unavailable` retain explicit local discard without a futile GET. The same shared pending descriptor now disables Retry for that definitive refusal in all three consumers and in the actual recovery action.

First-save and stored-original replay share the existing admission function: more than 100 splits or a non-positive split amount is refused without rewriting the draft or original payload. The old reason, item, scalar and Unicode checks remain. No server request/schema, Room column, principal owner, cache writer, queue, scheduler, retention policy or backend/Web/batch mutation owner changes. Required direct producers remain the existing repository tests, global factory/recovery tests, real Room continuity tests and the exact-head Android/backend cloud jobs. This is production source closure, not yet new-head runtime qualification.

### Fact media and derived-write production closure after 06294fc8

The same actual 06294fc8 JVM result above reached the original repayment assertions at lines 70/121, bill-split assertion at 314 and complete-image-state assertion at BundleTest:220. These are the authorized behavioral RED for this bounded VM/UI implementation. The following changes do not weaken or rewrite those three test files. The Goal remains original-image ownership plus current authoritative root use; allowed changes are the four existing Fact VM owners, their five direct UI consumers, one string, and the required bound repository call chain discovered below. Correction storage/admission/Drop remain with their existing independent owner, Debt remains a separate producer, and there is no new DTO, server command, queue, schema, lifetime policy or Windows surface.

| Direct entry / owner / consumer | Before → after, including the old exit retired | Verification boundary |
| --- | --- | --- |
| Root snapshot eligibility | `canStartCorrection` alone combined root loading/staleness and unresolved correction status; repayment and new invitations only used the old permission/domain checks. `ExpenseFactUiState.authoritativeRootReady` now owns the same adopted-root decision for all three entries. It requires known bound access, a root, Loaded/not-loading/not-stale and no unresolved correction. Root refresh claims Loading before coroutine dispatch, so Done does not expose the old root through a queued write. | Existing historical-Done initialization, authoritative GET/bundle adoption and failed-refresh owners remain. Separate items/history/bundle query failures do not create an additional blanket write gate; their original domain controls still apply. The original repayment Pending/InFlight/Failed/Conflict and Done-refresh cases remain the direct behavior producer. |
| Thumbnail / full image lifetime | The old repository correctly refused a result from an obsolete binding, but both VM failure exits could overwrite B's loaded images with A's failure. Each existing image owner now captures binding and a separate generation, claims Loading before launch and uses one current-media predicate at queued/success/failure exits. Binding reset invalidates both generations and resets both projections; a no-image thumbnail decision also retires its old pending read. | The original A pending → B images loaded → A failures → whole B UI unchanged case is retained. Full-image/thumbnail retry and initial-load calls retain the same repository, byte model and UI media consumer. This is VM publication protection, not an assertion that the old bound repository leaked bytes. |
| Repayment creation | The real action captures the selected Expense and original binding, claims its busy state synchronously and rechecks root readiness and the selected RV before sending. The existing confirmed/positive/synchronized-expense domain check is also enforced at this direct action. After an accepted result, only the original binding gates navigation/result adoption: a later root RV must not erase that accepted success. | The one route still consumes the existing `openRepaymentDraftPublicId` once. Unresolved/delivered-before-refresh attempts do not call the repository; a successful authoritative refresh allows the original entry again. No draft amount, idempotency body or server repayment writer changes. |
| New split / existing relation | Open and final send use the same root gate plus the existing split domain check. Queued sends retain original binding/RV, and current-binding success still upserts the accepted relation before refreshing the sent list. Roster read and cancellation callbacks cannot publish into another binding. Existing invitation cancellation is not subject to the new root-readiness gate; original list/history reads remain reachable. | Original split test preserves old-invitation cancellation while unresolved, blocks both open and send, then permits a fresh-root new invitation. The existing sent-list-failure control still delegates remaining-amount validation to the server rather than blocking all relation work on an optional query. |
| Real UI consumers | FactScreen now passes required `canCreate` / `canStartInvite`; FactSheetHosts passes required `canSend`. Their sole repayment panel, split panel and already-open invite sheet disable only the new action and show the same root-readiness explanation. The original sent relation list/cancel control remains. A stale remaining amount is not presented as usable in the open sheet. | All three changed UI signatures have one real constructor/call site, each migrated explicitly; no permissive default or abandoned enabled-only-by-busy caller remains. Fact summary, media, items, offsets and timeline retain their existing hosts and responsibilities. |
| Actual IO dispatch / binding owner | A newly verified direct seam remained beneath the VM: `NetworkErrorHandler.safeCall` dispatches to IO before `ExpenseDetailRepository` / `ExpenseBillSplitRepository` formerly called unqualified `bind()`. A→B during that queue could send A's id/RV to B despite a later UI callback guard. Root accepted FIX. Both `ExpenseFactCommandActions` signatures and `ExpenseRepository` facade now require the original `LogicalSessionBinding`; both concrete owners call existing `bindExact(expectedBinding)` inside safeCall. The unbound overloads are physically gone. | `ExpenseFactViewModelTestSupport` migrates the two port overrides. Actual API interfaces, DTOs, FakeApiService and LedgerRepository API fixtures retain their original signatures because transport bodies did not change. New `ExpenseFactWriteBindingTest` reuses the real facade/session/DAO fixture: two explicitly queued A requests released after B rebind must fail with zero transport calls; unchanged binding must send the original expense/RV and invitation arguments successfully. These three new cases are source only, not claimed as executed RED or GREEN. |

The implementation keeps readiness, mutation acceptance and request lifetime separate. Root-readiness/RV are pre-send guards; original binding still guards both success and failure after send. A current-binding accepted success is never discarded merely because a refresh installed a new RV. Items acknowledgment and offset operations retain their already established FIFO/OCC owners; existing relation cancellation does not become a new snapshot-derived command. No original correction bytes/key/OCC, root cache writer, budget callback or recovery deletion owner is duplicated here.

Verification is bounded to the changed files: existing pinned Lizard was invoked through the repository's real `_lizard_functions` producer, resources parsed as XML, required signature/call sites and imports checked, original three VM test files compared with HEAD, and each changed path passed individually through the existing CI scope classifier. Lizard's returned records for the initial nine VM/UI paths had no over-80/over-15 record, but several extension/expression owners were not returned by that parser; this does not replace the real main/unit Detekt and full weight jobs. No local Gradle, PG, emulator, application run, commit or push occurred. The completed source and the new required-binding controls still need the next exact candidate's cloud compilation and actual test results.

### Additional current formal findings: persisted split admission and delivered-cache recovery

Live review readback at remote `06294fc8` returns 22 unresolved threads, without a further page. New `fvV__` is covered by the shared positive split admission candidate. The precise defect is persisted replay: the existing `ExpenseSplitDraft.toRequest` already refuses zero on first save, while a stored version-1 request bypasses that draft mapper. This corrects the earlier interpretation of the zero-valued draft producer; first-save controls must not be described as newly failing business behavior.

New `fvWAF` is FIX within the same correction delivery responsibility. `CorrectExpenseDispatcher` correctly preserves known 2xx delivery when cache publication fails, but only an active detail model exposes its refresh-pending state. Both global Sync entrances unconditionally hide every supported Done row; no-detail delivery can therefore leave old financial cache with no visible recovery exit. The confirmed callback starts the existing budget check and is not evidence of canonical cache adoption.

Before-change impact closure: the sole sender/real AppContainer callback produces the cache failure; the drain owns the durable Done transition; the existing outbox row and binding/cleanup rules remain its persistence owner. The detail, both global Sync sections/overview and existing authoritative fact GET/cache path consume delivery/readiness. The old successful exit to retire is hiding a delivered correction regardless of whether its fact was adopted. Any fix must preserve known delivery, original bytes/key/OCC and no command replay, while keeping a durable readable refresh requirement until authoritative cache adoption succeeds. It must cover failed refresh, Room reopen, binding changes and stale reads; this does not authorize another financial writer, queue, status-as-fake-failure or automatic resend.

The new `ExpenseCorrectionRoomContinuityTest.deliveredCorrectionWithFailedCacheRemainsRecoverableAfterRoomReopenWithoutDetail` uses the existing real Room fixture, actual dispatcher/drain and real global factory/UI with no detail model. After a real synthetic-transport 2xx and injected cache failure, it checks the old cache, reopens Room, requires the delivered-but-refresh-pending original card and review action, retains that state through failed GET, and requires successful bound GET/cache to clear the pending presentation without deleting or resending the original Done row. Its 49-line/CC2 source and unchanged class count of 11 functions pass the bounded structural check; actual execution is pending. No production refresh marker or runtime qualification is claimed yet for this new finding.

### Delivered-cache recovery decision and additional before-impact closure

The counterexample is now submitted as `360d5b99` and awaits its actual cloud result. Codex owns this reversible recovery decision under the same full Goal and no-silent-loss delegation. Known 2xx remains Done. If its root cache publication fails, the existing row must retain a local refresh requirement together with the response's actual row version. Comparing a later read only with the original OCC is insufficient: another writer may have advanced the root between that OCC and the correction receipt. The refresh requirement clears only after a same-binding authoritative snapshot at least as new as the receipt is adopted. A failed, stale or obsolete-binding read cannot clear it, and no recovery action may resend the known-delivered command.

The existing row's operational error field can carry this local requirement; no Room column, request field, API version, command key or second recovery owner is needed. The sole correction dispatcher supplies it, and the existing drain/DAO Done transition persists it atomically with delivery. Ordinary successful dispatchers retain their existing Done behavior. The existing completed-row cleanup must retain a correction that still needs refresh; this does not introduce a GC producer or a new time-based retention policy. Explicit bound/quarantine cleanup retains its current authority.

All three original-submission card consumers must show the same refresh requirement. Both global overview consumers must count it as delivered work needing a read, separately from an unsupported old command whose delivery is unknown. Detail's shared root-readiness predicate and correction submission owner must continue to block a new snapshot-derived write while the durable requirement remains. Historical Done without a requirement is still historical delivery, not a new completion event.

The direct cache adoption paths are the bound detail GET, correction Drop's current duplicate GET, confirmed mutation-response cache owner, confirmed-stream synchronization, and ExpenseOffsetRepository's authoritative fact-bundle adoption. The first two should share the existing canonical read responsibility; the latter three already adopt versioned authoritative roots and should acknowledge only matching receipt versions after successful adoption. Pending-list wholesale replacement does not retire an old confirmed root and therefore cannot prove this confirmed-root recovery. Local cache reads, optimistic offline writes, image/item/history-only reads, and the budget/advice notification callbacks do not supply an authoritative root adoption. AppContainer's direct correction callback executes before Done persistence and remains the producer being acknowledged, not another recovery queue. These paths are explicitly accounted for rather than presumed unaffected.

The confirmed mutation-response consumers include direct/optional-offline Patch, Confirm, MarkNotDuplicate and manual create. Their existing IOException handlers can enqueue after a network failure. Consequently a failure to clear only the local refresh marker must retain the marker without turning their accepted server response into an offline fallback. Cancellation and binding refusal still propagate through the existing boundaries. Marker acknowledgment is subordinate cleanup after canonical cache adoption, not another financial submission result.

The existing real-Room counterexample remains the primary no-detail/reopen producer. Direct dispatcher controls must distinguish cache failure from notifier-only failure while preserving known delivery and cancellation; focused repository/DAO controls must retain the receipt barrier through stale reads, binding changes and completed-row cleanup, then clear it without changing original payload/key/OCC or another target. This decision is ready for implementation after the submitted counterexample is observed; no production marker or new runtime GREEN is claimed by this record.

### Actual 360d5b99 result and delivered-cache implementation closure

Connected job `101574393801`, artifact `9999118848`, executed 133 tests with two failures and no errors/skips. The new no-detail case reached line 300: its actual Room submit, one drain completion, one synthetic-transport server result at 1200, old cache at 1000, stored Done, Room/graph reopen and real global factory's delivered observation all passed. The refresh-pending card was absent; later review/failed-read/recovery/no-resend assertions were masked. All seven Debt Room cases passed. JVM executed 2184 tests with one remaining Drop assertion whose fixture had not awaited its real IO job; its assertions are retained and its operation is now joined. The presentation-copy gate separately identified three new Debt literals, now rendered through the existing UiText/resource path without changing their canonical predicates. These results qualify the preceding candidate only.

The sole correction dispatcher now returns the receipt's actual row version when root-cache publication fails. The existing drain passes it into the same DAO Done update, which stores `correction_refresh_required:<version>` in the original row's operational error field. Notifier-only failure does not create the marker; cancellation retains its existing propagation. Other successful dispatchers pass no marker. Payload, original key/OCC, owner, ledger and delivery meaning remain unchanged.

Outbox acknowledgment runs under the existing binding-transition lease, rereads this owner/ledger's Done correction rows and clears only an exact original marker whose target's adopted version reaches the receipt version. The DAO also checks correction type and Done status. Detail GET and correction Drop now share `ExpenseRepositoryCore.fetchAuthoritativeExpense`; the old duplicate GET is physically removed. Confirmed responses use the existing cache owner; non-confirmed responses retire only this root and its offsets under the existing monotonic transaction. Confirmed response caching, confirmed-stream synchronization and authoritative offset-bundle publication acknowledge after their existing cache lock has been released, avoiding a non-reentrant lock cycle. Cancellation/binding refusal propagate; failure of subordinate marker cleanup leaves recovery visible without changing an accepted online command into offline enqueue.

The shared original-submission card, both global Sync sections and both overview consumers now expose known delivery awaiting refresh. Overview counts it separately from unsupported historical delivery. The detail's shared root predicate and the actual correction submit owner refuse a new snapshot-derived correction while the marker remains. No Retry/Drop is introduced for a supported Done row. The existing completed-row cleanup retains marked corrections; after successful acknowledgment its original retention behavior resumes. There is no new GC producer, table, column, server request, API version, queue or scheduler.

The original real-Room oracle is unchanged. Four focused repository controls cover receipt RV 11 versus a stale read at RV 8 after original OCC 7, another target remaining intact, an obsolete binding's late read, fixed-clock cleanup before/after recovery, and failed local acknowledgment retaining recovery while preserving a successful adopted read. Existing dispatcher tests now assert the additional local receipt marker while keeping all known-delivery, notification, original OCC and cancellation assertions. The existing fake DAO implements the same CAS/cleanup behavior, and the overview control distinguishes delivered-refresh work from unknown-delivery work. These new sources await actual cloud execution. Resource XML, presentation-copy, each changed Android path's CI classification and diff checks pass. Pinned Lizard has no new over-limit record or file-size threshold crossing; its pre-existing expression-function span incorrectly includes later methods in ExpenseOffsetRepository and grows from 106 to 107 lines when its actual publish method gains one acknowledgment call. This parser limitation is recorded rather than used to waive final Detekt/weight qualification.

### Workspace return-route production closure

The other `360d5b99` Connected failure was still Workspace's original reason assertion. Its earlier title assertion matched both the settings-root entry and the Sync page, so it did not prove return to the internal Sync destination. The actual SettingsDestinationHost stored its subroute with `remember`; leaving the outer MAIN composition for EXPENSE reset it to Root on return. Its sole SettingsRoute consumer now uses the project's existing enum plus `rememberSaveable` representation. All 15 destinations are parameterless and retain their names and back branches. Background editor draft/URI ownership stays in AppearanceViewModel, with the existing missing-editor effect returning to Appearance. Ordinary removal of the navigation entry still starts a newly opened Settings page at Root.

The real navigation test preserves every original click/detail/Back/reason assertion and additionally checks the unchanged original Room row, full binding, zero mutation sends and Sync's unique subtitle before accepting the return. The old candidate did not prove those new postconditions; current source review confirms the route-reset cause, while the strengthened actual navigation path awaits the next cloud candidate. No external route, second navigation owner, persistent protocol or fixture timing extension is introduced.

### Detail adoption after another consumer acknowledges the receipt

Before-impact review found one remaining consumer exit: a live detail can still hold RV 7 while another bound GET or stream adopts receipt RV 11 and clears the global marker. Merely replacing its observed correction list would then let that detail write from its old root. A newly opened route can also carry an old initial snapshot after the marker was already acknowledged. Global Room adoption and this detail's own snapshot adoption are distinct postconditions.

The current detail now retains the highest receipt RV it has observed under its binding. When another consumer clears the marker while this view still needs that version and has no relevant read in flight, it synchronously claims Loading and triggers the existing root/bundle refresh; this view must itself adopt at least its retained receipt RV before correction, new repayment or new split becomes writable. The initial route snapshot is first reconciled with the same-binding local Room projection, preserving a valid cache's offline entry. A missing cache can mean that an authoritative non-confirmed response retired the old root, so it falls through to the existing fresh-read owner and retains the write barrier on failure. Binding reset retires the old floor, and both generation and the complete observed binding reject a late initial cache result. Existing accepted results, original payload/key/OCC, DAO/Core adoption and bundle ownership are unchanged.

The existing correction-recovery owner holds this initial reconciliation and observation logic; no second cache or lifetime owner is introduced. Four focused VM controls cover another consumer's acknowledgment with an older own read, initial observation after acknowledgment while offline, retired cache plus failed authoritative read, and binding reset followed by an obsolete cache completion. They retain direct action refusal, original submission count and no invented advice-change event. The original Room producer and shared fake are unchanged. These four cases are source-complete only and require the next exact cloud JVM result; source checks are not runtime qualification.

Independent review identified and corrected a self-acknowledgment race before publication: the detail's own successful GET clears the marker before returning its root. That notification must not start a second GET and invalidate the first successful result, nor make an already adopted root stale if the unnecessary second read fails. Because bundle adoption also updates `expenseLoading`, actual root Job/generation now tracks the in-flight read and releases only its own generation on completion or cancellation; binding reset retires it. This is read-lifecycle bookkeeping, not another financial or recovery owner. A fifth focused VM control exercises acknowledgment before and after the original result is adopted, with any second GET failing, and requires exactly one GET, RV 11 writable, unchanged original row except marker cleanup, and no replay or historical edit event. All four preceding cases remain byte-for-byte intact. Final exact cloud execution remains required.

### 84f35cf1 post-adoption acknowledgment boundary

Actual Android fast `101581861706`, artifact `9999854921`, executed 2193 tests / 3 failures / 0 errors / 0 skips. All four new repository refresh controls, all five new detail recovery controls and all five OutboxStatusViewModel controls passed. One direct failure in `ExpenseRepositoryConfirmedSyncTest.bindingTransitionWaitsForCacheCommitThenClearsTheOldOriginProjection` exposed the distinction between adopting a cache and subsequently cleaning up its recovery marker: the original bound cache commit completed, the waiting binding transition then cleared that old projection, and the later acknowledgment threw a stale-binding exception. The existing test's successful committed sync result is a valid postcondition; the new cleanup must not redefine that completed operation as failure.

Before-change impact closure: this late acknowledgment is shared by confirmed-stream sync, direct confirmed mutation-response cache, bound detail/Drop reads and authoritative bundle publication. Their actual GET and cache-commit boundaries must still reject an obsolete binding before adoption. After an adoption has already completed, a later binding transition must instead make acknowledgment a no-op that preserves the original marker; it may not clear another binding's row or reverse the original operation's success. The canonical request guard, cache lock, full binding comparison and all original command bytes remain authoritative. The direct producer is the unchanged cache-commit/transition test above, alongside the already passing pre-adoption obsolete-binding and stale-version marker controls. This corrects the earlier decision to propagate a binding refusal from subordinate marker cleanup.

The acknowledgment owner now catches only the repository binding refusal from its own `requireStillActiveFor` check and returns before selecting or clearing any row. No pre-adoption guard is changed. Independent bounded review verified all four real acknowledgment sites occur after successful bound cache/retirement/stream/bundle adoption, and the normal acknowledgment still filters current owner/ledger/type/Done and checks the original marker plus receipt RV. It neither changes payload/key/OCC/status nor has a network submission exit. Cancellation and unrelated exceptions retain their previous paths. This is current source closure; the unchanged direct failure producer still requires the next exact execution.

The other JVM failure source was `DebtAdjustmentRecoveryViewModelTest`: its actual Drop launched a repository call on Dispatchers.IO, but the test inspected Failed rows after only `runCurrent`. Its early assertion then canceled the VM without joining it, allowing an IO continuation to resume after resetMain; the next test reported UncaughtExceptionsBeforeTest and had not reached its own business assertions. Both VM Drop calls now join their actual new recovery Job before assertions, while authoritative reads remain separate observation-owned jobs with their original explicit gates. All gates release before VM scope cancel-and-join, including the reopened model. The original business assertions remain intact; no production Debt change, sleep, timeout extension or test suppression was added.

The complete `84f35cf1` Connected execution `101581871410` passed all 133 tests with zero failures/skips in the original unfiltered lane, and CodeQL succeeded. This confirms that candidate's actual Room/navigation and other device controls; its CI remains failed by the JVM issues above. This success run's existing workflow retained screenshots but did not upload JUnit XML, so evidence is the actual complete execution log and verified same-tree checkout, not a claim of individually downloaded success XML. The new three-file source fix still needs its own exact CI/CodeQL/Connected qualification before formal resolution or merge.

### 8bf3e131 complete behavior and remaining test-quality correction

The exact 8bf3e131 Android fast job 101584870298 executed 2193 tests with zero failures, errors or skips; all three previous failures and the nine new refresh/recovery controls passed. CodeQL 34069772156 succeeded. Connected 101584866826 executed all 133 tests with zero failures/skips; its successful workflow retained screenshots and full execution logs, without success JUnit XML. All actual checkout producers were verified against source tree 617fdc647ab1f2db0dd5c8d67084a82e3178da5a. CI remained failed only because test Detekt measured assertInvitationActionsRespectCorrection at 62 nonblank lines, above 60.

Before/after impact is confined to the existing BillSplit test scenario: the complete correction form submission and its Pending precondition now have one named setup step, submitCorrectionBeforeSharing. Expanding that call reproduces the entire original file exactly; all 11 tests, actions, assertions, ordering, explicit refresh gates, original cancellation and subsequent successful invitation remain unchanged. Production entrypoints, command ownership, persistence, protocol and recovery exits do not change. Source and scope checks pass, but the actual Detekt result and final exact cloud qualification are still required before resolving the formal threads and merging.

### 7608943a offset admission and live delivered-cache adoption: before impact

Formal threads `PRRT_kwDOS5LrfM6fwWok` and `PRRT_kwDOS5LrfM6fwWom` were independently traced at source `7608943a2ec71080a713a85e60673cebd141b12b` and admitted FIX. Their source counterexamples are separate from that candidate's still-running cloud qualification; no new runtime RED is claimed. The authorized local work writes direct counterexamples before production and runs only short source checks, with JVM/Connected execution reserved for the next exact cloud candidate.

| Boundary | Current producers and consumers | Required outcome and preserved exits |
| --- | --- | --- |
| New offset admission | FactOffsetsSection refund/reversal actions; the refund sheet's chargeback choice; canSubmitOffset and submitOffset; ExpenseFactActions -> ExpenseRepository -> ExpenseOffsetRepository | An unresolved or unadopted correction blocks opening and submitting a new offset, including an already open draft. The final repository owner checks the current typed correction observation under the captured full binding. Trusted root plus failed optional bundle remains usable; existing-offset void remains unchanged. |
| Original money and queue | Offset draft parses minor units under the viewed root currency; the repository can currently queue behind CorrectExpense; successful correction delivery cascades root OCC to CreateExpenseOffset; backend money reads the current root currency | The old minor amount must not be reinterpreted after a currency correction. Refuse this admission before a new row or HTTP request; retain the original correction bytes/key/OCC. Do not rewrite queued payloads, alter all token cascading, or replace idempotency/currency owners. |
| Delivered root publication | AppContainer's real cache callback -> CorrectExpenseDispatcher 2xx/cache -> original Outbox Done -> the live detail's correction observation -> root/bundle reads | With successful cache publication and no refresh marker, the same live view must adopt that bound authoritative Room root while offline. It must advance beyond the original correction RV and cover the observed receipt floor; old/missing cache or a refresh marker keeps the existing read barrier and recovery path. |
| Cache/read lifetime | Existing initial-root cache reconciliation, root read generation, full correction binding and monotonic adoption; correction/repayment/invitation/new-offset readiness; independent bundle/history presentation | Reuse the existing cache adoption owner for new delivery without allowing a late old-binding read, stale cache, or an auxiliary network failure to overwrite the adopted root. Done stays delivered and no correction is resent; actual cache failure still requires authoritative recovery. |
| Direct verification | Existing offset VM/repository/dispatcher/drain tests; existing refresh-recovery and correction-outcome VM tests; full Android fast and Connected producers selected by the changed Android paths | Add the missing new-offset-during-correction and live-Done/cache-success/network-loss tasks, preserve the bundle-failure positive control and all earlier receipt/binding controls, and distinguish source checks from actual cloud results. |

### Offset admission and live-cache implementation closure

The new-offset UI actions, open method, enabled predicate and actual submit method now use the existing authoritative-root readiness boundary. An already open form retains its amount/date/reason while an unresolved correction blocks submission. Refund, chargeback and reversal share this admission; existing-offset void is unchanged. A failed optional fact-bundle read does not invalidate an otherwise trusted root or impose a new refundable-amount eligibility rule.

`ExpenseFactActions.createExpenseOffsetAllowingOffline` now requires the caller's full `LogicalSessionBinding`. Its sole production caller captures that binding and root before launching, rechecks both before submission, and rejects obsolete-binding result publication. The facade passes the binding into `ExpenseOffsetRepository`, which calls `bindExact` and reads the existing correction repository's typed observation before creating a key, row or HTTP request. An unresolved/unsupported correction, remaining refresh marker or unadvanced root refuses the new offset. The old unbound overload is retired. The facade, shared fake and all four existing/new direct repository call sites are migrated; the additional stale-binding counterexample exercises the same final admission. No DTO, backend amount/currency rule, offset void path, dispatcher cascade or original correction payload/key/OCC is changed.

Newly delivered corrections without a refresh marker now reuse initial-root cache verification. Its full binding, request generation and current-observation checks surround the actual local read. A completion cache must advance beyond the original correction OCC and cover the highest receipt version already observed by this VM. The same monotonic adoption step installs the complete cached Expense and clears stale/read-failure state; it does not merely enable actions. Auxiliary bundle/items/splits/history reads remain independent. A missing/unchanged cache, insufficient receipt version or remaining marker retains the original authoritative-read fallback. An obsolete binding's late cache cannot publish. Initial offline cache, historical Done, own-GET marker acknowledgment and original Retry/Drop semantics remain under their existing owners.

| Changed boundary | Direct verification and preserved consumers |
| --- | --- |
| Offset open/enable/submit and full-binding repository admission | Existing offset VM tests keep all six original scenarios, including trusted-root/bundle-failure registration and offset void. New pending-currency-correction assertions retain the whole open draft and original command while proving zero create calls. Actual repository tests retain all four original cases and add original-row/zero-HTTP refusal plus obsolete-binding/zero-row refusal. |
| New Done -> actual cache -> live detail | RefreshRecovery keeps the five existing receipt/initial-cache/binding controls. Three new scenarios cover successful cache plus offline root/bundle reads, unchanged/missing cache retaining the barrier, and a binding change during a gated completion-cache read. Successful adoption checks the full Expense, ready/not-stale state, no second root GET and an actual correction-sheet open; original command identity and no resubmission remain asserted. |
| Shared success/readiness consumers | Correction, repayment-draft, bill-split invitation and new offset actions consume the same adopted-root predicate. Bill-split eligibility refresh follows adoption. Existing offset cancellation and read-only presentation are preserved. No status, receipt marker, queue row or financial projection is fabricated by the VM. |
| Direct verification producers | Every changed Android path still selects Android CI. Presentation-copy and targeted pinned-Lizard/delimiter/diff checks pass; no new over-limit function/file category is introduced. The existing Lizard expression-function span in ExpenseOffsetRepository already included later methods before this change; its single over-80 record remains one, while real `voidAllowingOffline` behavior is unchanged. Final Detekt/JVM/Connected compilation and execution remain required on the next exact candidate. |

The first four counterexample bodies were saved before production in the qualification evidence directory; two further direct full-binding controls accompany the required API/cache-owner migration. These six new JVM test methods are source evidence only: no local JVM/Gradle or new runtime RED/GREEN was run. Existing test bodies and assertions are retained, apart from supplying the newly required binding in the two old direct-create calls.

The preceding exact `7608943a` completed all selected cloud gates: CI `34070958357`, CodeQL `34070958341` and Connected `34070958313` succeeded. Android fast `101588119305` executed its real JVM/ratchet, Detekt and lint tasks successfully, but this successful run uploaded no JVM XML and printed no exact JVM count; the prior candidate's 2193 count is not substituted. Connected `101588102431` logged all 133 tests completed with zero failures/skips, without success XML. Backend contracts `101588119206` passed 95 tests and presentation-copy; weight artifact `10000524411` reports HEALTHY GROWTH with no failures. The actual checkout `e64336134ced58f10b509b4796d1ee6a77c7c7d4` has the same tree `d6d4757702f11a92a3cca8b9a854b93944b95506`. These results do not qualify this dirty source fix. Final formal readback still contains 24 unresolved threads, including these two; no resolution or merge is claimed.


### 7608943a interleaved refund and currency correction: before impact

Root accepted the same-root race after the preceding read-only review: a refund HTTP request can remain in flight before any offset outbox row exists; a real composite currency correction is accepted in that window; IOException then persists the old refund behind it. The generic completion cascade replaces the refund parent OCC, and the create dispatcher sends the old minor amount against the new currency. The earlier correction-first refusal case did not exercise this sequence.

The decision is specific to CreateExpenseOffset: the original parent condition, key and complete request body are immutable. Automatic token replacement is not a valid refund-continuation capability when it changes the root under which the amount was understood. Other mutation cascades and the offset-entity OCC used by VoidExpenseOffset stay unchanged. GET-plus-rebase is rejected here: expected_row_version participates in the existing idempotency fingerprint, so changing it under an already attempted key cannot preserve the original HIT receipt. No backend API, currency, date, capability, schema, queue or scheduler is added.

| Direct boundary | Before and intended closure |
| --- | --- |
| Fact offset entry / already-open sheet / submit | The sheet parses amountText using the current Expense. A delivered currency correction can therefore reinterpret an old open input. Capture the viewed root when opening; root changes preserve the draft and old currency label but require explicit review. Reviewing adopts the current root, retains date/reason, and requires fresh amount entry if the currency changed. |
| Direct POST / queued sibling / IOException fallback | ExpenseOffsetRepository owns the original key and wire request. Preserve the positive original expected_row_version in its existing queue DTO rather than normalizing it to zero; both enqueue paths retain the original body and key. Direct success and authoritative bundle publication stay unchanged. |
| Replay / legacy / protocol | CreateExpenseOffsetDispatcher requires the original positive body OCC to equal the row OCC and sends that request unchanged. Old normalized-zero or otherwise unverifiable payloads remain as readable failure/review, with no automatic HTTP replay or false Done. |
| Cascade / explicit recovery | Exclude only CreateExpenseOffset from the existing automatic token cascade and DAO/Fake fresh-token Conflict/Failed paths. Preserve ordinary original-token Retry and explicit Drop. Both global sync entrances must stop offering KeepMine for this type and expose the existing fact-navigation callback for review. |
| Direct verification producers | The new gated real Repository test performs create HTTP entry, real submitCorrection, IOException fallback and the actual cascade owner before asserting the whole original refund row. Existing dispatcher tests add legacy no-send and key/OCC recovery controls; the real VM adds open-draft -> delivered JPY adoption -> no-send -> explicit review/new input. These new tests are source-only and have not run locally; exact cloud Android fast/Connected remain required. |

### 7608943a immutable refund command: after impact

The queued create now serializes its original complete `ExpenseOffsetCreateRequestDto`, including the positive parent RV. The single supported reader is shared by the dispatcher and the existing ExpenseRepository facade consumed by global recovery. It requires a key and matching positive body/row RV; dispatch sends that original DTO and key unchanged. A zero-normalized legacy request, inconsistent row or missing create target stays Failed with `offset_create_requires_review`. Create-specific 404/otherwise-Discarded outcomes cannot become Done. Existing direct POST and successful bundle publication remain the same; a lost successful response can still reach the original server idempotency HIT because its fingerprint is not rewritten.

| Affected owner and consumer | Completed source closure / retired exit |
| --- | --- |
| Outbox completion and persisted recovery | `cascadeFreshToken` preserves CreateExpenseOffset as well as the already protected commands. Both real DAO fresh-token UPDATE predicates and Fake equivalents reject only the added create type. Ordinary original-key Retry and explicit local Drop remain; existing Patch and Void behavior is retained. The normalized-zero writer, dispatcher row-RV replacement and CreateOffset KeepMine/fresh-token exits are physically retired. |
| Both global sync routes | Their existing factory, required ExpenseRepository and `SyncStatusActions.onOpenExpense` still lead to the same global VM and card. Create conflicts and unverifiable failed creates offer fact review plus explicit Drop, with no overwrite/retry button. Direct VM KeepMine also refuses; supported failed creates retain original Retry. The existing label resource mapper is consumed directly by these cards; its redundant one-line composable wrapper is removed. No new navigation owner or recovery writer is introduced. |
| Existing refund/chargeback/reversal form | Opening freezes the source Expense. Submit requires the adopted root to match that source and parses money using its original currency. A changed root preserves the draft and old currency label; explicit review adopts the current root, retains date/reason and same-currency amount, and clears amount after a currency change. Binding reset still retires the form. Queued-send binding checks and accepted-success handling from the preceding slice remain intact. Void and independent failed bundle reads retain their original authority boundaries. |
| Verification producer closure | The original six Repository methods remain present: five are byte-for-byte unchanged from the pre-P1 working snapshot; only the old refund payload-RV-zero assertion is replaced with equality to the original sent DTO, retaining its key/amount/binding/status checks. The existing direct-409 VM positive control now requires explicit review after root adoption and verifies preserved same-currency input. The new gated Repository race, dispatcher legacy/mismatch/404/immutable-recovery controls, VM currency-adoption control and global KeepMine control are source-only. Existing Compose tests add actual review/no-overwrite/no-retry button controls. A single new `ExpenseOffsetRoomIdentityTest` reuses the real disk Room fixture to cover reopen, cascade, both real SQL recovery refusals, ordinary original Retry and an unchanged Patch positive control; the original correction Room oracle is untouched. |

Short source verification passed presentation-copy (47 existing allowlisted residuals), strings XML parsing and `git diff --check`. Each changed Android production/test/resource path independently selects Android through the existing CI classifier, also used by Connected scope. Pinned Lizard found no increase in over-80-line or over-15-complexity records and no newly crossed file-size category; its existing expression-function span in ExpenseOffsetRepository still includes later methods and it does not report all Kotlin expression functions. These checks do not prove Detekt, Kotlin compilation, JVM, Room SQL or Connected execution. None of these new P1 controls has been run locally, and the preceding 7608943a cloud success is not evidence for this dirty implementation. No schema, expiry, backend, capability, queue, other cascade owner or Windows lifecycle behavior is changed.

### f044900c actual execution and verification-precondition correction

Source `f044900ca5a4d592eabb028790ac90671be77980`, tree `7ea92dc58dd3c73adaa40a7a3298d41ddc9618f4`, reached JVM execution in Android fast job `101601804875`. Its original XML artifact `10002139340` contains 377 suites / 2206 tests / 3 failures / 0 errors / 0 skips. CodeQL `34075888114` succeeded; CI `34075888081` failed. Connected `34075888093` / execution `101601807004` finished 136 tests with BUILD SUCCESSFUL; that successful run retained nine previews but no XML, so the execution log does not independently establish individual final case or skip counts. Source and actual checkout `70ddf629fc4f65481d7d5ce7fc53be78e33f17df` were independently verified to share the same tree. These are executed results, not proof that the following changed source is qualified.

Root and two independent bounded readers traced all three assertions. The offset recovery test already passed fresh-token refusal, whole original-row preservation and ordinary Retry before its first failure: real DAO and Fake both persist `manual_retry`, while the new expected-row copy incorrectly required null. Only that expected marker is corrected; the complete row/key/body/OCC comparison and subsequent explicit Drop assertions remain.

The repayment and invitation tests gated only network `fetchExpense` while their shared fake's cache immediately returned the newly assigned RV2/1400 Expense. With Done and no refresh marker, this is successful authoritative-cache publication, which the current contract deliberately allows the live view to adopt offline. Their own existing repository delegates now explicitly expose the pre-correction cache for repayment and an unavailable cache for invitation. Their original loading barrier, failed refresh, existing-invitation cancellation, retry, subsequent successful action and no-correction-resubmission assertions remain. The separate successful-cache/offline positive control and unchanged/missing-cache negative controls all passed in f044; no production barrier, cache owner or success exit is weakened to satisfy an obsolete network-only premise.

Before/after impact is limited to these three direct verification producers. Production admission, queue/persistence/protocol, original command recovery, binding and consumer actions are unchanged. No shared fake default changes, test-count reduction, suppression or threshold change is introduced. All previously masked downstream assertions still require execution at the next exact candidate; no local Gradle, PostgreSQL or device suite is run.

### 0d29c936 stream projection and recovery wakeup: before impact

Final review readback found two further concrete consumers: `PRRT_kwDOS5LrfM6fxK9_` (P1) and `PRRT_kwDOS5LrfM6fxK-B` (P2). Root accepts both FIX after tracing current production. A plain corrected Expense is upserted while old stream date/contribution/sort/lineage fields are preserved. The live ledger can therefore combine a new card amount with an old total/month; recurring-occurrence expense selection also consumes that stream. The successful-dispatch notification currently reaches only the budget checker. The fix must publish or invalidate through the existing authoritative stream owner, including a changed month, without computing financial contributions or accounting dates from device-local values. A failed derived read cannot resend an already delivered correction. A real Room/Repository/dispatcher/engine and live Ledger VM counterexample is being added before this production change.

The recovery finding is also reproduced in source: correction Drop conditionally deletes its blocker after authoritative-root adoption, but only Retry schedules the existing worker. A previously queued VoidExpenseOffset can remain Pending despite a successful Drop. The same generic `resolveFailed` and `resolveConflict` owners currently rely on their callers to arrange a wakeup; the global sync VM, income-plan recovery and recurring-occurrence recovery do not. Correction and debt-adjustment repositories add partial Retry-only notifications. This is a shared recovery-publication boundary, not a missing UI button.

| Recovery boundary / consumers | Planned ownership and preserved exits |
| --- | --- |
| Generic Failed Retry/Drop and Conflict KeepMine/Drop; global sync, income, recurring, correction/debt Retry | The existing Outbox mutation owner must notify the existing scheduler after a successful conditional recovery change and after releasing its binding lease. A rejected or stale action must not notify. Remove caller-side duplicate wakeup decisions; preserve each mutation's existing key/OCC, expiry and status rules. |
| Correction detail and both global sync entrances | All use the existing correction recovery repository and its sole `discardCorrection` call. Keep authoritative GET/adoption before supported Drop and status-conditional deletion; wake remaining original commands only after the accepted change. Unknown/definitively missing targets retain explicit local handling. |
| Explicit adjustment abandonment | Its existing status owner preserves the original command as Abandoned. This terminal transition can remove a FIFO blocker and needs the same continuation signal; it must not become Done or imply server success. |
| Other deletion/publication paths | clearAll leaves no rows to continue; quarantine deletion affects owner/ledger rows excluded from the active queue; Done GC deletes nonblocking rows. Upload Stop in the separate Capture candidate removes all unfinished rows of its entire target, while original upload Retry already schedules. Engine delivery continuation belongs to the existing bounded drain and scheduler work in Capture. These are verified distinct postconditions, not assumed unaffected consumers. |
| Direct producers | Add an actual correction-repository -> queued VoidExpenseOffset -> Drop -> existing engine continuation control for both Failed and Conflict, retaining original body/key/OCC. Shared recovery controls must prove committed state before notification and zero notification on repeated stale actions. The already running 0d29 cloud candidate cannot qualify these new tests or future production edits. |
