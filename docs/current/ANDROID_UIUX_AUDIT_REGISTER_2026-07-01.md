# Android UIUX Audit Register - 2026-07-01

This register turns the Android IA/UIUX Phase 0 audit into an execution ledger.
It tracks each product-design issue by page, authority risk, verification need,
and commit slice. The Phase 0 audit defines the direction; this file is the
working control board for closing it without losing gaps between slices.

Read with:

- `docs/current/ANDROID_IA_UIUX_PHASE0_AUDIT_2026-07-01.md`
- `docs/current/ANDROID_UIUX_IA_REFERENCE_2026-07-01.md`
- `docs/current/UIUX_GAP_REGISTER.md`
- `docs/current/CODE_DEBT_REGISTER.md`
- ADR-0023, ADR-0024, ADR-0042, ADR-0044, and ADR-0055

## 2026-07-01 Scope Lock

The current goal is a product-design-led Android IA/UIUX refactor, not a
surface polish pass. This register is the acceptance ledger for the five root
pages, Settings secondaries, chart/insight data expression, and authority-state
language.

Do not mark a page done because one visible defect is fixed. A page closes only
when its product job, data source, offline/cache behavior, layout density,
resource-backed copy, interaction feedback, and device evidence are all recorded
below. If a feature contract is missing, register it here or in
`UIUX_GAP_REGISTER.md`; do not fill the gap with hardcoded Android data.

## Status Legend

| Status | Meaning |
| --- | --- |
| Registered | The issue is accepted and not yet implemented. |
| In progress | A slice is active or partially landed, but verification is not complete. |
| Fixed | The issue has a local commit or documented resolution. |
| Needs QA | Code or copy has landed, but true-device or screenshot review is still required. |
| Deferred | The issue needs a backend/API contract, dependency decision, or separate ADR before implementation. |

## Audit Gates

Every UIUX slice should close these gates before being called done:

| Gate | Required evidence |
| --- | --- |
| Product job | The page answers its assigned job before controls and decoration. |
| Backend authority | Confirmed facts, stats, duplicates, roles, members, devices, and settings remain server-authoritative. |
| Offline clarity | Cached, queued, failed, conflict, read-only, and direct-only states are named as user states. |
| Visual system | The page uses shared Android tokens and shell components without heavy card stacking. |
| Copy | User-visible Android copy comes from resources and avoids engineering wording. |
| Interaction | Primary actions, back behavior, sheets, keyboard, bottom nav, and error recovery have a closed loop. |
| Number layout | Amounts, counts, dates, and labels fit on narrow phones without ellipsis hiding the main fact. |
| Data expression | Charts, rankings, and summaries show a readable comparison or degrade to facts when data is sparse. |
| Verification | Relevant unit/Compose/ViewModel tests, detekt/lint/build, and true-device or emulator evidence are recorded. |

## Page Register

| ID | Surface | Status | Priority | Finding | Acceptance |
| --- | --- | --- | --- | --- | --- |
| AIA-001 | Global shell | Needs QA | P0 | The five root pages have a shared shell, but the final true-device pass must verify header rhythm, status language, bottom padding, and dense pages together. | Today, Pending, Ledger, Insights, and Settings share one skeleton language without becoming the same card layout. |
| AIA-002 | Data authority | Needs QA | P0 | Backend authority and local cache labels exist, but every page must keep cached/offline state below server facts. The Connection page now filters its confirmed account snapshot by `serverSettingsFresh` so stale settings cannot keep the confirmed state after a refresh failure. | Fresh backend, local cache, read-only, queued, conflict, and failed states are visibly distinct and never imply fake server truth. |
| AIA-003 | Today | Needs QA | P0 | Today now gives the month amount full width, includes the source/status in the month line, and derives a single next action from real pending state; true-device review still needs to judge density and amount fitting with live data. | The first screen states source, pending workload, month status, and next action without truncating large amounts. |
| AIA-004 | Pending | Needs QA | P0 | Pending now has a real queue overview derived from `state.items`; the density pass reduced repeated top metrics, and the current real-item pass replaces the default `ExpenseCard` stack with a dedicated scan-first review row showing merchant, time, real issue signals, full amount, and the next action. Broader action/empty/offline QA and true-device screenshots remain open. | No fake duplicate/ready counts; the user can immediately see ready, missing amount, missing merchant, duplicate, and blocked work; item rows stay scan-first when the queue has real records. |
| AIA-005 | Ledger | Needs QA | P0 | The 2026-07-01 ledger density slice compressed the header, moved "记一笔" into the summary row, removed duplicate full dates from default rows, and tightened list typography. Multi-day long-history sampling still needs a larger backend dataset. | Confirmed records scan by day/month with compact rows, useful filters, no bottom-nav obstruction, and no large-card pile. |
| AIA-006 | Insights root | Needs QA | P0 | Insights has been reshaped, and the 2026-07-01 true-device pass fixed two authority-language defects: Budget no longer shows generic "month insight" content when no budget exists, and Goals no longer labels zero goals as stable. The Budget secondary page now treats `configured=false` as a not-enabled state instead of drawing progress, remaining amount, category execution, or excluded-detail modules; the Budget drill's stacked cards are now open sections with non-truncating amount lanes. The Debt Goals secondary page now opens with an authority strip, a true goal/evaluation summary, divider-row goal and linked-debt lists, and a clearer split between repayment goals and spending-control goals. Tag-filtered Insights now hides global Budget/Goals tabs and global recurring/pending/upload modules, and its Trend tab renders an explicit tag-scope summary from monthly stats plus the tag-filtered recent trend instead of falling back to unrelated global report modules. Full tag-scoped Reports Overview remains a backend API gap, so Android does not invent budget, goal, or global report conclusions in the tag view. Full-page answer-flow review remains open. | The page answers spend, comparison, recent signal, top contributors, and action signals in that order; every tab and drill page uses the same answer model and does not reuse an unrelated module. |
| AIA-007 | Insights charts | Needs QA | P0 | Recent 7-day and month trend views must show comparison, variation, concentration, or degrade to facts. The current trend-expression slice keeps dominant/sparse degradation, adds a product takeaway to the month trend, adds native Canvas average guide and peak emphasis, and renders the recent 7-day daily bars with all labels plus previous/recent 3-day comparison in both Reports and the standalone fallback. | No chart exists only as decoration; dominant or sparse data becomes facts/ranking, not flat bars or fake lines. |
| AIA-008 | Insights merchants | Needs QA | P0 | High-frequency merchants and spend-ranked merchants must stay separate. Reports overview is requested with `ranking_metric=count`, and the merchant ranking row switches title/value/supporting text from the returned metric. | Frequency lists sort by confirmed count; spend lists sort by amount; each row shows the active metric clearly. |
| AIA-009 | Settings root | Needs QA | P1 | The 2026-07-01 Settings root density slice tightened navigation rows and section titles, and initial server-settings fetch now presents a refreshing cache state instead of immediately labeling the page offline. Secondary inventory remains open. | Account, ledger/family, device, connection/sync, data rules, safety, and appearance are grouped by responsibility. |
| AIA-010 | Settings secondaries | In progress | P1 | Secondary pages are uneven. Sync status has improved, Recycle Bin has a compact overview/list model with explicit read-only, empty, loading, failed, restoring, and backend-count states, Appearance/background has a first-read overview plus resource-backed labels, Homepage Modules now moves backend-backed dashboard-card configuration into a ViewModel with scan-first overview/order sections, and Notification Preferences now separates device notification preference, system authorization, reminder permission, and server-ledger authority in a compact overview/list layout. The current Category Rules slice makes rules the first-read content, collapses create/edit until requested, and lets Settings secondary routes hide the global bottom nav. Other pages still need page-by-page state parity. User follow-up: second-level surfaces must match the five root tabs and avoid isolated rounded-card language. | Each secondary page has consistent title, back, explanation, loading, empty, error, cached/offline, read-only, success, destructive states, density, and component language. |
| AIA-011 | Copy and resources | Needs QA | P0 | New Android UI copy must not be hardcoded, and product copy must avoid engineering phrasing. Background Tasks now resolves task type/status labels through resources and maps unknown backend tokens to generic product copy. | Touched user-visible strings live in `res/values/*.xml`; grep review finds no new hardcoded Chinese/English UI copy. |
| AIA-012 | Interaction closure | Registered | P0 | Confirm/reject/edit/undo/retry/read-only paths need consistent feedback across Today, Pending, Ledger, and Settings. | Every write action shows success/failure/queued/conflict and gives retry or recovery where applicable. |
| AIA-013 | Chart dependencies | Fixed | P1 | Android Vico was previously allowed but later retired for the current chart need. Future dependency work must not be aesthetic-only. | Native Canvas/token charts remain current; new chart dependency requires ADR-0023 review, official metadata, size/accessibility impact, and fallback. |
| AIA-014 | True-device review | Registered | P0 | The official package must be checked on a real phone after the audit-led slices, not only by code review. | Backend is started, official `com.ticketbox` is bound by pairing code, and screenshots/notes cover scroll, clipping, keyboard, nav, dark/light, and current real data. |
| AIA-015 | Visual density | Registered | P0 | Some screens can still feel crowded because summary, tools, filters, and records compete in the same viewport. | Each root page has one first-read story, compact secondary controls, and no nested pile of large rounded cards. |
| AIA-016 | Product copy tone | Registered | P0 | Offline/cache/error language must sound like product state, not casual chat or engineering status. | Copy uses calm states such as updated, offline data, waiting to sync, unavailable, read-only, and retry; no release copy says "backend", "local db", or casual phrases. |
| AIA-017 | Amount and metric fitting | Needs QA | P0 | Large monthly amounts and merchant/category metrics must never hide the primary number behind ellipsis. The Budget secondary page now lets summary/list amount lanes wrap to two lines instead of ellipsizing the primary value; true-device review still needs large real amounts across the five main tabs and secondary pages. | True-device screenshots show full major amounts and count/amount labels across Today, Ledger, Insights, and Settings summaries. |
| AIA-018 | Ledger long-list efficiency | Needs QA | P0 | Default rows are now compact and day headers own date/subtotal context instead of repeating dates per record. The remaining risk is long multi-day data where group collapse thresholds and scroll depth need real-device review. | Ledger supports compact scan, day/month grouping, collapse where useful, and bottom-safe record tails. |
| AIA-019 | Insight drill contracts | Deferred | P1 | Mature insight reading needs "why" and "show me the bills" paths, but Android cannot invent ledger query truth. | Drill targets are added only when backed by backend-authoritative query/filter contracts and tested request parameters. |
| AIA-020 | Settings secondary inventory | Registered | P1 | Root Settings and Sync Status are not enough; every secondary page must be inspected and closed individually. Notifications now has a code-level pass with explicit auto-draft, system authorization, reminder, and privacy boundary states; true-device evidence is still pending. | Connection, devices, members, join family, rules, merchants, tags, recycle bin, export, security, background, appearance, homepage modules, notifications, and about each have a recorded state pass. |
| AIA-021 | All drill / secondary pages | In progress | P0 | User observation: second-level pages, including Insight Tag/Budget/Goal drill pages and Settings secondaries, feel semantically inconsistent with root pages and too card-heavy. The Budget drill now uses the shared secondary-workbench direction: header plus open summary/editor/execution sections, divider rows, resource-backed state copy, and a pure configured/not-enabled decision model instead of stacked card containers. Debt Goals now follows the same direction with open overview/progress/selection sections, divider rows for goals and linked debts, and resource-backed copy that avoids exposing unsupported savings-goal semantics as a fake Android state. | Secondary pages inherit the same page frame, row density, section rhythm, state language, and bottom-safe behavior as the root tabs while keeping their page-specific job clear. |
| AIA-022 | Type and capsule alignment | Registered | P0 | User observation: capsule containers and text baselines can look visually off, with labels appearing crooked or poorly centered. | Buttons, pills, segmented controls, and status chips use shared token heights, padding, typography, and baseline alignment; true-device screenshots confirm text appears centered and not cramped. |
| AIA-023 | About / product trust page | Fixed | P1 | User observation calls out "About Ticketbox" as part of the secondary-page mismatch. About is now a compact product trust page with version, confirmation boundary, sync authority, and screenshot/OCR boundary rows. True-device screenshots `142-about-page.png` and `145-about-back-light-clean.png` confirm the page has no global bottom nav, uses resource-backed copy, keeps icon/text alignment readable, and no longer opens with a heavy back-button capsule. | About reads as a polished product trust page, not a diagnostics dump; version/build/support/legal rows are resource-backed, compact, and consistent with Settings secondaries. |
| AIA-024 | Edit / confirm detail | Needs QA | P0 | User screenshot `Screenshot_2026-07-01-23-48-19-093_com.ticketbox.jpg` showed the amount/currency edit surface with the keyboard open: the bottom action area became a large rounded block, actions formed a heavy 2x2 grid, and the amount section still used old card language. Current slices fix IME-state propagation, also force the action bar into compact mode whenever the amount field is focused, move amount input before a single-line currency selector, and stop existing receipt edit pages from auto-opening the keyboard while preserving auto-focus for manual entry. The edit-detail slice replaces the preview, repayment draft, and date blocks with open secondary-workbench sections, token spacing, divider rhythm, and autosizing preview amounts so large values are not hidden behind ellipsis. True-device QA then exposed the same root problem in the manual-entry sheet: `LocalAppImeVisible` can be false inside `ModalBottomSheet`, so the form still placed actions below the keyboard. The manual-entry follow-up now forces the sheet to expanded height, reads `WindowInsets.ime` directly as a fallback, and moves the cancel/save row directly under the amount and currency section while the keyboard is open. Evidence: `115-manual-entry-keyboard-fixed-clean.png`. | The edit/confirm/detail and manual-entry pages use the same secondary-workbench language as Pending and Ledger, keep keyboard actions thin and thumb-reachable, avoid hardcoded placeholders, and do not hide the form behind a large action card. |
| AIA-025 | Refresh latency | Needs QA | P0 | User follow-up: frontend refresh/loading feels too slow. Current slices prioritize authoritative monthly stats before supplemental Insights modules, start Today's month refresh before Pending, stop Today/Pending/Ledger page-level pull indicators from being held while readable content already exists, treat empty Pending cache and previously synced empty Ledger as readable empty states, release Today/Insights pull indicators when existing monthly stats are readable, keep Today/Pending/Ledger/Insights authority strips from turning background sync into a strong whole-page loading state, raise confirmed-ledger sync page size from 50 to the backend-supported 200, share readable-refresh gating across Budget, Recurring, Bill Split, Debt, Income Plan, Receivables, Repayment Drafts, Debt Goals, Create Debt Goal, and Global Search, and now coalesce duplicate same-scope monthly-stats, reports, and confirmed-ledger refreshes while letting Insights budget refresh follow the primary monthly-stats completion instead of forcing an extra early request. True-device timing is still pending. | Pull-to-refresh keeps existing content/readable empty states visible, does not block the whole page on non-primary modules, reduces avoidable duplicate frontend-triggered requests and confirmed-ledger round trips, and never claims backend freshness before the primary authoritative response arrives. |

## Secondary Settings Register

| ID | Page | Status | Required closeout |
| --- | --- | --- | --- |
| SET-001 | Connection | Needs QA | Connection now shows the bound ledger address, separates confirmed service state from cached state, uses resource-backed role/scope labels, avoids exposing raw storage/role tokens, and only passes fresh server settings into the confirmed account panel. Final true-device verification is still required, especially under VPN/offline transitions. |
| SET-002 | Sync status | Fixed | Queued, conflict, and failed offline mutations are summarized from real outbox state. |
| SET-003 | Devices | Needs QA | Device rows now use compact scan-first rows, overflow actions, resource-backed loading text, and device-specific missing-activity copy instead of the generic "未填写时间"; final true-device screenshot after the overflow-action update is still required. |
| SET-004 | Members | Needs QA | Member rows now keep role/status visible and move role/transfer/disable actions into an overflow menu; loading and audit loading states use product copy. Final true-device screenshot is still required. |
| SET-005 | Join family | Needs QA | Join family now separates current device, invitation verification, identity, and accept actions; preview/accept feedback uses the page status slot and known roles resolve through resources instead of raw tokens. Final true-device verification is still required for both bound settings entry and cold-start invitation entry. |
| SET-006 | Category rules | Needs QA | Rule rows now use a denser scan-first surface with keyword, target category, priority/status, condition summary, and lighter row actions. The current category-rules slice makes both the rule list and rule-application history a single divider-row group instead of a stack of repeated row panels, collapses create/edit by default, and shows backend-returned rule rows before local editing controls. True-device evidence `136-category-rules-clean-no-bottom-nav.png` confirms the page opens as a full-screen Settings secondary without the global bottom nav covering the list; `137-settings-back-bottom-nav-restored.png` confirms the bottom nav returns on back to Settings root. Empty, conflict, queued, failed, read-only, stale, and form-expanded states still need a true-device pass. |
| SET-007 | Merchant management | Needs QA | Merchant Management now opens with a real catalog/alias overview, keeps catalog and alias lists as compact divider rows, moves rename/merge/hide/delete actions into overflow menus, and states that catalog merge changes directory/stat grouping without rewriting historical bill merchant text. Final true-device coverage still needs populated lists, read-only mode, conflict-driven merge, offline alias queue, and catalog delete/merge failure states. |
| SET-008 | Tags | Needs QA | Tag Management now uses a compact overview plus divider rows and overflow actions instead of per-tag card stacks, separates active vs unused tags with resource-backed copy, and keeps rename/merge/delete/undo server-confirmed. Final true-device coverage still needs populated lists, read-only mode, rename conflict steering, delete/merge undo, and stale-token failure states. |
| SET-009 | Recycle bin | Needs QA | Recycle Bin now keeps the backend `short_window_count` in the domain/UI state instead of inferring short-window rows from Chinese labels, uses an open overview plus divider rows instead of a single large card, and exposes read-only, empty, load-failed, refreshing, restoring, and restore-confirm states with resource-backed copy. Final true-device screenshot is still required. |
| SET-010 | Export/data tools | Needs QA | Data & Export now separates confirmed ledger records, this-device offline copy, and export scope in a compact open section, with resource-backed clear-copy copy and shared action buttons. The stale `DataExportScreen` LongMethod baseline is removed. Final true-device coverage still needs refresh, offline-copy clearing, empty export scope, and failure states. |
| SET-011 | Security | Needs QA | Security & Privacy now separates device verification, session credential storage, background privacy, offline-copy clearing, and leaving the ledger with resource-backed copy and open divider rows. The stale `SecurityPrivacyScreen` LongMethod baseline is removed. Final true-device coverage still needs locked/unlocked builds, clear-copy feedback, and leave-ledger confirmation. |
| SET-012 | Background tasks | Needs QA | Task labels now resolve through Android resources, unknown backend task/status tokens no longer leak into UI, and the page explains that task progress comes from the service while Android only views or requests cancellation. Final visual/state QA remains open. |
| SET-013 | Appearance/background | Needs QA | Appearance now opens with a compact overview for theme, currency, background, and motion state; skin, immersion, crop, category, and built-in background labels resolve through Android resources instead of enum copy; built-in preview titles are passed from the localized visible label. Background remains local-only per the appearance/background contract. Final true-device coverage still needs gallery, crop, preview, custom image, clear, dark/light, and bottom-safe states. |
| SET-014 | About | Fixed | About uses a compact product trust layout with resource-backed version, confirmation, sync, and screenshot/OCR boundary rows. True-device screenshots `142-about-page.png` and `145-about-back-light-clean.png` confirm full-screen secondary behavior, no bottom-nav overlap, readable row density, and the lighter shared back affordance. |
| SET-015 | Homepage modules | Needs QA | Homepage Modules now uses a `DashboardCardsViewModel` instead of direct repository calls in Compose, reads/saves backend-authoritative dashboard-card configuration through `DashboardCardsActions`, shows a compact overview before the reorder list, preserves drag/up/down/toggle interactions, and removes the old `DashboardCardsScreen` detekt complexity baselines. Final true-device coverage still needs populated modules, all-hidden state, read-only role, failed load/save, drag reorder, and bottom-safe action layout. |
| SET-016 | Notifications | Needs QA | Notification Preferences now opens with a compact overview for auto-draft, notification-listener authorization, enabled reminder count, and Android notification permission; it keeps reminder toggles as divider rows, makes read-only auto-draft unavailable from real role state, and states that notification settings are this-device behavior while ledger facts remain server-authoritative. The old `NotificationPreferencesScreen` long-method baseline was removed, and a pure summary model has focused tests. | True-device QA still needs listener authorization grant/revoke, Android notification permission grant/deny, read-only role, reminder toggles, and return-from-system-settings behavior. |
| SET-017 | Income record | Registered | The Settings root true-device pass shows the Income Record entry subtitle wraps into a taller row and weakens the scan rhythm. Treat this with the Income Record secondary-page pass: tighten the root subtitle, inspect the drill page for card-heavy layout, and keep income facts behind the backend income-plan/record contracts. |

## Functional Gap Register

| ID | Surface | Status | Gap | Required handling |
| --- | --- | --- | --- | --- |
| FUNC-INS-001 | Insights high-frequency merchants | Needs QA | The high-frequency merchant section must be count-ranked; amount-ranked rows cannot wear a frequency label. Android now requests `ranking_metric=count` for Reports overview and draws count as the active bar/value. | Verify with populated real merchant rows that high-frequency order, title, and primary value are count-led while spend remains supporting context. |
| FUNC-INS-002 | Insights spend ranking | Needs QA | Spend-ranked merchants/categories are useful but answer a different question from frequency. Android keeps amount-ranked rendering available when the backend returns `ranking_metric=amount`, with count as secondary detail. | Verify any amount-ranked surface is labeled as spend ranking and does not reuse the high-frequency title. |
| FUNC-INS-003 | Insight chart drill-through | Deferred | Charts do not yet close the loop from visible point/bar to filtered ledger records. | Register backend query contract first, then add UI drill and tests. |
| FUNC-INS-004 | Saved report presets | Deferred | Repeated analytical views are not first-class saved presets yet. | Add only after report query/filter contracts are explicit and server-authoritative. |
| FUNC-INS-005 | Insights budget state | Fixed | The Budget tab reused a generic "month insight" block, so an unconfigured budget could look like an active analytical module. | Budget now branches on real `BudgetProgress`: configured budgets show progress; missing budgets show a resource-backed "not enabled" state and budget setup entry. |
| FUNC-INS-006 | Insights goals state | Fixed | The Goals tab treated "0 enabled goals" as stable because status copy only checked for attention items. | Goals status now distinguishes Empty, Attention, and Stable; "stable" requires at least one real goal. |
| FUNC-INS-007 | Insights budget drill semantics | Needs QA | User observation: the Budget/Tag area can still show a mismatched "month insight" module and unclear remaining/options language even after the empty-budget root fix. The Budget secondary now uses backend `configured` to split not-enabled from active budget, hides active-only breakdown modules until a budget exists, and presents the drill as open summary/editor/execution sections rather than independent cards. | True-device review still needs the Budget page in not-enabled, active, over-budget, and read-only states; Tag drill semantics remain registered separately under AIA-021. |
| FUNC-INS-008 | Insights goal taxonomy | Needs QA | User observation: "goal enabled" is not semantically clear enough. Goals should distinguish repayment, saving, and spending-control goals instead of relying on a generic enabled/disabled idea. Android now labels existing backend-backed goal types as spending-control or debt-repayment in the summary, and the Debt Goals drill page only derives its overview from backend-backed `debt_repayment` goals and their evaluation blocks. | True-device review still needs populated goal rows and Debt Goals drill states. Saving goals remain a product/API contract gap; Android must not invent them without backend support. |
| FUNC-INS-009 | Insights tag scope | Needs QA | User observation: Tag-filtered Insights could still show global modules such as Budget or Goals, making the page read as a mismatched drill page. | Tag-active dashboard mapping now keeps only tag-relevant Overview/Trend/Category content and disables Budget/Goals tabs. The Trend tab now shows a tag-scope total, count, and recent-7-day signal from monthly stats and tag-filtered trend data. Full tag-scoped Reports Overview remains deferred until the backend exposes that contract; true-device QA still needs a real tag-selected screenshot. |
| FUNC-LED-001 | Ledger long history | Needs QA | Long ledgers can become repetitive vertical scrolling. | Header and default row density are fixed in the ledger density slice; next validation needs many-day real data to tune day folding without hiding confirmed facts. |
| FUNC-SET-001 | Settings secondary state parity | Registered | Secondary pages do not all expose the same loading, empty, error, read-only, cached, and destructive states. Recycle Bin closes its code-level parity pass, Appearance/background closes the first-read/resource-language pass, Homepage Modules closes the UI/repository separation pass, Notifications closes the local-device preference/system-permission summary pass, and Category Rules now uses divider row groups for the rule list and application history. Settings secondary routes now also tell the main shell to hide the global bottom nav while a secondary page is active. Data & Export and Security have their stale LongMethod baselines removed, but other secondaries remain open. | Close one page at a time and update the Secondary Settings Register. |
| FUNC-AUTH-001 | Offline creation and cache language | Registered | Android can create/cache offline, but must not present cache as authority. | Treat queued, syncing, failed, conflict, cached, and read-only as visible product states. |
| FUNC-PEND-001 | Pending compact scan density | Fixed | Compact mode still expanded duplicate-warning blocks and inline action buttons, so the first receipt could not be read in the first viewport. | Compact mode is now scan-first: duplicate state remains as a pill and action path, while large duplicate copy and inline buttons are reserved for Comfortable mode. |
| FUNC-PEND-002 | Pending real-item anti-card pass | Needs QA | User observation: with actual unconfirmed bills, Pending can still look like a stack of large cards, similar to the old Ledger problem. The current slice gives Pending a dedicated `PendingExpenseReviewRow` instead of reusing the Ledger-style expense card. | Default queue rows are dense, readable, status-first, and action-explicit; expanded containers stay out of the compact path. True-device review still needs real pending items. |
| FUNC-EDIT-001 | Edit keyboard action density | Needs QA | The receipt edit page is a shared path for Pending review, Ledger edits, and manual confirmation. With the keyboard open, a large action block can consume too much of the first viewport; the current slice also handles devices where IME visibility is not reported by compacting when the amount field is focused. The follow-up removes the remaining card-heavy preview/date/repayment blocks from the shared edit path and deletes the old detekt baselines for those field composables. True-device manual-entry QA showed that IME visibility can still be missed inside the sheet layer, so manual entry now uses direct IME insets and puts the action row immediately after amount/currency when the keyboard is open. | Keyboard/focused amount actions collapse into a compact action strip; primary and destructive actions remain clear; validation/status copy stays visible without becoming a large card. |
| FUNC-REFRESH-001 | Refresh loading latency | Needs QA | Root-page and drill-page refresh can feel slow when secondary requests start before the primary facts, keep the page-level indicator active after readable content or a readable empty state already exists, promote background sync to a strong loading strip, sync confirmed ledgers in too-small pages, or fire duplicate same-scope refreshes while the first request is still in flight. | Today/Insights prioritize authoritative monthly stats before supplemental work; Today/Pending/Ledger existing content, readable empty states, and readable monthly stats no longer hold the page pull indicator; Today/Pending/Ledger/Insights now reserve the strong refreshing authority tone for blocking first-load states; secondary/drill pages use shared readable-refresh gating; lifestyle stats and confirmed-cache sync continue behind the primary result; report goals no longer hold the reports/trend loading state after overview arrives; confirmed-ledger sync requests 200 rows per page; duplicate monthly-stats, report, and confirmed-ledger refreshes are coalesced by request scope; and Insights budget refresh follows primary monthly-stats completion to avoid the old forced duplicate request. |

## Slice Register

| Slice | Status | Commit or target | Scope |
| --- | --- | --- | --- |
| S0 Audit and reference | Fixed | `a66393e3`, `0a829305`, current docs register slice | Phase 0 audit, product references, initial gap register, expanded acceptance ledger. |
| S1 Shared shell / authority language | Needs QA | Existing Android IA commits on this branch plus current edit-detail secondary-workbench slice | Shared page skeleton, source/status strip, bottom safety, card reduction. |
| S2 Today cockpit | Needs QA | `05c4febd` plus true-device follow-up | First viewport hierarchy, amount fitting, real next-action priority. |
| S3 Pending queue | Needs QA | `6715c74b` plus current density and real-item row slices | Queue overview, compact review scan, batch and feedback closure. |
| S4 Ledger density | Needs QA | `1b8fb6bd` plus current ledger density slice | Day grouping, compact record surface, long-list safety. |
| S5 Insights answer flow | Needs QA | `575d9ebf`, `8e4e9d43`, Budget secondary semantics slice, Debt Goals drill slice, trend-expression slice, and current tag-scope slice | Merchant metric split, sparse/dominant trend handling, answer-first layout, trend takeaway plus readable 7-day/day-window charts, budget/goal empty-state authority language, Budget drill open-section parity, Debt Goals drill taxonomy/density parity, and tag-filtered Trend semantics. |
| S6 Settings governance | In progress | `b61dbedd` plus Settings root, My Devices, Family Members, Connection, Join Family, Category Rules, Background Tasks, Recycle Bin, Appearance/background, Homepage Modules, Notification Preferences, and current category-rule/settings-secondary shell slices | Root governance, secondary full-screen behavior, and secondary state parity. |
| S7 Debt cleanup and tests | Registered | Later focused slices | Same-page debt only; cross-domain debt stays in registers. |
| S8 True-device review | Registered | Final review doc | Official package, backend pairing, real-data visual QA. |

## Verification Register

| Check | Current state | Next requirement |
| --- | --- | --- |
| Documentation | Phase 0, reference pass, gap register, and this audit register exist; the register now includes page, functional, copy, density, and metric gates. | Keep statuses updated after every slice. |
| Gradle compile/detekt | Current Category Rules/settings-secondary shell slice passes focused `compileGrayDebugKotlin`, `detektGrayDebug`, and `detektGrayDebugUnitTest`. The previous manual-entry keyboard slice passed the full Android gate. | Rerun the full flavor-qualified gate before commit. |
| Unit tests | Today next-action priority, Today background-refresh authority tone, readable refresh-indicator release, Insights goal header-state decisions, Pending overview, row-density token decisions, tag-filtered dashboard tab mapping, tag-scope insight model decisions, primary-stats refresh completion, monthly/report/ledger duplicate-refresh coalescing, report overview-before-goals refresh completion, recent-window trend comparison, recent 7-day/full-label trend-axis decisions, Budget configured/not-enabled page decisions, Debt Goals drill summary decisions, Connection fresh-settings gating, Background Tasks label mapping, Recycle Bin summary/failure-state handling, Merchant Management summary handling, Appearance summary/resource mapping, Homepage Modules ViewModel/summary handling, and Notification Preferences summary handling have focused JVM tests. The Android test-count baseline is `1294`. | Add focused tests only where state authority or UI decisions are risky. |
| Lint | Required for broader Android slices. | Run before any final Android batch commit/push. |
| True device | Physical device `5c52fc22` installed the official `com.ticketbox` gray package against `https://api.zen70.cn`. Clean online screenshots `80-insights-budget-after-fix.png` and `81-insights-goal-after-fix.png` confirm Budget shows "未设置预算/未启用" and Goals shows "未设置" for zero enabled goals. Screenshot `83-pending-after-density-fix.png` confirms Pending compact mode shows two queue rows in the first viewport without expanded duplicate notices. Screenshot `90-ledger-density-final-settled.png` confirms Ledger header compaction, compact row meta, full amounts, and bottom-nav clearance on live data. Screenshots `95-settings-root-portrait-after-density.png` and `96-settings-root-settled-after-density.png` confirm Settings initial refreshing state and settled confirmed state. A later true-device pass installed the official package again on `5c52fc22`; `112-relaunch-after-manual-fix.png` captured live Today data, and `115-manual-entry-keyboard-fixed-clean.png` confirms manual entry keeps amount, currency, cancel, and save visible above the keyboard. Current pass installed the package again; ADB touch taps were unreliable on this device, so D-pad navigation was used to enter Settings and Category Rules. `136-category-rules-clean-no-bottom-nav.png` confirms Category Rules first-read layout with the create form collapsed and no global bottom nav; `137-settings-back-bottom-nav-restored.png` confirms returning to Settings root restores the bottom nav. | Continue physical-device capture for Connection online/offline/VPN behavior, Settings Devices, Family Members, Join Family bound/cold-start states, Category Rules edit/form-expanded, empty, offline, read-only, queued, conflict, and failed states, Recycle Bin empty/populated/read-only states, Appearance/background overview/gallery/crop/preview/custom-image/clear states, Homepage Modules populated/all-hidden/read-only/load-failed/save-failed/reorder states, Notifications with listener/system permission grant and denial states, Budget not-enabled/active states, Debt Goals list/detail/create states, populated Goals rows, tag-selected Insights, trend-expression charts, Pending with real unconfirmed items, edit amount/currency keyboard state, and Today/Insights pull-to-refresh timing before closing SET-001/SET-003/SET-004/SET-005/SET-006/SET-009/SET-013/SET-015/SET-016/FUNC-PEND-002/FUNC-REFRESH-001/FUNC-INS-007/FUNC-INS-008/FUNC-INS-009/AIA-024/AIA-025. |
| Screenshots | Local audit folders exist and are intentionally not auto-staged. | Commit only lightweight audit docs unless the user asks to include images. |

## Open Decisions

- Today cockpit still needs true-device review with the user's real large
  monthly amount so the UI never hides the amount behind an ellipsis.
- Product copy needs a release-facing pass. Casual fallback text and engineering
  labels should become calm product states before a page is called done.
- High-frequency merchants are a P0 insight metric issue: the section is not
  acceptable until it is count-ranked and visually distinct from spend ranking.
- Insights should not add a chart dependency until the missing interaction is
  specific: point selection, tooltip, grouped series, zoom, or export preview.
- Settings secondary work should proceed page by page; do not call Settings done
  after only the root page or Sync Status page.
- User-observed secondary-page drift now includes About, Insight Tag/Budget/Goal
  drill pages, Pending real-item rows, and the receipt edit/confirm detail page.
  Treat those as root-cause IA work, not isolated padding defects.
- True-device review remains a release gate for this Android UIUX goal, not an
  optional visual polish step.
